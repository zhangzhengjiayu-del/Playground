# Algebra: Span, Linear Independence, Subspaces Study Guide

这份总结对应题目里的所有题型。重点是先学会判断套路，再套到具体题目。

## 1. 向量个数、span、linear independence 的维数判断

在 `R^n` 里：

- 最多只能有 `n` 个线性无关向量。
- 少于 `n` 个向量不可能 span `R^n`。
- 多于 `n` 个向量一定线性相关。
- 等于或多于 `n` 个向量“有可能”span `R^n`，但不是一定 span。
- 等于或少于 `n` 个向量“有可能”线性无关，但不是一定线性无关。

### 快速判断表

| 说法 | 判断 |
| --- | --- |
| 所有 `R^4` 中 9 个向量都 span `R^4` | False，可能全是零向量 |
| 存在 `R^4` 中 5 个向量不 span `R^4` | True，例如 5 个零向量 |
| 存在 `R^3` 中 6 个线性无关向量 | False，最多 3 个 |
| 存在 `R^6` 中 8 个线性相关向量 | True，超过维数一定相关 |
| 存在无穷多组 `R^6` 中 3 个向量 span `R^6` | False，3 个太少 |
| 存在 `R^8` 中 6 个向量 span `R^8` | False，6 个太少 |

## 2. Span 的定义

设 `S = {v1, v2, ..., vn}` 是向量空间 `V` 中的向量集合，标量域是 `F`。

`span(S)` 是所有这些向量的线性组合组成的集合：

```text
span(S) = { x in V : x = lambda1*v1 + lambda2*v2 + ... + lambdan*vn,
            where lambda1, lambda2, ..., lambdan are in F }
```

注意：

- `lambda1, ..., lambdan` 是标量，所以属于 `F`，不是属于 `V`。
- `span(S)` 里面的元素是向量，所以属于 `V`。
- span 只要求“存在一组标量可以表示”，不要求标量唯一。
- 如果还要求表示唯一，那是在说这些向量线性无关。

## 3. Linear independence 的定义

向量 `v1, v2, ..., vn` 线性无关，当且仅当：

```text
如果 lambda1*v1 + lambda2*v2 + ... + lambdan*vn = 0,
那么 lambda1 = lambda2 = ... = lambdan = 0。
```

核心意思：

- 唯一能凑出零向量的办法，是所有系数都等于 0。
- 如果存在不全为 0 的系数也能凑出零向量，则线性相关。

常见错误：

- 不是要求 `v1 = v2 = ... = vn = 0`。
- 不是说“存在”一组全 0 系数，因为全 0 系数永远都能凑出 0。
- 正确关键词是 `whenever` / `if ... then ...`。

## 4. 判断子空间 Subspace

一个集合是子空间，必须满足：

1. 包含零向量。
2. 对加法封闭：如果 `u, v` 在集合里，那么 `u + v` 也在集合里。
3. 对数乘封闭：如果 `u` 在集合里，标量 `c` 任意，那么 `c*u` 也在集合里。

### 常见判断规则

| 形式 | 通常是不是子空间 | 原因 |
| --- | --- | --- |
| `ax + by + cz = 0` | 是 | 过原点的平面，是齐次线性方程 |
| `ax + by + cz = k`, `k != 0` | 不是 | 不包含零向量 |
| `x = lambda*v + mu*w` | 是 | 这是 `span{v,w}` |
| `x = lambda*v + mu*w` 且再满足一个齐次线性方程 | 是 | 两个子空间的交集 |
| `x = a + lambda*v + mu*w` | 通常不是 | 有平移，通常不经过原点 |
| `p(-2) = p(5) = 0` | 是 | 齐次条件 |
| `p(-1) = p(6) = 3` | 不是 | 非齐次条件，不含零多项式 |
| `p(x) = a*x^3 + b*x + c` | 是 | 缺少某些项没关系，系数自由且包含零多项式 |
| `p(x) = x^3 + a*x^2 + b*x + c` | 不是 | `x^3` 系数固定为 1，不含零多项式 |
| 有区间限制，如 `-4 < x < 4` | 不是 | 数乘可能超出范围 |

### 题目里的子空间答案

1. `A: 8x + 4y - 4z = 0`，`B: span(...)` 且齐次方程，`C: x^3 + ...`
   - True: `A`, `B`
   - False: `C`

2. `A: 4x + 5y + 3z = 0`，`B` 有 `= 2`，`C: p(-1)=p(6)=3`
   - True: `A`
   - False: `B`, `C`

3. `A` 有 `= -4`，`B` 有 `= 7`，`C: ax^3 + bx + c`
   - True: `C`
   - False: `A`, `B`

4. `A: -2x + 5y - 5z = 0`，`B` 有 `= 2`，`C: p(-2)=p(5)=0`
   - True: `A`, `C`
   - False: `B`

5. `A` 有区间限制，`B: span(...)` 且齐次方程，`C: x^3 + ...`
   - True: `B`
   - False: `A`, `C`

6. `A: 2x + 5y - 7z = 0`，`B` 是平移形式，`C: x^3 + ...`
   - True: `A`
   - False: `B`, `C`

## 5. 判断 closed under addition / scalar multiplication

题目通常让你输入：

- 如果封闭，输入 `CLOSED`。
- 如果不封闭，给一个反例。

反例格式：

```text
<1,2,3> , <4,5,6>
```

或数乘反例：

```text
<1,2,3> , 4
```

### 5.1 不等式半空间

```text
S = { x in R^3 : x1 - 2*x2 + 3*x3 >= 0 }
```

加法封闭：

```text
CLOSED
```

因为两个非负数相加还是非负数。

数乘不封闭，例如：

```text
<1,0,0> , -1
```

因为 `<1,0,0>` 满足条件，但乘以 `-1` 后不满足。

### 5.2 条件 `x3 - x1*x2 = 0`

也就是：

```text
x3 = x1*x2
```

加法不封闭，例如：

```text
<1,1,1> , <1,2,2>
```

两者都满足，但和是 `<2,3,3>`，而 `3 != 2*3`。

数乘不封闭，例如：

```text
<1,1,1> , 2
```

乘完是 `<2,2,2>`，但 `2 != 2*2`。

### 5.3 条件 `x3^2 - x1*x2 = 0`

也就是：

```text
x3^2 = x1*x2
```

加法不封闭，例如：

```text
<1,1,1> , <1,1,-1>
```

和是 `<2,2,0>`，但 `0^2 != 2*2`。

数乘封闭：

```text
CLOSED
```

原因是两边都是二次齐次：乘以 `lambda` 后整体变成 `lambda^2` 倍。

### 5.4 条件 `|x3| - |x1| - |x2| = 0`

也就是：

```text
|x3| = |x1| + |x2|
```

加法不封闭，例如：

```text
<1,0,1> , <-1,0,1>
```

和是 `<0,0,2>`，但 `|2| != |0| + |0|`。

数乘封闭：

```text
CLOSED
```

因为绝对值满足 `|lambda*x| = |lambda|*|x|`。

### 5.5 条件 `x3 - x1 - x2^2 = 0`

也就是：

```text
x3 = x1 + x2^2
```

加法不封闭，例如：

```text
<0,1,1> , <0,2,4>
```

和是 `<0,3,5>`，但应该有 `x3 = 0 + 3^2 = 9`，不是 5。

数乘不封闭，例如：

```text
<0,1,1> , 2
```

乘完是 `<0,2,2>`，但应该有 `x3 = 0 + 2^2 = 4`。

### 5.6 条件 `x3^3 - x1*x2^2 = 0`

也就是：

```text
x3^3 = x1*x2^2
```

加法不封闭，例如：

```text
<1,1,1> , <1,-1,1>
```

和是 `<2,0,2>`，但 `2^3 != 2*0^2`。

数乘封闭：

```text
CLOSED
```

因为两边都是三次齐次：乘以 `lambda` 后整体变成 `lambda^3` 倍。

## 6. 用增广矩阵判断一个向量是否在 span 里

如果题目问：

```text
x in span{v1, v2, v3}
```

意思是要找标量 `lambda1, lambda2, lambda3`，使得：

```text
lambda1*v1 + lambda2*v2 + lambda3*v3 = x
```

把 `v1, v2, v3` 放在矩阵的前三列，把目标向量放最后一列。

### 6.1 R^3 例题

```text
v1 = <2,3,-1>, v2 = <-4,-4,-4>, v3 = <2,1,5>
x = <x,y,z>
```

增广矩阵：

```text
Matrix([[2,-4,2,x],
        [3,-4,1,y],
        [-1,-4,5,z]])
```

需要满足的条件：

```text
4*x - 3*y - z = 0
```

所以答案不是 `NONE`。

### 6.2 R^4 例题

```text
v1 = <-5,-1,2,-3>
v2 = <5,-4,-3,-3>
v3 = <5,-9,-4,-9>
x = <x,y,z,t>
```

增广矩阵：

```text
Matrix([[-5,5,5,x],
        [-1,-4,-9,y],
        [2,-3,-4,z],
        [-3,-3,-9,t]])
```

需要满足两个条件：

```text
{ 11*x - 5*y + 25*z = 0, 3*x + 6*z - t = 0 }
```

等价写法也可以，例如全部乘以 `-1` 仍然正确。

## 7. 多项式的增广矩阵

多项式也可以看成向量。

在 `P2` 里：

```text
a + b*t + c*t^2
```

可以对应向量：

```text
<a,b,c>
```

所以列向量就是多项式的系数。

### 7.1 例题一

```text
r1(t) = 1 + 3t + 5t^2
r2(t) = 2 + 3t + t^2
r3(t) = 5 + 8t + 4t^2
q(t)  = 1 + t + t^2
```

增广矩阵：

```text
Matrix([[1,2,5,1],
        [3,3,8,1],
        [5,1,4,1]])
```

如果给出的 echelon form 是：

```text
Matrix([[1,2,-3,4],
        [0,1,7,6],
        [0,0,0,-3]])
```

判断 `p1, p2, p3` 是否线性无关：

```text
No
```

理由：

```text
There are non-leading columns in the first 3 columns of U
```

依赖关系：

```text
p3 = -17*p1 + 7*p2
```

输入：

```text
-17*p1 + 7*p2
```

### 7.2 例题二

```text
r1(t) = 1 + 3t + t^2
r2(t) = 3 + 10t + 2t^2
r3(t) = 1 + t + 4t^2
q(t)  = 1 + 7t - t^2
```

增广矩阵：

```text
Matrix([[1,3,1,1],
        [3,10,1,7],
        [1,2,4,-1]])
```

如果给出的 echelon form 是：

```text
Matrix([[1,3,1,1],
        [0,1,-2,4],
        [0,0,1,2]])
```

判断 `p1, p2, p3` 是否线性无关：

```text
Yes
```

理由：

```text
The first 3 columns of U are leading columns
```

因为它们线性无关，所以可以用它们表示 `q`：

```text
q = -25*p1 + 8*p2 + 2*p3
```

输入：

```text
-25*p1 + 8*p2 + 2*p3
```

## 8. 做题总流程

### 看到 span

问自己：

1. 是定义题吗？选“所有线性组合”。
2. 是判断能否 span `R^n` 吗？先看向量个数够不够。
3. 是具体向量 span 吗？把向量当列，目标向量放最后一列，做增广矩阵。

### 看到 linear independence

问自己：

1. 在 `R^n` 中向量个数是否超过 `n`？超过就一定相关。
2. 定义题选“如果线性组合等于 0，则所有系数都为 0”。
3. 矩阵题看前几列是不是都有 pivot。

### 看到 subspace

最快先检查零向量：

- RHS 是 `0`，通常有机会是子空间。
- RHS 是非零常数，通常不是。
- 固定某个系数为 `1`，通常不是。
- 有不等式、绝对值、乘积、平方时，要检查封闭性。

### 看到 closed under addition / scalar multiplication

1. 如果是线性齐次方程，比如 `x1 - 2x2 + 3x3 = 0`，通常加法和数乘都封闭。
2. 如果是不等式 `>= 0`，加法可能封闭，但负数数乘通常不封闭。
3. 如果有平方、乘积、绝对值，要尝试找简单整数反例。
4. 如果整个方程是同次齐次，比如两边都是二次或三次，数乘通常封闭。

