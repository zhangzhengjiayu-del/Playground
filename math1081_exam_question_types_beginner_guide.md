# MATH1081 上半部分题型与零基础做题指南

这份笔记根据你发来的上半部分题目整理。目标不是逐题背答案，而是让你第一次看到这些题时知道：

1. 它属于哪个知识点；
2. 题目在问什么；
3. 从哪里下手；
4. 常用公式或证明套路是什么。

后续还有题时，可以继续按这些模块补充。

---

## 0. 总览：这批题主要考什么

这批题大致分成 12 类。

| 模块 | 常见题型 |
| --- | --- |
| 集合 | 交集、并集、补集、对称差、集合族、幂集 |
| 逻辑 | 命题等价、蕴含、逆否命题、真值表、证明结构 |
| 证明 | 当且仅当、反证法、归纳法、抽屉原理 |
| 计数 | 排列组合、容斥、星与棒、函数计数、Wordle 类限制计数 |
| 图论 | 度数列、欧拉路、哈密顿圈、二分图、平面图、同构 |
| 加权图 | Dijkstra 最短路、Kruskal 最小生成树 |
| 偏序 | Hasse 图、极小/极大、最小/最大、上确界/下确界 |
| 关系 | 自反、对称、反对称、传递、关系矩阵 |
| 数论 | 整除、最大公因数、线性丢番图方程、同余方程、RSA |
| 递推 | 特征方程、齐次解、特解、初值代入 |
| 函数 | 单射、满射、双射、复合、反函数 |
| 形式化语言 | 量词、否定量词、数学语句翻译 |

---

## 1. 集合题

### 1.1 三个检测器的集合运算

题型特征：

- 给出 `X=A∩B`
- `Y=A⊖B`
- `Z=A∪B`
- 问哪些包含关系一定成立，例如 `X⊆Y∪Z`

核心知识：

- `A∩B`：同时在 A 和 B。
- `A∪B`：在 A 或 B 至少一个。
- `A⊖B`：在 A 或 B 其中一个，但不能同时在两个。也就是

```text
A⊖B = (A\B) ∪ (B\A)
```

关键观察：

```text
X = A∩B 是同时有两种化学物质的产品
Y = A⊖B 是恰好有其中一种的产品
Z = A∪B 是至少有一种的产品
```

所以：

- `X⊆Z` 一定真，因为同时有两种当然至少有一种。
- `Y⊆Z` 一定真，因为恰好有一种当然至少有一种。
- `X` 和 `Y` 不相交，因为一个是“同时有”，一个是“恰好有一个”。

做题方法：

1. 把全集分成 4 个区域：
   - 在 A 且在 B；
   - 在 A 不在 B；
   - 在 B 不在 A；
   - 不在 A 不在 B。
2. 写出 X、Y、Z 分别包含哪些区域。
3. 对每个选项检查左边区域是否都在右边。

---

### 1.2 集合族、并集、交集、幂集

例子：

```text
S_n = {k∈Z : 5n+3 ≤ k ≤ 3n^2+7}
A = {S_n : 14 ≤ n ≤ 22}
```

容易混淆的地方：

- `A` 是一堆集合组成的集合，所以 `|A|` 是有多少个 `S_n`。
- `B={x | x∈X for some X∈A}` 是所有 `S_n` 的并集。
- `D={x | x∈X for all X∈A}` 是所有 `S_n` 的交集。
- `C={x | x∈X for some X⊆A}` 这里 `X` 是 `A` 的子集，所以 `X` 的元素本身是一些集合。这个定义会让 `x` 变成 `A` 中的某个集合，而不是整数。
- `E={x | x∈X for all X⊆A}` 通常要特别小心空集，因为如果 `X=∅`，没有任何 `x∈X`。

区间型集合大小：

如果

```text
S_n = {整数 k : L ≤ k ≤ U}
```

那么

```text
|S_n| = U - L + 1
```

只要 `L≤U`。

---

### 1.3 幂集与笛卡尔积

例子：

```text
S = P({2,5}×{2,3,4}) × P({3,5}×{1,3,5})
```

核心公式：

```text
|A×B| = |A|*|B|
|P(A)| = 2^|A|
```

如果 `|{2,5}×{2,3,4}|=2*3=6`，那么它的幂集大小是 `2^6`。

所以这种题通常是：

```text
|P(A)×P(B)| = 2^|A| * 2^|B|
```

---

## 2. 逻辑题

### 2.1 命题等价分组

常见翻译：

令：

```text
P = X is skew-differentiable
Q = X is quantised
```

常用等价：

```text
P → Q  等价于  ¬P ∨ Q
P only if Q  等价于  P → Q
Q if P       等价于  P → Q
if P then Q  等价于  P → Q
逆否命题：P → Q 等价于 ¬Q → ¬P
```

不等价的常见陷阱：

```text
P → Q 不等价于 Q → P
P → Q 不等价于 P ∧ Q
P → Q 不等价于 P ∨ Q
```

做题方法：

1. 先用 P、Q 代替长句子。
2. 把每一句都化成 `¬P∨Q` 这种标准形式。
3. 形式相同的归为一组。

---

### 2.2 真值表补全

常见连接词：

```text
¬P       非 P
P∧Q      P 且 Q
P∨Q      P 或 Q
P→Q      只有 P 真 Q 假时为假，其余为真
P↔Q      P 和 Q 真值相同为真，不同为假
```

最重要：

```text
P→Q 只有一种情况是假：P=T 且 Q=F
```

做真值表顺序：

1. 先算括号最里面。
2. 再算非、且、或。
3. 最后算蕴含、双条件。

---

### 2.3 证明结构题

要证明：

```text
如果 X 是 multivalued，那么 X 是 doubly-Euclidean
```

记作：

```text
M → D
```

可以证明它的方式：

1. 直接证明：

```text
Suppose M. Therefore D.
```

2. 逆否证明：

```text
Suppose not D. Therefore not M.
```

3. 反证法：

```text
Suppose M and not D. This is a contradiction.
```

不可能证明原命题的常见错误：

- 假设 `M` 最后推出 `not D`，这是证明反面。
- 假设 `not M` 推出 `not D`，这是逆命题，不是原命题。
- 假设 `M and D` 得出矛盾，是证明 `not(M and D)`，也不是原命题。

---

### 2.4 逻辑推理顺序

例子：

```text
P1: s → ¬b
P2: d ∨ h
P3: h → b
P4: s
```

推理套路：

1. 由 `s` 和 `s→¬b` 用 Modus Ponens 得到 `¬b`。
2. 由 `h→b` 和 `¬b` 用 Modus Tollens 得到 `¬h`。
3. 由 `d∨h` 和 `¬h` 用 Elimination 得到 `d`。

规则名称：

```text
Modus Ponens: P, P→Q，所以 Q
Modus Tollens: P→Q, ¬Q，所以 ¬P
Elimination: P∨Q, ¬Q，所以 P
```

---

## 3. 量词与否定

### 3.1 否定“eventually even”

原句：

```text
∃N∈Z ∀n>N, a_n is even
```

意思：

存在一个界限 N，从那以后每一项都是偶数。

否定：

```text
∀N∈Z ∃n>N such that a_n is odd
```

意思：

无论你给多大的 N，我都能找到更后面的一项是奇数。

量词否定规则：

```text
¬(∃x P) = ∀x ¬P
¬(∀x P) = ∃x ¬P
```

---

### 3.2 函数不是 eventually increasing

原定义：

```text
∃x ∀y1 ∀y2 [y1>x and y2>y1 → f(y2)>f(y1)]
```

要证明不是 eventually increasing：

对任意 `x`，找两个更大的点 `y1<y2`，但让

```text
f(y2) ≤ f(y1)
```

例子给了：

```text
y1 = x + pi
y2 = y1 + 2*pi
```

因为 `cos(y1+2*pi)=cos(y1)`，所以

```text
f(y2)=f(y1)
```

并没有严格变大，因此不是 eventually increasing。

---

## 4. 证明题模板

### 4.1 当且仅当证明

题型：

```text
x is an integer iff floor(0.857x)+ceil(0.143x)=x
```

做法：

必须证明两个方向：

```text
(⇒) 如果 x 是整数，则等式成立。
(⇐) 如果等式成立，则 x 是整数。
```

常用事实：

```text
floor(a) ≤ a < floor(a)+1
ceil(b)-1 < b ≤ ceil(b)
```

以及：

```text
0.857x + 0.143x = x
```

证明思路：

- 若 x 是整数，利用 `ceil(x-y)=x-floor(y)` 这类关系。
- 反方向中，左边是两个整数相加，所以左边是整数；若它等于 x，则 x 必须是整数。

这个题最短的关键点：

```text
floor(0.857x)+ceil(0.143x) 是整数。
如果它等于 x，那么 x 是整数。
```

---

### 4.2 数学归纳法

题型：

```text
证明 10 | 17^n - 7^n
```

标准结构：

1. Base case：检查最小的自然数，一般是 `n=1`，有些课也用 `n=0`，看课程定义。
2. Inductive hypothesis：假设对某个 n 成立。
3. Inductive step：证明对 n+1 成立。

常用拆法：

```text
17^(n+1)-7^(n+1)
=17*17^n - 7*7^n
=17*17^n - 17*7^n + 17*7^n - 7*7^n
=17(17^n-7^n)+7^n(17-7)
```

如果第一项是 10 的倍数，第二项也有 `(17-7)=10`，所以整体是 10 的倍数。

---

### 4.3 抽屉原理

例子：

48 个学生选 5 个专业，证明至少出现某种人数下限。

反证法套路：

假设所有结论都不成立：

```text
Accounting ≤ 3
Bioinformatics ≤ 7
Commerce ≤ 19
Digital Media ≤ 3
Engineering ≤ 15
```

最多人数：

```text
3+7+19+3+15 = 47
```

但总共有 48 人，矛盾。所以至少一个结论成立。

---

## 5. 计数题

### 5.1 函数计数

从集合 A 到集合 B 的函数数：

```text
|B|^|A|
```

单射函数：

```text
P(|B|, |A|)
```

前提是 `|B|≥|A|`，否则为 0。

满射函数：

- 如果 `|A|<|B|`，没有满射。
- 如果大小一样，满射数量等于双射数量，也就是 `|A|!`。
- 一般情况用容斥：

```text
number of onto functions A→B
= |B|^|A| - C(|B|,1)(|B|-1)^|A| + C(|B|,2)(|B|-2)^|A| - ...
```

---

### 5.2 分配 lollies

不同糖果给不同孩子：

```text
n 个不同糖果，每个糖果可给任意一个孩子：children^lollies
```

不同糖果且每个孩子最多一个：

```text
P(children, lollies)
```

相同糖果分给孩子，可为 0：

```text
C(lollies + children - 1, children - 1)
```

这是星与棒。

每个孩子从 41 种糖果中选 16 种不同糖果，供应无限：

```text
C(41,16)^children
```

---

### 5.3 星与棒加上限

题型：

```text
a1+...+a8=50, ai≥0
```

无上限：

```text
C(50+8-1, 8-1)=C(57,7)
```

有上限 `ai≤7` 或 `ai≤8`：

用容斥。

通用形式：

```text
number = Σ (-1)^j * C(8,j) * C(50 - j*(limit+1) + 7, 7)
```

只保留组合数上面非负的项。

---

### 5.4 Wordle 类限制计数

这类题核心是“位置限制 + 字母限制 + 重复限制”。

常用步骤：

1. 列出必须出现的字母。
2. 列出禁止出现的字母。
3. 列出每个字母不能在哪些位置。
4. 判断是否允许重复。
5. 如果没有重复，用排列/容斥。
6. 如果有重复，先选重复字母和位置，再填其他位置。

例子：

“D 在单词中但不在第 1 位，J/L/Q/G 不出现。”

可用字母：

- 26 个字母去掉 J,L,Q,G，剩 22 个。
- D 必须出现，但不能在第 1 位。

常见计数法：

```text
所有不用 J,L,Q,G 的 5 位字符串 - 没有 D 的字符串 - D 只出现在第 1 位的情况修正
```

或按 D 出现的位置分类。

---

### 5.5 子串出现次数

题型：

21-letter words contain subword "HALFTIME" twice/exactly once/not at all.

`HALFTIME` 长度是 8。

做法：

- 把整块 `HALFTIME` 当作一个固定长度块。
- 在 21 个位置中选择起点。
- 检查两个块会不会重叠。

注意：

`HALFTIME` 没有明显自我重叠结构，所以两个出现不能重叠，除非题目中的子串前后缀允许重叠。一般要检查前缀=后缀情况。

---

## 6. 图论题

### 6.1 度数列

核心定理：

```text
所有顶点度数之和 = 2*边数
```

所以度数和必须是偶数。

简单图额外限制：

```text
每个度数 ≤ n-1
```

连通图：

```text
边数至少 n-1
```

题型：

```text
H 是否可能有度数列 4,4,3,3,2
```

先看度数和：

```text
4+4+3+3+2=16
```

边数是 8。

如果 H 是由两个连通图加一条桥连接而成，那么去掉桥以后会分成两个连通分量。桥的两端顶点度数各比原来增加 1。

这类题通常要结合：

- 度数和；
- 顶点数；
- 连通分量；
- 是否有桥；
- 原来两个岛都必须连通。

---

### 6.2 加桥后的图 H

如果 `G1` 和 `G2` 各有度数列：

```text
4,3,2,2,1
```

加一条桥后：

- 一边选一个顶点，各自度数 +1。
- 总顶点数变成 10。
- 总度数增加 2。

如果想让 H 没有 pendant vertices，也就是没有度数 1，要把两个原本度数为 1 的顶点连接起来。

所以两个 1 都变成 2。

新度数列：

```text
4,4,3,3,2,2,2,2,2,2
```

---

### 6.3 二分图

定义：

图可以把顶点分成两组，使每条边都跨组。

等价判断：

```text
没有奇环 ⇔ 二分图
```

如果两个二分图之间加一条桥：

- 一定还是二分图。
- 因为可以必要时把其中一个图的左右两边交换，使桥跨组。

---

### 6.4 Euler trail / Euler tour

欧拉路 Euler trail：

```text
走过每条边恰好一次
```

连通图中：

- 0 个奇度顶点：有 Euler tour，也有 Euler trail。
- 2 个奇度顶点：有 Euler trail，但不是 tour。
- 其他数量：没有 Euler trail。

加一条桥时：

- 桥两端顶点奇偶性会翻转。
- 所以两个图各自的奇度顶点数量会改变。

判断 “always/sometimes/never”：

要找是否总成立；如果不是总成立，再找一个成立例子和一个不成立例子。

---

### 6.5 Hamilton cycle

哈密顿圈：

```text
经过每个顶点恰好一次，最后回到起点
```

常见判断：

- 有割点或桥的图不能有 Hamilton cycle。
- 完全图 `K_n` 删除一条边后，`n≥4` 仍有 Hamilton cycle。
- `K_3` 删除一条边变成一条长度 2 的路径，没有 Hamilton cycle。

---

### 6.6 平面图 Euler 公式

连通平面图：

```text
V - E + F = 2
```

注意：

加一条边不一定破坏平面性，因为如果它连接两个原本分开的平面图，可以把两个图画在平面不同地方，再画桥连接。

如果两个平面图用一条桥连接，整体仍然平面。

---

### 6.7 Hasse 图也是图

Hasse 图来自偏序，边表示“覆盖关系”。

对于整除偏序：

```text
a connected to b in Hasse diagram
```

意思是：

```text
a|b, a≠b，并且中间没有 c 满足 a|c|b
```

题型：

- 数 Hasse 图边数；
- 找某个点的度；
- 构造 Hasse 图同构于 `K2,2` 或 `C5`；
- 证明不可能有三角形。

重要性质：

Hasse 图不会有三角形，因为如果有 `a<b<c`，那么 `a` 和 `c` 之间不是覆盖关系，中间有 `b`。

---

### 6.8 图同构

图同构就是给顶点重新命名，保持边关系不变。

解题步骤：

1. 先按度数分组。
2. 度数特殊的点先匹配。
3. 看邻居的度数模式。
4. 写出映射。
5. 检查每条边是否被映到另一图的一条边。

如果题目只有图片，必须能看到图才能精确给映射。

---

## 7. 加权图算法

### 7.1 Dijkstra 最短路

适用：

```text
所有边权非负
```

步骤：

1. 起点距离设为 0，其他为无穷。
2. 每次选当前未确定顶点中距离最小的。
3. 用它更新邻居距离。
4. 记录每个顶点是从哪条边来的。

表格题通常填：

```text
Next edge
Next vertex
Total distance to this vertex
```

注意：

Dijkstra 得到的是从起点到所有点的最短路径树，不是最小生成树。

---

### 7.2 Kruskal 最小生成树

目标：

连接所有顶点，总边权最小。

步骤：

1. 所有边按权重从小到大排序。
2. 每次选最小且不会形成环的边。
3. 直到选了 `n-1` 条边。

常见问法：

- 第一条边可以选哪条？
- 第一条边有多少选择？
- 最小生成树总权重是多少？
- 每种权重选了几条边？

注意：

Kruskal 看全局最小边；Dijkstra 看从起点出发的当前最短距离。

---

## 8. 偏序题

### 8.1 整除偏序

题型：

```text
S={n∈N: 10≤n≤3870 and n=5^i 13^j}
```

偏序是整除 `|`。

做法：

1. 列出符合范围的所有 `5^i13^j`。
2. 极小元素 minimal：集合中没有别的元素能整除它。
3. 极大元素 maximal：集合中它不能整除集合中别的更大元素。

区别：

- minimal 可以有多个。
- least element 最小元素必须整除所有元素，最多一个。
- maximal 可以有多个。
- greatest element 最大元素必须被所有元素整除，最多一个。

---

### 8.2 上界、下界、LUB、GLB

对两个元素 `x,y`：

- 上界：同时大于等于 x 和 y 的元素。
- 最小上界 LUB：所有上界里最低的那个。
- 下界：同时小于等于 x 和 y 的元素。
- 最大下界 GLB：所有下界里最高的那个。

在 Hasse 图中：

- 往上找共同祖先，是上界。
- 往下找共同祖先，是下界。

---

## 9. 关系题

### 9.1 自反、对称、反对称、传递

关系 `R` 在集合 S 上。

自反：

```text
对所有 x∈S, xRx
```

对称：

```text
xRy ⇒ yRx
```

反对称：

```text
xRy 且 yRx ⇒ x=y
```

传递：

```text
xRy 且 yRz ⇒ xRz
```

注意：

反对称不是“不对称”。反对称允许 `xRx`。

---

### 9.2 关系矩阵

Type Chart 转矩阵 `M`：

- `M_ij=1` 表示 i 对 j 有关系。
- `M^2_ij` 表示从 i 到 j 的长度为 2 的路径数量。

解释 `(2,4)` entry of `M^2` is 2：

从类型 2 到类型 4 有 2 种中间类型 `k`，使得：

```text
2 R k 且 k R 4
```

也就是类型 2 对某个类型有 bonus damage，而那个类型又对类型 4 有 bonus damage，这样的中间类型有 2 个。

---

### 9.3 关系数量

集合 A 有 10 个元素。

所有二元关系：

```text
2^(10^2)
```

因为关系是 `A×A` 的任意子集。

自反关系：

对角线 10 个必须选，其余 90 个任意：

```text
2^90
```

对称关系：

对角线 10 个任意；非对角线成对决定，共 `C(10,2)=45` 对：

```text
2^(10+45)=2^55
```

反对称关系：

对角线每个任意 2 种；每对不同元素 `(a,b),(b,a)` 有 3 种选择：

```text
2^10 * 3^45
```

---

## 10. 数论题

### 10.1 线性丢番图方程

题型：

```text
7c - 12d = 23
```

做法：

1. 先找一个特解。
2. 通解：

若

```text
Ax + By = C
```

且一个特解是 `(x0,y0)`，则

```text
x = x0 + (B/g)t
y = y0 - (A/g)t
```

其中 `g=gcd(A,B)`。

注意符号。如果原式是 `7c-12d=23`，可以看作 `A=7, B=-12`。

---

### 10.2 三变量线性方程

例子：

```text
28x - 12y + 77z = 23
```

改写成：

```text
7c - 12d = 23
```

可令：

```text
c = 4x + 11z
d = y
```

因为：

```text
7(4x+11z)-12y = 28x - 12y + 77z
```

然后把 `c,d` 的通解代入，再解 `4x+11z=c`。

---

### 10.3 同余方程

题型：

```text
39x ≡ 108 (mod 183)
```

基本定理：

```text
ax ≡ b (mod m)
```

有解当且仅当：

```text
gcd(a,m) | b
```

若 `g=gcd(a,m)`，则可除以 g：

```text
(a/g)x ≡ (b/g) (mod m/g)
```

如果化简后系数与模数互素，就乘以逆元。

在原模数 `m` 下，若有解，会有 `g` 个不同解。

---

### 10.4 Extended Euclidean Algorithm

用途：

找逆元。

如果：

```text
ax ≡ 1 (mod m)
```

需要找 `a` 的模 `m` 逆元。

方法：

用欧几里得算法写：

```text
m = qa + r
a = q'r + r'
...
```

再反代，得到：

```text
sa + tm = gcd(a,m)
```

如果 gcd 是 1，则 `s` 就是 `a` 的逆元。

---

### 10.5 齿轮与链条

题型：

链条有 L 个 link，齿轮有 T 个 teeth。问每个 link 是否会碰到每个 tooth。

核心判断：

```text
gcd(L,T)=1 时，磨损最均匀
```

因为链条与齿轮相对位置会循环遍历所有组合。

所以逐个算：

```text
gcd(chain links, gear teeth)
```

等于 1 的齿轮有 minimal wear。

---

### 10.6 RSA

给：

```text
m = pq
public key α
private key β ≡ α^(-1) mod (p-1)(q-1)
```

加密：

```text
y ≡ x^α mod m
```

解密：

```text
x ≡ y^β mod m
```

题目通常会给一些幂的提示，目的是避免你手算巨大指数。

---

## 11. 函数题

### 11.1 单射、满射、双射

单射 injective：

```text
f(x1)=f(x2) ⇒ x1=x2
```

满射 surjective：

```text
对每个目标 y，都存在 x 使 f(x)=y
```

双射 bijective：

既单射又满射。

---

### 11.2 证明不满射

例子：

```text
f:Z→Z, f(x)=2x+3
```

输出永远是奇数，所以偶数永远取不到。

因此不满射。

---

### 11.3 证明不单射

例子：

```text
g(y)=floor((y-1)/2)
```

找两个不同输入有同样输出。

例如：

```text
g(1)=0
g(2)=0
```

所以不单射。

---

### 11.4 复合函数与反函数

计算：

```text
(g∘f)(x)=g(f(x))
(f∘g)(x)=f(g(x))
```

如果 `g∘f` 最后化简成简单表达式，例如 `x+1`，那么反函数就是解：

```text
y=x+1
```

得到：

```text
x=y-1
```

---

## 12. 递推关系

### 12.1 二阶线性递推

例子：

```text
a_n - 4a_{n-2} = (-4)^n
```

齐次部分：

```text
a_n - 4a_{n-2}=0
```

设 `a_n=λ^n`：

```text
λ^n - 4λ^(n-2)=0
λ^2 - 4 = 0
λ = 2, -2
```

所以齐次解：

```text
h_n = A*2^n + B*(-2)^n
```

非齐次特解：

右边是 `(-4)^n`，试：

```text
p_n = C*(-4)^n
```

代入求 C。

最后：

```text
a_n = h_n + p_n
```

再用初值解 A、B。

---

### 12.2 指数右边的递推

例子：

```text
f(n)-6f(n-1)+8f(n-2)=4^n
```

齐次特征方程：

```text
λ^2 - 6λ + 8 = 0
(λ-2)(λ-4)=0
```

因为右边 `4^n` 的 4 已经是齐次根，所以特解不能只试 `C*4^n`，要乘 n：

```text
p_n = C*n*4^n
```

如果重复根更高，还要乘更多 n。

---

## 13. 平面图、完全图、特殊图

### 13.1 简单连通平面图

给度数列：

```text
[1,3,1,1,3,3,3,3]
```

边数：

```text
E = degree sum / 2
```

面数：

```text
V - E + F = 2
F = 2 - V + E
```

能用 Euler 公式需要：

- 图是平面图；
- 图是连通图。

简单不是 Euler 公式本身必须的。

---

### 13.2 K_n 删除一条边

图 `G=K_n` 删除一条边。

度数：

- 被删除边的两个端点度数从 `n-1` 变成 `n-2`。
- 其他 `n-2` 个顶点度数还是 `n-1`。

Euler trail 条件：

看奇度顶点个数。

Hamilton cycle：

- `n=3` 删除一条边后不是三角形，没有 Hamilton cycle。
- `n≥4` 通常有 Hamilton cycle。

Planar：

- `K_5` 非平面。
- 删除一条边的 `K_5` 仍非平面。
- `n≥6` 更不平面。
- `n=3,4` 平面。

---

## 14. 常见选择题判断策略

### 14.1 “Always true / sometimes / always false”

判断方法：

1. 先尝试证明总是真的。
2. 如果不能，找反例。
3. 如果既能找到真例子，又能找到假例子，就是 sometimes。
4. 如果所有情况都不可能，则 always false。

### 14.2 多选题

多选题最怕“看起来像对”。

建议：

- 每个选项单独判断。
- 不要因为另一个类似选项对，就顺手选它。
- 遇到包含关系、逻辑蕴含、图性质，都用定义检查。

---

## 15. 图片依赖题的处理

你发来的题里有多道题显示为 “Maple plot” 或图片占位，例如：

- 图同构映射；
- 某个图的 MST；
- 某个图的 Dijkstra；
- 判断某个图是否二分、Hamilton、Euler；
- 读加权图边权。

这些题如果没有清晰图片或边列表，不能可靠给出具体答案。

但做法固定：

1. 图同构：按度数和邻接结构匹配。
2. MST：Kruskal，从小边开始，不成环就选。
3. 最短路：Dijkstra，从起点逐步锁定最近顶点。
4. Euler：数奇度顶点。
5. Bipartite：看有没有奇环，或尝试二染色。
6. Hamilton：找经过所有点一次的圈，或用割点/桥排除。

后续如果你能上传截图，我可以逐题带你做。

---

## 16. 建议学习顺序

如果你现在几乎零基础，建议按这个顺序学：

1. 逻辑符号：`∧,∨,¬,→,↔` 和真值表。
2. 集合：`∩,∪,⊆, complement, symmetric difference`。
3. 计数基础：乘法原理、排列组合、星与棒。
4. 图论基础：度数、路径、连通、欧拉、哈密顿、二分。
5. 偏序与 Hasse 图。
6. 数论：gcd、同余、逆元、线性方程。
7. 递推关系。
8. 证明题：当且仅当、反证法、归纳法、抽屉原理。

---

## 17. 考试做题时的快速检查表

看到集合题：

```text
画区域，逐个检查包含关系。
```

看到逻辑题：

```text
用 P,Q 代替长句，化简成标准形式。
```

看到真值表：

```text
P→Q 只有 T→F 是 F。
```

看到计数题：

```text
先判断对象是否不同，是否允许重复，是否有位置限制。
```

看到函数题：

```text
单射找同输出不同输入；满射找有没有取不到的输出。
```

看到同余题：

```text
先算 gcd(a,m)，看是否整除 b。
```

看到图论题：

```text
先列顶点度数，再判断欧拉/二分/平面/哈密顿。
```

看到 MST：

```text
Kruskal：小边优先，不成环。
```

看到最短路：

```text
Dijkstra：当前最近点优先，更新邻居。
```

看到证明题：

```text
先写清楚要证明的方向，再用定义。
```

---

## 18. 后续 60 道题怎么继续整理

你后续可以继续把题贴过来。我建议我们按下面格式追加：

```text
题号/截图：
题型：
关键词：
必用知识点：
标准步骤：
容易错的点：
最终答案/证明模板：
```

这样最后能整理成一份完整考前复习手册，而不是一堆零散答案。
