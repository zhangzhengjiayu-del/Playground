# MATH1081 核心方法练题手册

这份文件不是知识点清单，而是“怎么用”的练题手册。你现在的状态很正常：看懂一遍不等于会做题。数学离不开大量小例子，把套路练成反射。

本手册重点：

1. 线性同余 `ax≡b mod m`
2. 线性丢番图方程 `ax+by=c`
3. Fermat 小定理
4. CRT 中国剩余定理
5. 递推找通解
6. Havel-Hakimi
7. 充分条件、必要条件、充要条件
8. Fibonacci 各种表达方式
9. 模运算 + 星与棒
10. 圆排列 / bracelet / necklace 类题

---

# 1. 线性同余：`ax≡b (mod m)`

## 1.1 先背一个总规则

线性同余：

```text
ax≡b (mod m)
```

有解当且仅当：

```text
gcd(a,m) | b
```

如果有解，模 `m` 下有：

```text
gcd(a,m)
```

个不同解。

---

## 1.2 标准做题流程

题目：

```text
54x≡96 (mod 183)
```

步骤：

### Step 1：算 gcd

```text
gcd(54,183)=3
```

### Step 2：看 gcd 是否整除右边

```text
3 | 96
```

所以有解。

### Step 3：全部除以 3

```text
18x≡32 (mod 61)
```

注意：模数也要除以 3。

### Step 4：求 18 在 mod 61 下的逆元

要找：

```text
18u≡1 (mod 61)
```

用 Euclidean algorithm：

```text
61 = 3*18 + 7
18 = 2*7 + 4
7 = 1*4 + 3
4 = 1*3 + 1
```

反代：

```text
1 = 4 - 3
  = 4 - (7 - 4)
  = 2*4 - 7
  = 2*(18 - 2*7) - 7
  = 2*18 - 5*7
  = 2*18 - 5*(61 - 3*18)
  = 17*18 - 5*61
```

所以：

```text
18^{-1}≡17 (mod 61)
```

### Step 5：两边乘逆元

```text
x≡32*17 (mod 61)
x≡544 (mod 61)
x≡56 (mod 61)
```

所以最小模数下：

```text
x≡56 (mod 61)
```

### Step 6：如果题目要原模数 183 的解集

原来 `g=3`，所以有 3 个解，间隔：

```text
183/3=61
```

解是：

```text
56
56+61=117
56+122=178
```

所以：

```text
x∈{56,117,178} (mod 183)
```

---

## 1.3 练习 1

解：

```text
39x≡108 (mod 183)
```

### 解

```text
gcd(39,183)=3
3|108
```

除以 3：

```text
13x≡36 (mod 61)
```

13 的逆元 mod 61：

```text
61=4*13+9
13=1*9+4
9=2*4+1
```

反代：

```text
1=9-2*4
 =9-2*(13-9)
 =3*9-2*13
 =3*(61-4*13)-2*13
 =3*61-14*13
```

所以：

```text
13^{-1}≡-14≡47 (mod 61)
```

于是：

```text
x≡36*47 (mod 61)
x≡1692 (mod 61)
x≡45 (mod 61)
```

原模数 183 下 3 个解：

```text
45,106,167
```

---

## 1.4 练习 2：无解判断

解：

```text
12x≡10 (mod 18)
```

### 解

```text
gcd(12,18)=6
```

但：

```text
6∤10
```

所以无解。

---

## 1.5 练习 3：只问有几个解

问：

```text
30x≡? (mod ?)
```

可能有多少个解？

### 解

对：

```text
30x≡b (mod m)
```

如果有解，解的个数是：

```text
gcd(30,m)
```

所以可能的解数就是 30 的正因数：

```text
{1,2,3,5,6,10,15,30}
```

---

## 1.6 线性同余常见错误

错误 1：

```text
54x≡96 mod 183
除以 3 后写 18x≡32 mod 183
```

错。模数也要除：

```text
18x≡32 mod 61
```

错误 2：

```text
有 gcd=3，所以答案只有一个
```

错。原模数下有 3 个解。

错误 3：

```text
看到 gcd>1 就说无解
```

错。要看 gcd 是否整除右边。

---

# 2. 线性丢番图方程：`ax+by=c`

## 2.1 总规则

方程：

```text
ax+by=c
```

有整数解当且仅当：

```text
gcd(a,b)|c
```

如果有一个特解 `(x0,y0)`，通解是：

```text
x=x0+(b/g)t
y=y0-(a/g)t
```

其中：

```text
g=gcd(a,b)
t∈Z
```

---

## 2.2 例题 1：`7c-12d=23`

### Step 1：看有无解

```text
gcd(7,12)=1
1|23
```

有解。

### Step 2：找一个特解

试小数：

```text
7*5 - 12*1 = 35-12=23
```

所以：

```text
c0=5, d0=1
```

### Step 3：写通解

这里：

```text
7c + (-12)d = 23
```

所以：

```text
a=7
b=-12
g=1
```

公式：

```text
c = 5 + (-12)t = 5-12t
d = 1 - 7t
```

也可以把 `t` 换成 `-t`，写成：

```text
c=5+12t
d=1+7t
```

两种都对，因为 t 遍历所有整数。

---

## 2.3 例题 2：`7c-25d=13`

找特解：

```text
7*9 - 25*2 = 63-50=13
```

所以：

```text
c0=9
d0=2
```

通解：

```text
c=9+25t
d=2+7t
```

检查：

```text
7(9+25t)-25(2+7t)
=63+175t-50-175t
=13
```

---

## 2.4 三变量丢番图怎么做

题目：

```text
28x-12y+77z=23
```

题目提示改成：

```text
7c-12d=23
```

观察：

```text
28x-12y+77z
=7(4x+11z)-12y
```

所以令：

```text
c=4x+11z
d=y
```

从上一题：

```text
c=5+12t
d=1+7t
```

得到：

```text
4x+11z=5+12t
y=1+7t
```

再解：

```text
4x+11z=5+12t
```

模 4：

```text
11z≡5+12t (mod 4)
3z≡1 (mod 4)
z≡3 (mod 4)
```

令：

```text
z=3+4s
```

代回：

```text
4x+11(3+4s)=5+12t
4x+33+44s=5+12t
4x=-28+12t-44s
x=-7+3t-11s
```

所以：

```text
x=-7+3t-11s
y=1+7t
z=3+4s
```

---

## 2.5 找通解的思维

你看到：

```text
ax+by=c
```

脑子里问：

1. gcd 是否整除 c？
2. 找一个特解。
3. 加上“零解方向”。

“零解方向”是什么意思？

如果：

```text
ax+by=c
```

那么你想改 x,y 但不改变左边。

找：

```text
aΔx+bΔy=0
```

一个选择是：

```text
Δx=b/g
Δy=-a/g
```

所以通解就是：

```text
x=x0+(b/g)t
y=y0-(a/g)t
```

---

# 3. Fermat 小定理

## 3.1 定理本体

如果 p 是 prime，且 p 不整除 a，那么：

```text
a^(p-1)≡1 (mod p)
```

也可以写成：

```text
a^p≡a (mod p)
```

第二个版本即使 p 整除 a 也成立。

---

## 3.2 例题：算大指数

求：

```text
7^100 mod 13
```

因为 13 是 prime，且 gcd(7,13)=1：

```text
7^12≡1 mod 13
```

把 100 除以 12：

```text
100=8*12+4
```

所以：

```text
7^100≡(7^12)^8*7^4≡7^4 mod 13
```

算：

```text
7^2=49≡10 mod 13
7^4≡10^2=100≡9 mod 13
```

答案：

```text
9
```

---

## 3.3 RSA 里怎么用

RSA 解密证明里会出现：

```text
m^(de)≡m mod p
```

如果：

```text
de=1+k(p-1)
```

那么：

```text
m^(de)=m^(1+k(p-1))
=m*(m^(p-1))^k
≡m*1^k
≡m mod p
```

这就是 Fermat 小定理的用法。

---

## 3.4 常见错误

错误：

```text
a^(p-1)≡1 mod p 对所有 a 都成立
```

错。如果 p 整除 a，则左边是 0，不是 1。

正确：

```text
gcd(a,p)=1 时才用 a^(p-1)≡1
```

---

# 4. CRT 中国剩余定理

## 4.1 最常见版本

如果：

```text
x≡a (mod m)
x≡b (mod n)
```

且：

```text
gcd(m,n)=1
```

那么有唯一解 modulo：

```text
mn
```

---

## 4.2 例题：两个同余合并

解：

```text
x≡4 (mod 5)
x≡2 (mod 11)
```

第一条写成：

```text
x=4+5k
```

代入第二条：

```text
4+5k≡2 (mod 11)
5k≡-2≡9 (mod 11)
```

5 的逆元 mod 11 是 9：

```text
k≡9*9=81≡4 (mod 11)
```

所以：

```text
k=4+11t
```

代回：

```text
x=4+5(4+11t)
x=24+55t
```

答案：

```text
x≡24 (mod 55)
```

---

## 4.3 非互素模数怎么办

例如：

```text
x≡2 (mod 6)
x≡5 (mod 9)
```

模数不互素：

```text
gcd(6,9)=3
```

有解条件是：

```text
2≡5 (mod 3)
```

因为：

```text
2≡2 mod 3
5≡2 mod 3
```

所以有解。

如果不一致，就无解。

---

## 4.4 CRT 在 RSA 证明里的作用

如果 p,q 是不同 primes，且：

```text
N≡0 mod p
N≡0 mod q
```

因为 p,q 互素，所以：

```text
N≡0 mod pq
```

也就是说：

```text
A≡B mod p
A≡B mod q
```

推出：

```text
A≡B mod pq
```

---

# 5. 递推找通解

## 5.1 一阶递推

题型：

```text
a_n = r*a_{n-1}+c
```

### 方法：找 fixed point

假设极限/平衡值是 L：

```text
L=rL+c
```

解出 L。

然后：

```text
a_n = L + (a_0-L)r^n
```

### 例题

```text
a_n=0.92a_{n-1}+9000
a_0=327000
```

固定点：

```text
L=0.92L+9000
0.08L=9000
L=112500
```

所以：

```text
a_n=112500+(327000-112500)(0.92)^n
```

---

## 5.2 二阶齐次递推

题型：

```text
a_n = P*a_{n-1}+Q*a_{n-2}
```

猜：

```text
a_n=r^n
```

代入：

```text
r^n=P*r^(n-1)+Q*r^(n-2)
```

除以 `r^(n-2)`：

```text
r^2=Pr+Q
```

这就是特征方程。

---

## 5.3 例题：tower recurrence

```text
a_n=9a_{n-1}+4a_{n-2}
```

特征方程：

```text
r^2=9r+4
r^2-9r-4=0
```

根：

```text
r=(9±sqrt(97))/2
```

所以：

```text
a_n=A*((9+sqrt(97))/2)^n+B*((9-sqrt(97))/2)^n
```

A,B 用初值解。

---

## 5.4 二阶非齐次递推

题型：

```text
a_n-6a_{n-1}+8a_{n-2}=4^n
```

步骤：

1. 解齐次：

   ```text
   r^2-6r+8=0
   (r-2)(r-4)=0
   ```

   所以：

   ```text
   h_n=A*2^n+B*4^n
   ```

2. 找特解。

   右边是 `4^n`，但 `4^n` 已经在齐次解里。

   所以猜：

   ```text
   p_n=C*n*4^n
   ```

3. 总解：

   ```text
   a_n=A*2^n+B*4^n+C*n*4^n
   ```

4. 代入递推求 C，再用初值求 A,B。

---

## 5.5 特解猜法表

| 右边 RHS | 如果不撞根，猜 |
| --- | --- |
| 常数 | `C` |
| 一次多项式 `an+b` | `An+B` |
| `r^n` | `C*r^n` |
| `n*r^n` | `(An+B)r^n` |
| 二次多项式 | `An^2+Bn+C` |

如果撞了齐次根，就乘 n。

如果撞的是重根，乘更高次 n。

---

## 5.6 递推常见错误

错误：

```text
右边是 4^n，就永远猜 C4^n
```

错。要先看 4 是否是特征根。

错误：

```text
闭式里有 sqrt，所以答案不是整数
```

错。递推有整数初值和整数系数，每项一定整数。无理数项会抵消。

---

# 6. Havel-Hakimi

## 6.1 用来干嘛

判断一个 degree sequence 能不能是 simple graph 的 degree sequence。

---

## 6.2 算法

对序列：

```text
d1,d2,...,dn
```

步骤：

1. 从大到小排序。
2. 取最大数 d。
3. 删除 d。
4. 接下来 d 个数各减 1。
5. 重新排序。
6. 如果全 0，成功。
7. 如果出现负数或 d 比剩余个数还大，失败。

---

## 6.3 例题：成功

判断：

```text
3,3,2,2,2
```

步骤：

```text
3,3,2,2,2
删除 3，对后 3 个减 1：
2,1,1,2
排序：
2,2,1,1
删除 2，对后 2 个减 1：
1,0,1
排序：
1,1,0
删除 1，对后 1 个减 1：
0,0
成功
```

所以 simple graph 存在。

---

## 6.4 例题：失败

判断：

```text
4,4,3,1,1
```

步骤：

```text
4,4,3,1,1
删除 4，对后 4 个减 1：
3,2,0,0
排序：
3,2,0,0
```

现在最大是 3，但剩下只有 3 个数，其中有 0。

继续：

```text
删除 3，对后 3 个减 1：
1,-1,-1
```

出现负数，失败。

所以 simple graph 不存在。

---

## 6.5 Havel-Hakimi 的图像解释

最大 degree d 的点必须连 d 个其他点。

为了最容易成功，就把它连向当前 degree 最大的 d 个点。

这就是“后 d 个减 1”的意思。

---

# 7. 充分条件、必要条件、充要条件

## 7.1 先翻译成人话

命题：

```text
P → Q
```

读作：

```text
如果 P，那么 Q
```

这时：

```text
P 是 Q 的充分条件
Q 是 P 的必要条件
```

记忆：

```text
有 P 足够推出 Q，所以 P sufficient for Q
要有 P 必须有 Q，所以 Q necessary for P
```

---

## 7.2 例子

命题：

```text
如果一个数能被 4 整除，那么它能被 2 整除。
```

令：

```text
P = 能被 4 整除
Q = 能被 2 整除
```

有：

```text
P→Q
```

所以：

```text
能被 4 整除 是 能被 2 整除 的充分条件。
能被 2 整除 是 能被 4 整除 的必要条件。
```

但不是充要条件，因为 6 能被 2 整除，但不能被 4 整除。

---

## 7.3 充要条件

`P iff Q` 表示：

```text
P→Q
Q→P
```

也就是 P 和 Q 等价。

例：

```text
n 是偶数 iff n^2 是偶数
```

证明要写两个方向：

1. 如果 n 偶数，则 n² 偶数。
2. 如果 n² 偶数，则 n 偶数。

---

## 7.4 必要不充分、充分不必要

### 充分但不必要

```text
P: x 是 4 的倍数
Q: x 是 2 的倍数
```

P 推出 Q。

但 Q 不推出 P。

所以 P 是 Q 的充分不必要条件。

### 必要但不充分

对“x 是 4 的倍数”来说：

```text
x 是 2 的倍数
```

是必要条件，但不充分。

---

## 7.5 图论里的条件例子

### Euler tour

对 connected graph：

```text
有 Euler tour iff 所有顶点 degree 偶数
```

这是充要条件。

但如果不要求 connected，就不够。

所以准确说：

```text
connected + all degrees even ⇔ Euler tour
```

### Tree

对 connected graph：

```text
tree iff E=V-1
```

如果没有 connected 条件，`E=V-1` 不是充分条件。

例如一个 triangle 加一个 isolated vertex：

```text
V=4
E=3
E=V-1
```

但不是 tree，因为不 connected。

### Bipartite

```text
bipartite iff no odd cycle
```

这是充要条件。

### Planar

```text
E≤3V-6
```

对 simple planar graph 是必要条件。

意思是：

```text
planar ⇒ E≤3V-6
```

但不是充分条件。

因为有些非平面图也满足这个边数上限。

---

# 8. Fibonacci 的各种表达方式

## 8.1 递推定义

题库里可能定义：

```text
F0=1
F1=1
F_{n+2}=F_{n+1}+F_n
```

注意有的课本是：

```text
F0=0, F1=1
```

所以先看题目定义。

---

## 8.2 1 和 2 组成 sum n 的序列

题：

```text
a_n = number of sequences of 1s and 2s with sum n
```

例：

```text
n=4
1111
112
121
211
22
```

所以：

```text
a_4=5
```

为什么是 Fibonacci？

看最后一个数：

- 最后是 1：前面 sum 是 n-1，有 `a_{n-1}` 种。
- 最后是 2：前面 sum 是 n-2，有 `a_{n-2}` 种。

所以：

```text
a_n=a_{n-1}+a_{n-2}
```

这就是 Fibonacci recurrence。

---

## 8.3 组合数表达式

如果序列中有 k 个 2。

2 贡献：

```text
2k
```

剩下要用 1 补：

```text
n-2k
```

所以 1 的个数是：

```text
n-2k
```

总长度：

```text
k+(n-2k)=n-k
```

在 `n-k` 个位置里选 k 个位置放 2：

```text
C(n-k,k)
```

k 可以从：

```text
0 到 floor(n/2)
```

所以：

```text
F_n = C(n,0)+C(n-1,1)+C(n-2,2)+...+C(n-m,m)
```

其中：

```text
m=floor(n/2)
```

---

## 8.4 例题

问：sum 30 且 exactly 7 个 2 的序列有多少？

7 个 2 贡献：

```text
14
```

剩下 1 的个数：

```text
30-14=16
```

总长度：

```text
7+16=23
```

选 7 个位置放 2：

```text
C(23,7)
```

---

# 9. 模运算 + 星与棒

## 9.1 基础星与棒

非负整数解：

```text
x1+x2+...+xr=N
```

数量：

```text
C(N+r-1,r-1)
```

---

## 9.2 带模条件的例题

题：

```text
x1+x2+...+x13=62
```

每个 `xk` 都 congruent to 0 mod 5 或 3 mod 5。

求解数。

---

### Step 1：设有 r 个变量 ≡3 mod 5

其他 `13-r` 个变量 ≡0 mod 5。

总和 mod 5：

```text
3r mod 5
```

右边：

```text
62≡2 mod 5
```

所以：

```text
3r≡2 mod 5
```

3 的逆元 mod 5 是 2：

```text
r≡4 mod 5
```

在 0 到 13 之间：

```text
r=4 或 9
```

---

### Step 2：固定 r 后做星与棒

若变量 ≡3：

```text
xi=5yi+3
```

若变量 ≡0：

```text
xi=5yi
```

总和：

```text
5(y1+...+y13)+3r=62
```

所以：

```text
y1+...+y13=(62-3r)/5
```

---

### Step 3：乘上选哪些变量是 3 mod 5

对每个 r：

```text
C(13,r)*C((62-3r)/5 + 13 - 1, 13 - 1)
```

答案：

```text
C(13,4)*C(10+12,12) + C(13,9)*C(7+12,12)
```

因为：

```text
r=4: (62-12)/5=10
r=9: (62-27)/5=7
```

---

## 9.3 digit sum 题

三位数 digit sum = 16。

设三位数是：

```text
abc
```

其中：

```text
1≤a≤9
0≤b,c≤9
a+b+c=16
```

令：

```text
a'=a-1
```

则：

```text
a'+b+c=15
```

限制：

```text
0≤a'≤8
0≤b≤9
0≤c≤9
```

无上限先算：

```text
C(15+3-1,2)=C(17,2)
```

减去超上限：

- `a'≥9`：令 `a''=a'-9`，剩余和 `6`：

  ```text
  C(6+2,2)
  ```

- `b≥10`：剩余和 `5`：

  ```text
  C(5+2,2)
  ```

- `c≥10`：同上。

两个同时超上限通常看剩余是否非负。

---

## 9.4 prime score 题

如果允许质数：

```text
7,13,17,19
```

prime score 26 表示：

```text
n=7^a13^b17^c19^d
a+b+c+d=26
```

非负解数：

```text
C(26+4-1,4-1)=C(29,3)
```

如果要求 square number：

```text
a,b,c,d 全部偶数
```

令：

```text
a=2A, b=2B, c=2C, d=2D
```

则：

```text
A+B+C+D=13
```

数量：

```text
C(13+4-1,3)=C(16,3)
```

---

# 10. 圆排列

## 10.1 线性排列 vs 圆排列

线性排列：

```text
ABC, BCA, CAB
```

都不同。

圆排列里旋转相同：

```text
ABC
BCA
CAB
```

是同一种。

---

## 10.2 n 个不同元素围成一圈

公式：

```text
(n-1)!
```

原因：

固定一个元素位置，剩下排列：

```text
(n-1)!
```

---

## 10.3 有重复字母且有唯一 O

题型：

```text
4 个 B, 3 个 A, 1 个 O 排成一圈
```

如果只按 clockwise 读，旋转相同。

因为 O 唯一，可以固定 O 在顶部。

剩下 7 个位置放：

```text
4 个 B, 3 个 A
```

数量：

```text
7!/(4!*3!) = C(7,3)
```

---

## 10.4 如果 clockwise 和 anticlockwise 也相同

现在反转也算同一种。

不能总是直接除以 2，因为有些排列反过来和自己一样。

用 Burnside 思想：

```text
答案 = (顺时针排列数 + 反射固定的排列数)/2
```

在 O 固定后，剩下 7 个位置排成一条线，反射就是左右反转。

要被反射固定，7 位必须是 palindrome。

但剩下字母：

```text
4B,3A
```

长度 7 的 palindrome 中，只有一个字母数量可以是奇数。

这里：

```text
B 数量 4 偶
A 数量 3 奇
```

中间必须是 A。

剩下左右 3 对中：

```text
2 对 B
1 对 A
```

选择哪一对是 A：

```text
C(3,1)=3
```

所以：

```text
答案 = (C(7,3)+3)/2
```

---

## 10.5 necklace / bracelet 词汇

```text
necklace: 旋转相同，反射不同
bracelet: 旋转和反射都相同
```

Mu Torere 棋盘题通常是 bracelet，因为可以从任意位置开始，也可以 clockwise/anticlockwise。

---

# 11. 练习题总表

## 线性同余练习

### 题 1

```text
24x≡18 (mod 30)
```

解：

```text
gcd(24,30)=6
6|18
4x≡3 (mod 5)
4^{-1}≡4 mod 5
x≡12≡2 mod 5
```

原模数 30 下有 6 个解，间隔 5：

```text
{2,7,12,17,22,27}
```

### 题 2

```text
14x≡9 (mod 21)
```

解：

```text
gcd(14,21)=7
7∤9
```

无解。

### 题 3

```text
35x≡20 (mod 50)
```

解：

```text
gcd(35,50)=5
5|20
7x≡4 (mod 10)
```

7 的逆元 mod 10 是 3：

```text
x≡12≡2 mod 10
```

原模数 50 下 5 个解，间隔 10：

```text
{2,12,22,32,42}
```

---

## 丢番图练习

### 题 1

```text
6x+15y=9
```

解：

```text
gcd(6,15)=3
3|9
```

除以 3：

```text
2x+5y=3
```

特解：

```text
x=-1, y=1
```

因为：

```text
2(-1)+5(1)=3
```

通解：

```text
x=-1+5t
y=1-2t
```

### 题 2

```text
10x+14y=9
```

```text
gcd(10,14)=2
2∤9
```

无整数解。

---

## CRT 练习

### 题 1

```text
x≡1 mod 3
x≡2 mod 5
```

令：

```text
x=1+3k
```

代入：

```text
1+3k≡2 mod 5
3k≡1 mod 5
```

3 的逆元 mod 5 是 2：

```text
k≡2 mod 5
```

所以：

```text
x=1+3(2+5t)=7+15t
```

答案：

```text
x≡7 mod 15
```

---

## 星与棒 + 模练习

### 题 1

```text
x1+x2+x3+x4=20
每个 xi≡1 mod 3
```

令：

```text
xi=3yi+1
```

则：

```text
3(y1+y2+y3+y4)+4=20
```

得到：

```text
y1+y2+y3+y4=16/3
```

不是整数，所以无解。

### 题 2

```text
x1+x2+x3+x4=22
每个 xi≡1 mod 3
```

令：

```text
xi=3yi+1
```

则：

```text
3Σyi+4=22
Σyi=6
```

非负解：

```text
C(6+4-1,4-1)=C(9,3)
```

---

## Havel-Hakimi 练习

### 题 1

```text
4,4,3,3,2,2
```

步骤：

```text
4,4,3,3,2,2
delete 4, reduce next 4:
3,2,2,1,2
sort:
3,2,2,2,1
delete 3, reduce next 3:
1,1,1,1
sort:
1,1,1,1
delete 1:
0,1,1
sort:
1,1,0
delete 1:
0,0
success
```

simple graph exists.

### 题 2

```text
5,5,5,1,1,1
```

步骤：

```text
5,5,5,1,1,1
delete 5, reduce next 5:
4,4,0,0,0
```

接下来最大 4，但只有 4 个数，其中会减到负数：

```text
delete 4:
3,-1,-1,-1
```

fail.

---

# 12. 最后记忆卡片

```text
线性同余 ax≡b mod m:
有解 iff gcd(a,m)|b
有解数 = gcd(a,m)
```

```text
丢番图 ax+by=c:
有整数解 iff gcd(a,b)|c
通解 x=x0+(b/g)t, y=y0-(a/g)t
```

```text
Fermat:
p prime, gcd(a,p)=1 ⇒ a^(p-1)≡1 mod p
```

```text
CRT:
x≡a mod m, x≡b mod n, gcd(m,n)=1
⇒ unique solution mod mn
```

```text
二阶递推:
猜 a_n=r^n，写特征方程
```

```text
Havel-Hakimi:
删最大 d，后 d 个减 1，排序重复
```

```text
充分必要:
P→Q 中，P 是 Q 的充分条件，Q 是 P 的必要条件
P iff Q 是充要条件
```

```text
Fibonacci:
sum n with 1s and 2s
= Σ C(n-k,k), k=0..floor(n/2)
```

```text
星与棒:
x1+...+xr=N 非负解数 C(N+r-1,r-1)
```

```text
圆排列:
n 个不同元素成圈是 (n-1)!
有唯一 O 时固定 O
反射也相同用 Burnside: (总数+固定数)/2
```

