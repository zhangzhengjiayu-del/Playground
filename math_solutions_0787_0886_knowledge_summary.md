# IMG_0787 到 IMG_0886 知识点总结

说明：这份总结不是逐题答案，而是把原讲解稿里的题目压缩成复习用的知识点。重点是：题型识别、常用公式、解题套路、容易错的地方。

---

## 1. 向量与空间几何

### 1.1 向量表达式是否 well-defined

判断一个表达式是否有定义，先给每一小块标类型：

- scalar：标量；
- vector：向量。

常用规则：

\[
u\cdot v\text{ 是标量},\qquad u\times v\text{ 是向量}.
\]

向量只能和向量相加减，标量只能和标量相加减。不能把

\[
\text{scalar}+\text{vector}
\]

混在一起。

常见无定义情况：

- 对向量开平方；
- 对向量套 \(\sin\)、\(\cos\) 等实函数；
- 点积或叉积的输入不是同维向量；
- 标量和向量相加。

解题套路：

1. 不急着算数值；
2. 先判断每一块输出是 scalar 还是 vector；
3. 检查最后的运算是否类型匹配。

### 1.2 平面、法向量与标量三重积

若平面经过 \(A,B,C\)，则

\[
n=\overrightarrow{AB}\times\overrightarrow{AC}
\]

是平面的法向量。

点 \(P\) 在平面上当且仅当

\[
\overrightarrow{AP}\cdot n=0.
\]

这个量也可以看成“带符号距离”的分子：

- 正负号判断点在平面哪一侧；
- 绝对值越小，点越靠近平面；
- 等于 \(0\)，点就在平面上。

点到平面距离：

\[
d=\frac{|\overrightarrow{AP}\cdot n|}{\|n\|}.
\]

如果已知三角形 \(ABC\) 面积：

\[
\text{Area}(ABC)=\frac12\|\overrightarrow{AB}\times\overrightarrow{AC}\|.
\]

### 1.3 连续路径穿过平面

把三维问题转化成一维函数：

\[
f(t)=\overrightarrow{AR(t)}\cdot n.
\]

若路径两端在平面两侧，则 \(f(0)\) 与 \(f(1)\) 异号。由于 \(f\) 连续，由介值定理，存在 \(c\in(0,1)\) 使

\[
f(c)=0.
\]

于是 \(R(c)\) 在平面上。

核心思想：连续路径不能从平面一侧跳到另一侧而不经过平面。

### 1.4 叉积、点积与“消去律”

实数乘法有消去律：

\[
xz=yz,\ z\ne0\Rightarrow x=y.
\]

但点积和叉积通常没有这种消去律。

点积：

\[
x\cdot z=y\cdot z
\]

只说明 \(x\) 和 \(y\) 在 \(z\) 方向上的投影相同，不说明 \(x=y\)。

叉积：

\[
x\times z=y\times z
\]

等价于

\[
(x-y)\times z=0,
\]

只说明 \(x-y\) 与 \(z\) 平行，不说明 \(x-y=0\)。

常用叉积性质：

\[
a\times b=-b\times a,\qquad a\times a=0,
\]

\[
a\times(b+c)=a\times b+a\times c.
\]

### 1.5 标量三重积与行列式

若矩阵 \(A\) 的三列是 \(u,v,w\)，则

\[
\det(A)=\det[u\ v\ w]=u\cdot(v\times w).
\]

重要含义：

- \(\det(A)=0\)：三列线性相关；
- \(|\det(A)|\)：由 \(u,v,w\) 张成的平行六面体体积；
- \(\det(A)=\pm1\)：体积为 \(1\)，不代表向量一定标准正交。

若 \(w=u-v\)，则 \(u,v,w\) 线性相关，所以 \(\det(A)=0\)。

---

## 2. 线性代数

### 2.1 线性方程组解的个数

行化简后看两件事：

1. 有没有矛盾行；
2. 未知数列有多少主元。

若出现

\[
[0\ 0\ \cdots\ 0\mid c],\qquad c\ne0,
\]

则无解。

若没有矛盾行，且每个未知数都有主元，则唯一解。

若没有矛盾行，但有自由变量，则无穷多解。

含参数题的关键：

- 行化简中除过的参数式必须单独检查；
- 例如除过 \(4m-1\)，就必须单独讨论 \(m=\frac14\)；
- 不能把最后的阶梯矩阵无条件套到特殊值上。

### 2.2 矩阵乘法元素

矩阵乘积 \(C=AB\) 的第 \(i,j\) 个元素是：

\[
C_{ij}=\text{row}_i(A)\cdot \text{col}_j(B).
\]

口诀：

\[
C_{2,3}\Rightarrow A\text{ 的第 2 行，}B\text{ 的第 3 列}.
\]

### 2.3 行操作对行列式的影响

三条必须熟：

1. 交换两行：

\[
\det\mapsto -\det.
\]

2. 某一行乘以 \(k\)：

\[
\det\mapsto k\det.
\]

3. 某一行加上另一行的倍数：

\[
\det\text{ 不变}.
\]

整体数乘矩阵时：

\[
\det(cA)=c^n\det(A)
\]

其中 \(A\) 是 \(n\times n\) 矩阵。不是 \(c\det(A)\)，而是 \(c^n\det(A)\)。

### 2.4 余子式展开

沿第 \(i\) 行展开：

\[
\det(A)=\sum_j(-1)^{i+j}a_{ij}M_{ij}.
\]

符号棋盘：

\[
\begin{pmatrix}
+&-&+&-\\
-&+&-&+\\
+&-&+&-\\
-&+&-&+
\end{pmatrix}.
\]

易错点：题目给的字母顺序可能和余子式顺序不同。要看每个小行列式删的是哪一行哪一列，不要按字母顺序猜。

### 2.5 逆矩阵与增广矩阵

若

\[
(A\mid I)\sim (I\mid A^{-1}),
\]

右半边就是 \(A^{-1}\)。

转置和逆的关系：

\[
(A^T)^{-1}=(A^{-1})^T.
\]

乘积转置：

\[
(AB)^T=B^TA^T.
\]

化简矩阵表达式时，顺序不能乱。

### 2.6 对称矩阵

对称矩阵满足：

\[
A^T=A.
\]

两个条件：

- 必须是方阵；
- 主对角线两边对应元素相等。

常用事实：

\[
K+K^T\text{ 一定对称},
\]

\[
KK^T\text{ 一定对称}.
\]

所以

\[
\alpha K+\beta K^T+\gamma KK^T
\]

要对称，通常关键是让

\[
\alpha=\beta.
\]

### 2.7 方阵可逆的常见判定

方阵 \(B\) 可逆的等价条件：

- \(\det(B)\ne0\)；
- 列向量线性无关；
- 齐次方程 \(Bx=0\) 只有零解；
- 每一列都有主元。

证明 \(I+A\) 可逆的常用方法：

若对所有非零 \(x\)，有 \(|Ax|>|x|\)，则不可能存在非零 \(x\) 使

\[
(I+A)x=0.
\]

否则 \(Ax=-x\)，从而

\[
|Ax|=|x|,
\]

矛盾。因此 \(I+A\) 的零空间只有零向量，所以可逆。

### 2.8 分块矩阵

分块对角矩阵会把未知向量拆块处理。

若

\[
B\binom{r}{s}=\binom{Ar}{As},
\]

就分别解两个 \(A\)-方程。

若有

\[
C_p\binom{r}{s}=\binom{A(r+8s)}{A(pr+s)},
\]

因为 \(A\) 可逆，真正决定 \(C_p\) 可逆性的是

\[
\begin{pmatrix}
1&8\\
p&1
\end{pmatrix}.
\]

其行列式为

\[
1-8p.
\]

所以 \(C_p\) 可逆当且仅当

\[
p\ne\frac18.
\]

---

## 3. 微积分

### 3.1 分部积分

公式：

\[
\int u\,v'=uv-\int v\,u'.
\]

常见选择：

- 反三角函数积分：令 \(u=\) 反三角函数，\(v'=1\)；
- 多项式乘 \(f'\)：令 \(u=\) 多项式，\(v'=f'\)；
- 指数权重 \(w(x)g'(x)\)：把复杂指数先命名为 \(w(x)\)，再套分部积分。

定积分版本：

\[
\int_a^bP(x)f'(x)\,dx=[P(x)f(x)]_a^b-\int_a^bP'(x)f(x)\,dx.
\]

### 3.2 变上限积分与微积分基本定理

若

\[
F(x)=\int_a^x g(t)\,dt,
\]

且 \(g\) 连续，则

\[
F'(x)=g(x).
\]

若上限是 \(u(x)\)：

\[
\frac d{dx}\int_a^{u(x)}g(t)\,dt=g(u(x))u'(x).
\]

若上下限都变：

\[
\frac d{dx}\int_{a(x)}^{b(x)}g(t)\,dt
=g(b(x))b'(x)-g(a(x))a'(x).
\]

口诀：

上限正，下限负，再乘各自导数。

### 3.3 积分函数的可导性

若

\[
I(x)=\int_{-2}^x f(t)\,dt,
\]

则 \(I'(x)\) 是否等于 \(f(x)\)，关键看 \(f\) 在该点是否连续。

- \(f\) 连续，即使有尖点，仍有 \(I'(x)=f(x)\)；
- \(f\) 只在单个点改值，积分不受影响，导数可能等于连续延拓后的值；
- \(f\) 有跳跃不连续，\(I\) 通常连续但不可导。

### 3.4 中值定理

条件：

- 闭区间上连续；
- 开区间上可导。

结论：

\[
\frac{f(b)-f(a)}{b-a}=f'(c)
\]

for some \(c\in(a,b)\)。

若

\[
f'(x)\le M,
\]

则

\[
f(b)-f(a)\le M(b-a).
\]

若

\[
m<f'(x)<M,
\]

则

\[
m(b-a)<f(b)-f(a)<M(b-a).
\]

证明零点个数常用组合：

- 介值定理证明至少一个零点；
- 严格单调证明最多一个零点。

### 3.5 反函数

函数有反函数，常用充分条件是严格单调。

用导数判断：

\[
f'(x)>0\Rightarrow f\text{ 严格递增}.
\]

反函数值：

\[
f^{-1}(a)=b\Longleftrightarrow f(b)=a.
\]

反函数导数：

\[
(f^{-1})'(a)=\frac1{f'(b)},\qquad b=f^{-1}(a).
\]

重点：求反函数导数不需要显式写出反函数。

### 3.6 极限与不定式

幂型极限：

\[
f(x)^{p(x)}=e^{p(x)\ln f(x)}.
\]

先求指数里的极限，再利用 \(e^x\) 连续。

常见等价：

\[
\ln x\sim x-1\quad(x\to1),
\]

\[
e^y-1\sim y\quad(y\to0).
\]

比值极限只看最高阶主导项，但

\[
\frac{u(x)}{v(x)}\to1
\]

不代表

\[
u(x)-v(x)\to0.
\]

当 \(v(x)\to\infty\) 时，相对误差趋于 \(0\)，绝对误差仍可能趋于常数或无穷。

### 3.7 洛必达法则的逻辑

洛必达法则不能反向使用。

若

\[
\lim\frac{g'}{k'}
\]

不存在，不能推出

\[
\lim\frac{g}{k}
\]

不存在。

这是逻辑上的“不能否定前件”：

\[
P\Rightarrow Q
\]

不代表

\[
\neg P\Rightarrow \neg Q.
\]

### 3.8 反常积分与比较判别

比较判别法：

- 若 \(0\le f\le g\)，且 \(\int g\) 收敛，则 \(\int f\) 收敛；
- 若 \(0\le g\le f\)，且 \(\int g\) 发散，则 \(\int f\) 发散。

要证明发散，找一个“更小但已经发散”的函数。

\[
\int_1^\infty \frac1{x^p}\,dx
\]

在 \(p>1\) 时收敛，在 \(p\le1\) 时发散。

极限比较时，常取最高次项：

\[
(7x^7+6x^5+\cdots)^{1/4}\sim 7^{1/4}x^{7/4}.
\]

### 3.9 黎曼和

Upper Riemann Sum：每个小区间取最大值。

递增函数：

- 上和取右端点；
- 下和取左端点。

递减函数：

- 上和取左端点；
- 下和取右端点。

非单调函数：每段都要检查候选最大值：

- 左端点；
- 右端点；
- 区间内部驻点。

单点改值不影响定积分，但会影响某些上和或下和。

### 3.10 单调性、凸性与数值积分比较

递增函数：

- 左端点和低估；
- 右端点和高估。

仅知道递增，不能判断中点和一定高估或低估。

若 \(g''(x)=0\)，函数是线性的，中点法精确。

若 \(g''(x)>0\)，函数凸，中点和低估积分：

\[
E<K.
\]

---

## 4. 复数

### 4.1 极形式

复数极形式：

\[
z=re^{i\theta}=r(\cos\theta+i\sin\theta).
\]

其中：

- \(r=|z|\) 是模长；
- \(\theta=\arg z\) 是辐角。

乘除法最适合用极形式：

\[
z_1z_2=r_1r_2e^{i(\theta_1+\theta_2)},
\]

\[
\frac{z_1}{z_2}=\frac{r_1}{r_2}e^{i(\theta_1-\theta_2)}.
\]

共轭：

\[
\overline{re^{i\theta}}=re^{-i\theta}.
\]

乘以 \(i\)：逆时针旋转 \(\pi/2\)。

平方：

\[
(re^{i\theta})^2=r^2e^{i2\theta}.
\]

### 4.2 复数根与单位根

一个非零复数的 \(n\) 次根有 \(n\) 个，并且在复平面上均匀分布在同一个圆上。

\(n\) 次单位根满足：

\[
w^n=1,\qquad |w|=1.
\]

若 \(w^n=1\)，则

\[
(w^k)^n=1,
\]

所以 \(w^k\) 仍是 \(n\) 次单位根。

指数可以模 \(n\) 化简：

\[
w^{m+n}=w^m.
\]

若 \(w\ne1\) 且 \(w^n=1\)，则

\[
1+w+w^2+\cdots+w^{n-1}=0.
\]

### 4.3 实系数多项式与共轭根

实系数多项式的非实根必须成共轭对出现，并且重数相同。

如果 \(a+bi\) 是根，重数为 \(m\)，则 \(a-bi\) 也必须至少有重数 \(m\)。

最小次数补根题：

1. 找出所有共轭对；
2. 每一对的重数补到较大者；
3. 实根不需要配对；
4. 总次数为补完后的重数和。

### 4.4 含共轭的方程

遇到

\[
5z^2+4\bar z+8=0
\]

这类含 \(\bar z\) 的方程，不要套普通一元二次公式。

标准做法：

1. 设 \(z=x+iy\)；
2. 写 \(\bar z=x-iy\)；
3. 展开 \(z^2=x^2-y^2+2ixy\)；
4. 分别比较实部和虚部；
5. 如果题目说非实数解，就使用 \(y\ne0\)。

### 4.5 复数线性方程组

若系数矩阵 \(A\) 是实矩阵，且

\[
z=x+iy,\qquad w=u+iv,
\]

则

\[
Az=w
\]

等价于两个实方程组：

\[
Ax=u,\qquad Ay=v.
\]

若右端 \(w\) 全为实数，则虚部方程是

\[
Ay=0.
\]

如果 \(A\) 可逆，则 \(y=0\)，所以 \(z\) 全为实数。

### 4.6 复平面中的角度

两个复数向量 \(z_1,z_2\) 的夹角可以看商：

\[
\frac{z_1}{z_2}.
\]

如果这个商是纯虚数，则两向量垂直。

若

\[
\bar p=-p,
\]

则 \(p\) 是纯虚数。因为设 \(p=a+bi\)，比较可得 \(a=0\)。

---

## 5. 参数曲线、极坐标与图像

### 5.1 参数曲线

参数曲线：

\[
x=x(t),\qquad y=y(t).
\]

能否写成 \(y=h(x)\)，关键看同一个 \(x\) 是否只对应一个 \(y\)。

常用充分条件：

\[
x(t)\text{ one-to-one}.
\]

切线斜率：

\[
\frac{dy}{dx}=\frac{dy/dt}{dx/dt}.
\]

水平切线：

\[
dy/dt=0,\qquad dx/dt\ne0.
\]

垂直切线：

\[
dx/dt=0,\qquad dy/dt\ne0.
\]

若二者都为 \(0\)，不能直接判断，需要进一步分析。

双曲函数常用值域：

\[
\tanh t\in(-1,1),
\]

\[
\operatorname{sech}^2t\in(0,1].
\]

### 5.2 由 \(x(t),y(t)\) 判断 \(y(x)\) 图像

不要把 \(x(t)\)、\(y(t)\) 的图像直接当成 \(y(x)\)。

步骤：

1. 看 \(x(t)\) 的取值范围；
2. 看 \(t\) 增大时 \(x\) 如何变化；
3. 看 \(y(t)\) 如何变化；
4. 必要时近似消去参数；
5. 注意渐近线和方向。

若 \(x(t)\) 有水平渐近或竖直渐近，合成后的 \(y(x)\) 可能出现对应的竖直或水平渐近行为。

### 5.3 极坐标基础

极坐标转直角坐标：

\[
x=r\cos\theta,\qquad y=r\sin\theta.
\]

点到原点距离是

\[
|r|.
\]

若 \(r\ge0\)，最远/最近通常就是最大化/最小化 \(r\)。

但离 \(x\)-轴最远不是最大化 \(r\)，而是最大化

\[
|y|=|r\sin\theta|.
\]

在第二象限 \(y>0\)，所以最大化

\[
y=r\sin\theta.
\]

### 5.4 极坐标函数变换

\[
r=f(\theta-a)
\]

表示原图逆时针旋转 \(a\)。

\[
r=f(\theta+a)
\]

表示原图顺时针旋转 \(a\)。

\[
r=cf(\theta)
\]

表示半径放大 \(c\) 倍。

反射规则：

- 关于 \(x\)-轴反射：

\[
\theta\mapsto -\theta.
\]

- 关于 \(y\)-轴反射：

\[
\theta\mapsto \pi-\theta.
\]

- 关于原点反射：

\[
\theta\mapsto \theta+\pi.
\]

### 5.5 由 \(r\)-vs-\(\theta\) 图判断极坐标图

给的是 \(r\) 随 \(\theta\) 变化的图，不是普通 \(xy\) 图。

判断步骤：

1. 先看 \(\theta\) 的范围，确定曲线在哪些方向；
2. 再看 \(r\) 的正负；
3. \(r>0\)：点在角度 \(\theta\) 的射线上；
4. \(r<0\)：点在相反方向，也就是 \(\theta+\pi\)；
5. 再看 \(r\) 的大小变化，决定离原点远近。

---

## 6. 换元、双曲函数与定义域

### 6.1 换元积分

换元不是乱猜，而是看目标积分结构。

常见信号：

- 分母出现 \(x-1\)：试 \(u=x-1\)；
- 出现 \(1-u^2\)：常想到 \(u=\sin x\) 或 \(u=\cos x\)；
- 幂次匹配：若目标有 \(u^6du\)，原式有 \(x^{20}dx\)，可试 \(u=x^3\)，因为 \(u^6=x^{18}\)，\(du=3x^2dx\)。

### 6.2 双曲函数与指数

基本公式：

\[
\cosh t=\frac{e^t+e^{-t}}2,\qquad
\sinh t=\frac{e^t-e^{-t}}2.
\]

反过来：

\[
e^t=\cosh t+\sinh t,\qquad
e^{-t}=\cosh t-\sinh t.
\]

令 \(x=e^t\) 时，必须记住：

\[
x>0.
\]

所以双曲函数方程化成二次方程后，不仅要有实根，还要有正实根。

### 6.3 复合函数定义域

例如

\[
g(x)=\tanh^{-1}(\ln(f(x))).
\]

要逐层检查：

1. \(\ln(f(x))\) 有定义，需要 \(f(x)>0\)；
2. \(\tanh^{-1}(u)\) 的实定义域是 \((-1,1)\)，所以

\[
-1<\ln(f(x))<1.
\]

两边取指数：

\[
e^{-1}<f(x)<e.
\]

---

## 7. 最容易错的点

1. 向量表达式先看类型，不要把 scalar 和 vector 混加。
2. 含参数行化简时，除过的参数值必须单独讨论。
3. \(\det(cA)=c^n\det(A)\)，不是 \(c\det(A)\)。
4. 矩阵乘法和转置都要注意顺序。
5. 夹逼定理要求上下界极限相同；不同就不能推出中间极限。
6. \(\frac{u}{v}\to1\) 不代表 \(u-v\to0\)。
7. 洛必达法则不能反向推断。
8. Upper Riemann Sum 对非单调函数要检查内部驻点。
9. 实系数多项式的非实复根必须和共轭根成对出现。
10. 极坐标中“离 \(x\)-轴最远”要看 \(y=r\sin\theta\)，不是只看 \(r\)。

---

## 8. 快速复习清单

考前可以按下面顺序自查：

- 会不会判断向量表达式 well-defined？
- 会不会用叉积找平面法向量？
- 会不会用点到平面距离公式？
- 会不会从阶梯矩阵判断无解、唯一解、无穷多解？
- 会不会处理含参数矩阵的特殊值？
- 会不会用分部积分处理 \(P(x)f'(x)\)？
- 会不会对 \(\int_{a(x)}^{b(x)}g(t)\,dt\) 求导？
- 会不会用中值定理把导数界变成函数值界？
- 会不会用反函数导数公式？
- 会不会用比较判别法判断反常积分？
- 会不会用极形式做复数乘除、共轭和幂？
- 会不会补齐实系数多项式的共轭根？
- 会不会判断参数曲线的水平/垂直切线？
- 会不会做极坐标旋转、放大、反射？
- 会不会区分 \(r(\theta)\) 图和 polar plot？

