# IMG_0787 到 IMG_0886 知识点总结 + 例题解释

说明：这份是复习增强版。每个知识点后面都配一个典型例题，重点解释“为什么这样做”和“同类题怎么想”。

---

## 1. 向量与空间几何

### 1.1 判断向量表达式是否 well-defined

知识点：

- 点积 \(u\cdot v\)：两个向量输入，输出 scalar。
- 叉积 \(u\times v\)：两个 \(\mathbb R^3\) 向量输入，输出 vector。
- 向量只能和向量加减，标量只能和标量加减。
- \(\sqrt{\cdot}\)、\(\sin(\cdot)\)、\(\ln(\cdot)\) 默认作用在实数上，不作用在向量上。

例题：

设 \(x,y,z\in\mathbb R^3\)，\(t\in\mathbb R\)。判断下面哪些有定义：

\[
x\cdot y+x\times z,\qquad y-(x\times z),\qquad \sqrt{x\cdot x},\qquad \sin z.
\]

解释：

- \(x\cdot y\) 是 scalar，\(x\times z\) 是 vector，所以 \(x\cdot y+x\times z\) 无定义。
- \(x\times z\) 是 vector，\(y\) 也是 vector，所以 \(y-(x\times z)\) 有定义。
- \(x\cdot x=\|x\|^2\) 是非负 scalar，所以 \(\sqrt{x\cdot x}\) 有定义。
- \(z\) 是 vector，\(\sin z\) 默认无定义。

同类题方法：

先不要算，先给每一块标注 scalar/vector。只要出现 scalar 和 vector 混加、函数套向量、点积/叉积输入不对，就判无定义。

### 1.2 平面法向量、点在平面上、点到平面距离

知识点：

如果平面经过 \(A,B,C\)，则

\[
n=\overrightarrow{AB}\times\overrightarrow{AC}
\]

是法向量。

点 \(P\) 在平面上：

\[
\overrightarrow{AP}\cdot n=0.
\]

点到平面距离：

\[
d=\frac{|\overrightarrow{AP}\cdot n|}{\|n\|}.
\]

例题：

设 \(A=(0,0,0)\)，\(B=(1,0,0)\)，\(C=(0,2,0)\)，\(P=(3,4,5)\)。求 \(P\) 到平面 \(ABC\) 的距离。

解释：

\[
\overrightarrow{AB}=(1,0,0),\qquad \overrightarrow{AC}=(0,2,0).
\]

\[
n=\overrightarrow{AB}\times\overrightarrow{AC}=(0,0,2).
\]

\[
\overrightarrow{AP}=(3,4,5).
\]

所以

\[
\overrightarrow{AP}\cdot n=(3,4,5)\cdot(0,0,2)=10.
\]

\[
\|n\|=2.
\]

距离为

\[
d=\frac{|10|}{2}=5.
\]

同类题方法：

1. 用叉积找法向量；
2. 用 \(\overrightarrow{AP}\cdot n\) 判断在不在平面；
3. 加绝对值并除以 \(\|n\|\) 得距离。

### 1.3 平面两侧与介值定理

知识点：

\[
f(t)=\overrightarrow{AR(t)}\cdot n
\]

可以把路径是否穿过平面的问题变成一维函数问题。

若 \(f(0)\) 与 \(f(1)\) 异号，且 \(f\) 连续，则由介值定理存在 \(c\in(0,1)\)，使

\[
f(c)=0.
\]

例题：

一条连续路径 \(R(t)\) 满足

\[
\overrightarrow{AR(0)}\cdot n=6,\qquad \overrightarrow{AR(1)}\cdot n=-2.
\]

证明路径中有点在平面上。

解释：

定义

\[
f(t)=\overrightarrow{AR(t)}\cdot n.
\]

由于路径连续，点积是坐标的线性组合，所以 \(f\) 连续。

又因为

\[
f(0)=6>0,\qquad f(1)=-2<0,
\]

所以 \(0\) 介于 \(f(0)\) 和 \(f(1)\) 之间。由介值定理，存在 \(c\in(0,1)\)，使

\[
f(c)=0.
\]

这正是 \(R(c)\) 在平面上的条件。

同类题方法：

把几何位置转成一个带符号函数。两端异号 + 连续 = 中间必过 \(0\)。

### 1.4 点积、叉积不能随便消去

知识点：

点积等式

\[
x\cdot z=y\cdot z
\]

不一定推出 \(x=y\)。

叉积等式

\[
x\times z=y\times z
\]

也不一定推出 \(x=y\)。

例题：

找反例说明 \(x\cdot z=y\cdot z\) 不能推出 \(x=y\)。

取

\[
x=(1,0),\quad y=(-1,0),\quad z=(0,1).
\]

则

\[
x\cdot z=0,\qquad y\cdot z=0,
\]

但

\[
x\ne y.
\]

解释：

点积只看一个向量在 \(z\) 方向上的投影。两个不同向量可以在某个方向上的投影相同。

叉积同理：

\[
x\times z=y\times z
\Rightarrow (x-y)\times z=0.
\]

这只说明 \(x-y\) 与 \(z\) 平行，不说明 \(x-y=0\)。

---

## 2. 线性代数

### 2.1 行化简判断解的个数

知识点：

行化简后：

- 出现 \([0\ 0\ 0\mid c]\)，\(c\ne0\)：无解；
- 没有矛盾行，每个未知数都有主元：唯一解；
- 没有矛盾行，但有自由变量：无穷多解。

例题：

判断下列增广矩阵对应方程组的解：

\[
\begin{bmatrix}
1&0&2&|&3\\
0&1&-1&|&4\\
0&0&0&|&0
\end{bmatrix}.
\]

解释：

未知量有 \(3\) 个，但只有前两列有主元。第三个未知量是自由变量。

最后一行是

\[
0=0,
\]

没有矛盾。

所以方程组有无穷多解。

再看一个无解例子：

\[
\begin{bmatrix}
1&0&2&|&3\\
0&1&-1&|&4\\
0&0&0&|&5
\end{bmatrix}.
\]

最后一行表示

\[
0=5,
\]

矛盾，所以无解。

### 2.2 含参数方程组

知识点：

含参数题最容易漏的是：化简过程中除过的参数式为 \(0\) 的特殊情况。

例题：

行化简得到最后一行

\[
(m-2)z=m+1.
\]

但化简过程中曾除以 \(m-5\)。讨论解的情况时需要检查哪些 \(m\)？

解释：

首先看最后一行的主元：

\[
m-2=0\Rightarrow m=2.
\]

如果 \(m=2\)，最后一行变成

\[
0\cdot z=3,
\]

即 \(0=3\)，所以这一值可能导致无解。

但化简过程中除过 \(m-5\)，说明最后矩阵默认 \(m\ne5\)。所以 \(m=5\) 不能直接套最后结果，必须代回除法前矩阵或原方程单独检查。

同类题方法：

1. 从最后阶梯矩阵找主元可能为 \(0\) 的值；
2. 把化简中除过的参数式列出来；
3. 所有特殊值都单独代回检查。

### 2.3 矩阵乘法元素

知识点：

\[
C=AB,\qquad C_{ij}=\text{row}_i(A)\cdot\text{col}_j(B).
\]

例题：

\[
A=
\begin{pmatrix}
1&2&3\\
4&5&6
\end{pmatrix},
\quad
B=
\begin{pmatrix}
7&8\\
9&10\\
11&12
\end{pmatrix}.
\]

求 \(C_{2,1}\)。

解释：

\(C_{2,1}\) 取 \(A\) 的第 2 行和 \(B\) 的第 1 列：

\[
(4,5,6)\cdot(7,9,11)=4\cdot7+5\cdot9+6\cdot11.
\]

\[
=28+45+66=139.
\]

所以

\[
C_{2,1}=139.
\]

### 2.4 行列式与行操作

知识点：

- 交换两行：行列式变号；
- 某行乘以 \(k\)：行列式乘以 \(k\)；
- 某行加另一行倍数：行列式不变。

例题：

若 \(\det(A)=6\)，矩阵 \(B\) 由 \(A\) 交换两行得到，矩阵 \(C\) 由 \(B\) 把一行乘以 \(3\) 得到，求 \(\det(C)\)。

解释：

交换两行：

\[
\det(B)=-\det(A)=-6.
\]

再把一行乘以 \(3\)：

\[
\det(C)=3\det(B)=3(-6)=-18.
\]

### 2.5 \(\det(cA)\)

知识点：

若 \(A\) 是 \(n\times n\) 矩阵，

\[
\det(cA)=c^n\det(A).
\]

例题：

\(A\) 是 \(3\times3\) 矩阵，\(\det(A)=4\)。求 \(\det(2A)\)。

解释：

整体乘以 \(2\)，等于三行都乘以 \(2\)，所以

\[
\det(2A)=2^3\det(A)=8\cdot4=32.
\]

易错点：

不能写成 \(2\det(A)=8\)。那只适用于“某一行乘以 2”，不是整个矩阵乘以 2。

### 2.6 转置、逆矩阵与乘法顺序

知识点：

\[
(AB)^T=B^TA^T,
\]

\[
(A^T)^{-1}=(A^{-1})^T.
\]

例题：

化简

\[
(C^TA^T)^TC^{-1}.
\]

解释：

因为

\[
C^TA^T=(AC)^T,
\]

所以

\[
(C^TA^T)^T=AC.
\]

于是原式为

\[
ACC^{-1}=A.
\]

同类题方法：

转置乘积时，顺序会反过来；矩阵乘法不能随便交换。

### 2.7 对称矩阵

知识点：

对称矩阵必须满足：

\[
A^T=A.
\]

也就是关于主对角线对称的元素相等。

例题：

\[
A=
\begin{pmatrix}
1&a&3\\
2&5&b\\
c&7&9
\end{pmatrix}.
\]

求 \(a,b,c\) 使 \(A\) 对称。

解释：

比较对称位置：

\[
A_{12}=A_{21}\Rightarrow a=2.
\]

\[
A_{13}=A_{31}\Rightarrow 3=c.
\]

\[
A_{23}=A_{32}\Rightarrow b=7.
\]

所以

\[
a=2,\quad b=7,\quad c=3.
\]

### 2.8 可逆性与零空间

知识点：

方阵 \(B\) 可逆，当且仅当

\[
Bx=0
\]

只有零解。

例题：

已知对所有非零 \(x\)，有 \(|Ax|>|x|\)。证明 \(I+A\) 可逆。

解释：

反证。假设 \(I+A\) 不可逆，则存在非零 \(x\) 使

\[
(I+A)x=0.
\]

所以

\[
Ax=-x.
\]

两边取范数：

\[
|Ax|=|-x|=|x|.
\]

这和题设 \(|Ax|>|x|\) 矛盾。

因此不存在非零 \(x\) 使 \((I+A)x=0\)，所以 \(I+A\) 可逆。

### 2.9 分块矩阵

知识点：

分块矩阵常常可以把大问题拆成小问题。

例题：

设 \(A\) 可逆，且

\[
B=
\begin{pmatrix}
A&0\\
0&A
\end{pmatrix}.
\]

若

\[
A\binom12=\binom34,
\]

求一个 \(x\)，使

\[
Bx=\begin{pmatrix}3\\4\\-3\\-4\end{pmatrix}.
\]

解释：

把 \(x\) 拆成两块：

\[
x=\binom{r}{s}.
\]

则

\[
Bx=\binom{Ar}{As}.
\]

要让

\[
Ar=\binom34,
\]

可取

\[
r=\binom12.
\]

要让

\[
As=\binom{-3}{-4}=-\binom34,
\]

可取

\[
s=-\binom12=\binom{-1}{-2}.
\]

所以

\[
x=\begin{pmatrix}1\\2\\-1\\-2\end{pmatrix}.
\]

---

## 3. 微积分

### 3.1 分部积分

知识点：

\[
\int u\,v'=uv-\int v\,u'.
\]

例题：

计算

\[
\int x e^x\,dx.
\]

解释：

取

\[
u=x,\qquad v'=e^x.
\]

则

\[
u'=1,\qquad v=e^x.
\]

所以

\[
\int xe^x\,dx=xe^x-\int e^x\,dx=xe^x-e^x+C.
\]

同类题方法：

多项式乘指数/三角函数时，常把多项式放进 \(u\)，让它求导后变简单。

### 3.2 反三角函数积分

知识点：

遇到

\[
\int \arccos(3x)\,dx
\]

这类题，常取

\[
u=\arccos(3x),\qquad v'=1.
\]

例题：

求

\[
\int \arctan x\,dx.
\]

解释：

取

\[
u=\arctan x,\qquad v'=1.
\]

则

\[
u'=\frac1{1+x^2},\qquad v=x.
\]

所以

\[
\int\arctan x\,dx
=x\arctan x-\int\frac{x}{1+x^2}\,dx.
\]

后一个积分令 \(w=1+x^2\)，得

\[
\int\frac{x}{1+x^2}\,dx=\frac12\ln(1+x^2).
\]

因此

\[
\int\arctan x\,dx
=x\arctan x-\frac12\ln(1+x^2)+C.
\]

### 3.3 变上限积分求导

知识点：

\[
\frac d{dx}\int_a^{u(x)}g(t)\,dt=g(u(x))u'(x).
\]

例题：

\[
F(x)=\int_0^{x^3+1}\cos(t^2)\,dt.
\]

求 \(F'(x)\)。

解释：

上限是

\[
u(x)=x^3+1,\qquad u'(x)=3x^2.
\]

所以

\[
F'(x)=\cos((x^3+1)^2)\cdot3x^2.
\]

### 3.4 变上下限积分求导

知识点：

\[
\frac d{dx}\int_{a(x)}^{b(x)}g(t)\,dt
=g(b(x))b'(x)-g(a(x))a'(x).
\]

例题：

求

\[
\frac d{dx}\int_{x^2}^{\ln x}g(t)\,dt.
\]

解释：

上限 \(b(x)=\ln x\)，下限 \(a(x)=x^2\)。

\[
b'(x)=\frac1x,\qquad a'(x)=2x.
\]

所以

\[
\frac d{dx}\int_{x^2}^{\ln x}g(t)\,dt
=g(\ln x)\frac1x-g(x^2)2x.
\]

口诀：

上限正，下限负，各乘自己的导数。

### 3.5 中值定理

知识点：

若 \(f\) 在 \([a,b]\) 连续，在 \((a,b)\) 可导，则存在 \(c\in(a,b)\) 使

\[
\frac{f(b)-f(a)}{b-a}=f'(c).
\]

例题：

已知 \(f'(x)\le 10\)，\(f(2)=3\)。求 \(f(5)\) 的上界。

解释：

由中值定理，在 \([2,5]\) 上存在 \(c\)，使

\[
\frac{f(5)-f(2)}{5-2}=f'(c).
\]

因为 \(f'(c)\le10\)，所以

\[
\frac{f(5)-3}{3}\le10.
\]

\[
f(5)-3\le30.
\]

\[
f(5)\le33.
\]

同类题方法：

导数上界控制函数增长量。

### 3.6 反函数与反函数导数

知识点：

\[
(f^{-1})'(y)=\frac1{f'(f^{-1}(y))}.
\]

例题：

\[
f(x)=x^3+5x-3.
\]

求 \((f^{-1})'(3)\)。

解释：

先找 \(f^{-1}(3)\)，即解

\[
f(x)=3.
\]

试 \(x=1\)：

\[
f(1)=1+5-3=3.
\]

所以

\[
f^{-1}(3)=1.
\]

再求导：

\[
f'(x)=3x^2+5.
\]

\[
f'(1)=8.
\]

所以

\[
(f^{-1})'(3)=\frac1{8}.
\]

### 3.7 幂型极限

知识点：

\[
f(x)^{p(x)}=e^{p(x)\ln f(x)}.
\]

例题：

求

\[
\lim_{x\to1^+}x^{6/(1-x)}.
\]

解释：

写成

\[
x^{6/(1-x)}=e^{\frac6{1-x}\ln x}.
\]

先求指数：

\[
\lim_{x\to1^+}\frac{6\ln x}{1-x}.
\]

这是 \(0/0\)，用洛必达：

\[
\lim_{x\to1^+}\frac{6/x}{-1}=-6.
\]

所以原极限是

\[
e^{-6}.
\]

### 3.8 比较判别法

知识点：

要证明发散，找更小但发散的函数；要证明收敛，找更大但收敛的函数。

例题：

判断

\[
\int_5^\infty\frac{\sin x+8}{x-4}\,dx
\]

是否收敛。

解释：

因为

\[
-1\le\sin x\le1,
\]

所以

\[
\sin x+8\ge7.
\]

当 \(x\ge5\) 时，

\[
\frac{\sin x+8}{x-4}\ge\frac7{x-4}.
\]

而

\[
\int_5^\infty\frac7{x-4}\,dx
\]

像 \(\int\frac1x dx\) 一样发散。

所以原积分发散。

### 3.9 极限比较

知识点：

\[
\int_1^\infty\frac1{x^p}\,dx
\]

在 \(p>1\) 收敛，在 \(p\le1\) 发散。

例题：

\[
f(x)=\frac1{(7x^7+6x^5+9x+2)^{1/4}}.
\]

判断

\[
\int_1^\infty f(x)\,dx.
\]

解释：

最高次项是

\[
7x^7.
\]

所以

\[
(7x^7+6x^5+\cdots)^{1/4}\sim 7^{1/4}x^{7/4}.
\]

于是

\[
f(x)\sim \frac1{7^{1/4}x^{7/4}}.
\]

因为

\[
\frac74>1,
\]

所以积分收敛。

### 3.10 黎曼和

知识点：

Upper Riemann Sum 每段取最大值。

例题：

\(f\) 在 \([0,3]\) 上递增，分割为 \([0,1]\)、\([1,2]\)、\([2,3]\)。写出上和。

解释：

递增函数每段最大值在右端点。

所以

\[
U=f(1)\cdot1+f(2)\cdot1+f(3)\cdot1.
\]

若函数递减，上和则取左端点。

若函数非单调，要检查每段内部驻点。

### 3.11 单点改值不影响积分

知识点：

只改变有限个点的函数值，不改变定积分。

例题：

\[
g(x)=
\begin{cases}
-2,&x=8,\\
-7,&x\ne8.
\end{cases}
\]

求

\[
\int_7^8g(x)\,dx.
\]

解释：

除了单点 \(x=8\)，函数一直等于 \(-7\)。单点没有宽度，不贡献面积。

所以

\[
\int_7^8g(x)\,dx=-7(8-7)=-7.
\]

### 3.12 积分函数的导数与连续性

知识点：

若

\[
I(x)=\int_a^xf(t)\,dt,
\]

则 \(I'(x)=f(x)\) 需要 \(f\) 在 \(x\) 处连续。

例题：

\[
f(x)=
\begin{cases}
-2,&x<-1,\\
7,&x\ge-1.
\end{cases}
\]

\[
I(x)=\int_{-2}^xf(t)\,dt.
\]

求 \(I'(-1)\) 是否存在。

解释：

左侧 \(f\) 值是 \(-2\)，右侧 \(f\) 值是 \(7\)。所以 \(I\) 在 \(-1\) 处左导数为 \(-2\)，右导数为 \(7\)。

左右导数不同，因此

\[
I'(-1)
\]

不存在。

---

## 4. 复数

### 4.1 极形式与乘除法

知识点：

\[
z=re^{i\theta}.
\]

乘法：

\[
r_1e^{i\theta_1}\cdot r_2e^{i\theta_2}
=r_1r_2e^{i(\theta_1+\theta_2)}.
\]

除法：

\[
\frac{r_1e^{i\theta_1}}{r_2e^{i\theta_2}}
=\frac{r_1}{r_2}e^{i(\theta_1-\theta_2)}.
\]

例题：

\[
z_1=2e^{i\pi/3},\qquad z_2=3e^{i\pi/6}.
\]

求 \(z_1z_2\) 和 \(z_1/z_2\)。

解释：

\[
z_1z_2=6e^{i(\pi/3+\pi/6)}=6e^{i\pi/2}.
\]

\[
\frac{z_1}{z_2}=\frac23e^{i(\pi/3-\pi/6)}
=\frac23e^{i\pi/6}.
\]

### 4.2 共轭、乘 \(i\)、平方

知识点：

\[
\overline{re^{i\theta}}=re^{-i\theta}.
\]

\[
iz=re^{i(\theta+\pi/2)}.
\]

\[
z^2=r^2e^{i2\theta}.
\]

例题：

若

\[
z=4e^{i\pi/5},
\]

求 \(\bar z\)、\(iz\)、\(z^2\) 的极形式。

解释：

\[
\bar z=4e^{-i\pi/5}.
\]

\[
iz=4e^{i(\pi/5+\pi/2)}=4e^{i7\pi/10}.
\]

\[
z^2=16e^{i2\pi/5}.
\]

### 4.3 单位根

知识点：

若 \(w^n=1\)，则

\[
|w|=1.
\]

并且

\[
(w^k)^n=1.
\]

例题：

若 \(w^5=1\)，证明 \(w^{47}+w^{63}=w^2+w^3\)。

解释：

指数模 \(5\) 化简：

\[
47\equiv2\pmod5,\qquad 63\equiv3\pmod5.
\]

所以

\[
w^{47}=w^2,\qquad w^{63}=w^3.
\]

因此

\[
w^{47}+w^{63}=w^2+w^3.
\]

如果 \(w\ne1\)，还可以用

\[
1+w+w^2+w^3+w^4=0.
\]

### 4.4 实系数多项式与共轭根

知识点：

实系数多项式的非实根必须成共轭对出现，并且重数相同。

例题：

一个实系数多项式有根 \(2+3i\)，重数 \(4\)，还有根 \(2-3i\)，重数 \(1\)。如果要让某个实系数多项式被它整除，最少要补多少个 \(2-3i\)？

解释：

实系数要求共轭根重数相同。

\[
2+3i
\]

已经有重数 \(4\)，所以

\[
2-3i
\]

也至少要重数 \(4\)。

现在只有重数 \(1\)，所以要补

\[
4-1=3
\]

个。

### 4.5 含 \(\bar z\) 的方程

知识点：

含 \(\bar z\) 时，设

\[
z=x+iy,\qquad \bar z=x-iy.
\]

再分实部虚部。

例题：

解非实数解的共同实部：

\[
5z^2+4\bar z+8=0.
\]

解释：

令 \(z=x+iy\)。则

\[
z^2=x^2-y^2+2ixy,\qquad \bar z=x-iy.
\]

代入：

\[
5(x^2-y^2+2ixy)+4(x-iy)+8=0.
\]

虚部为

\[
10xy-4y=0.
\]

即

\[
y(10x-4)=0.
\]

非实数解满足 \(y\ne0\)，所以

\[
10x-4=0.
\]

\[
x=\frac25.
\]

所以非实数解的共同实部是

\[
\frac25.
\]

### 4.6 复数线性方程组拆实部虚部

知识点：

若 \(A\) 是实矩阵，

\[
Az=w,\qquad z=x+iy,\quad w=u+iv,
\]

则

\[
Ax=u,\qquad Ay=v.
\]

例题：

若 \(Az=w\)，\(A\) 是可逆实矩阵，且 \(w\) 全为实数，证明 \(z\) 全为实数。

解释：

设

\[
z=x+iy.
\]

因为 \(A\) 是实矩阵，

\[
Az=A(x+iy)=Ax+iAy.
\]

右端 \(w\) 全为实数，所以虚部为 \(0\)。因此

\[
Ay=0.
\]

又因为 \(A\) 可逆，所以齐次方程只有零解：

\[
y=0.
\]

所以

\[
z=x
\]

是实向量。

---

## 5. 参数曲线与极坐标

### 5.1 参数曲线是否能写成 \(y=h(x)\)

知识点：

如果 \(x(t)\) 一一对应，则每个 \(x\) 对应唯一 \(t\)，进而对应唯一 \(y\)。这时参数曲线可以写成 \(y=h(x)\)。

例题：

\[
x(t)=t^3,\qquad y(t)=t^2.
\]

能否写成 \(y=h(x)\)？

解释：

\[
x(t)=t^3
\]

在 \(\mathbb R\) 上严格递增，所以一一对应。每个 \(x\) 只对应一个 \(t\)，因此可以写成函数。

事实上

\[
t=\sqrt[3]{x},
\]

所以

\[
y=t^2=(\sqrt[3]{x})^2=x^{2/3}.
\]

### 5.2 参数曲线水平切线

知识点：

\[
\frac{dy}{dx}=\frac{dy/dt}{dx/dt}.
\]

水平切线：

\[
dy/dt=0,\qquad dx/dt\ne0.
\]

例题：

\[
x(t)=t^2+1,\qquad y(t)=t^3-3t.
\]

求水平切线对应的 \(t\)。

解释：

\[
\frac{dy}{dt}=3t^2-3=3(t^2-1).
\]

令

\[
dy/dt=0
\Rightarrow t=\pm1.
\]

再检查

\[
\frac{dx}{dt}=2t.
\]

当 \(t=1\)，\(dx/dt=2\ne0\)。

当 \(t=-1\)，\(dx/dt=-2\ne0\)。

所以水平切线在

\[
t=1,\quad t=-1.
\]

### 5.3 极坐标转直角坐标

知识点：

\[
x=r\cos\theta,\qquad y=r\sin\theta.
\]

例题：

极坐标点

\[
\left(6,\frac{5\pi}{6}\right)
\]

转成直角坐标。

解释：

\[
x=6\cos\frac{5\pi}{6}=6\left(-\frac{\sqrt3}{2}\right)=-3\sqrt3.
\]

\[
y=6\sin\frac{5\pi}{6}=6\cdot\frac12=3.
\]

所以直角坐标是

\[
(-3\sqrt3,3).
\]

### 5.4 极坐标最远、最近与离坐标轴距离

知识点：

离原点距离是 \(|r|\)。

离 \(x\)-轴距离是

\[
|y|=|r\sin\theta|.
\]

例题：

\[
r=\frac7{2+\cos\theta}
\]

在第二象限找离 \(x\)-轴最远的点。

解释：

第二象限 \(y>0\)，所以最大化

\[
y=r\sin\theta
=\frac{7\sin\theta}{2+\cos\theta}.
\]

求导：

\[
y'=\frac{7(2\cos\theta+1)}{(2+\cos\theta)^2}.
\]

令分子为 \(0\)：

\[
2\cos\theta+1=0
\Rightarrow \cos\theta=-\frac12.
\]

第二象限中

\[
\theta=\frac{2\pi}{3}.
\]

此时

\[
r=\frac7{2-1/2}=\frac{14}{3}.
\]

转坐标：

\[
x=\frac{14}{3}\left(-\frac12\right)=-\frac73,
\]

\[
y=\frac{14}{3}\cdot\frac{\sqrt3}{2}=\frac{7\sqrt3}{3}.
\]

所以点是

\[
\left(-\frac73,\frac{7\sqrt3}{3}\right).
\]

易错点：

离 \(x\)-轴最远不是最大化 \(r\)，而是最大化 \(y=r\sin\theta\)。

### 5.5 极坐标图像变换

知识点：

\[
r=f(\theta-a)
\]

表示逆时针旋转 \(a\)。

\[
r=cf(\theta)
\]

表示半径放大 \(c\) 倍。

反射：

\[
x\text{-轴： }\theta\mapsto-\theta,
\]

\[
y\text{-轴： }\theta\mapsto\pi-\theta.
\]

例题：

若原曲线是 \(r=f(\theta)\)，新曲线

\[
r=2f\left(\theta-\frac\pi3\right)
\]

表示什么？

解释：

外面的 \(2\)：半径放大 \(2\) 倍。

里面的 \(\theta-\frac\pi3\)：图像逆时针旋转 \(\frac\pi3\)。

所以新图像是原图放大 \(2\) 倍，并逆时针旋转 \(60^\circ\)。

### 5.6 由 \(r\)-vs-\(\theta\) 图判断 polar plot

知识点：

\(r\)-vs-\(\theta\) 图不是普通 \(xy\) 图。

- 横轴 \(\theta\)：方向；
- 纵轴 \(r\)：离原点距离；
- \(r>0\)：点在 \(\theta\) 方向；
- \(r<0\)：点在 \(\theta+\pi\) 方向。

例题：

若 \(r(\theta)>0\)，且 \(\theta\in[-\pi/2,0]\)，图像大概在哪个象限？

解释：

\[
\theta=-\frac\pi2
\]

是负 \(y\)-轴方向。

\[
\theta=0
\]

是正 \(x\)-轴方向。

因为 \(r>0\)，点就在这些方向的射线上，所以曲线位于第四象限附近。

如果 \(r\) 在两端很大，中间较小，那么 polar plot 会在靠近负 \(y\)-轴和正 \(x\)-轴方向向外伸，中间靠近原点。

---

## 6. 换元、双曲函数与定义域

### 6.1 换元积分

知识点：

换元要看结构匹配。

例题：

把

\[
\int 18x^{20}\,dx
\]

变成

\[
\int6u^6\,du.
\]

应取什么 \(u\)？

解释：

想让

\[
u^6=x^{18},
\]

可以取

\[
u=x^3.
\]

则

\[
du=3x^2\,dx.
\]

于是

\[
6u^6du=6x^{18}\cdot3x^2dx=18x^{20}dx.
\]

所以

\[
u=x^3.
\]

### 6.2 双曲函数与指数代换

知识点：

\[
e^t=\cosh t+\sinh t,\qquad e^{-t}=\cosh t-\sinh t.
\]

令 \(x=e^t\) 时，要记得 \(x>0\)。

例题：

把

\[
4x^2-20x+21=0
\]

用 \(x=e^t\) 化成双曲函数方程。

解释：

代入 \(x=e^t\)：

\[
4e^{2t}-20e^t+21=0.
\]

两边除以 \(e^t\)：

\[
4e^t-20+21e^{-t}=0.
\]

用

\[
e^t=\cosh t+\sinh t,\qquad e^{-t}=\cosh t-\sinh t.
\]

得到

\[
4(\cosh t+\sinh t)-20+21(\cosh t-\sinh t)=0.
\]

合并：

\[
25\cosh t-17\sinh t-20=0.
\]

### 6.3 复合函数定义域

知识点：

复合函数定义域要从外到内逐层检查。

例题：

\[
g(x)=\tanh^{-1}(\ln(f(x))).
\]

求对 \(f(x)\) 的要求。

解释：

第一层：

\[
\ln(f(x))
\]

要求

\[
f(x)>0.
\]

第二层：

\[
\tanh^{-1}(u)
\]

要求

\[
-1<u<1.
\]

这里

\[
u=\ln(f(x)).
\]

所以

\[
-1<\ln(f(x))<1.
\]

两边取指数：

\[
e^{-1}<f(x)<e.
\]

这个条件已经自动保证 \(f(x)>0\)。

---

## 7. 快速练习清单

你复习时可以每个知识点问自己一个小问题：

1. 这个表达式最后输出是 scalar 还是 vector？
2. 这个点代入 \(\overrightarrow{AP}\cdot n\) 是正、负还是零？
3. 行化简有没有出现 \(0=c\)？
4. 含参数题我有没有检查除数为 \(0\) 的特殊值？
5. 这个矩阵表达式转置后顺序有没有反？
6. 求反函数导数前，我有没有先找 \(f^{-1}(a)\)？
7. 变上下限积分里，上限正、下限负有没有写对？
8. 反常积分要证明收敛还是发散？比较函数方向对吗？
9. 复数运算能不能先写成 \(re^{i\theta}\)？
10. 实系数多项式的共轭根重数补齐了吗？
11. 极坐标题要最大化的是 \(r\)、\(x\)、还是 \(y\)？
12. 参数曲线的水平切线是否检查了 \(dx/dt\ne0\)？

