# MATH1081 Core Summary

这份文件是把这组 core 题压成一份速查版：只留答案、关键套路、少量必要理由。  
图像题因缺少原图，只标注“需原图”。

---

## 1. 集合与关系

设
- `X = A∩B`
- `Y = A⊖B`
- `Z = A∪B`

正确选项：

- `X⊆Y∪Z`
- `Yc∩Z⊆X`
- `Xc∩Y⊆Z`

其余都假。

---

## 2. Archimedean Archipelago

### (a)
`H` 不可能有度序列 `4,4,3,3,2`。  
理由：这 5 个点总度数是 `16`，所以 `H` 有 `8` 条边。  
但 `H` 含一条桥边。删掉这条桥后，5 个顶点分成两个连通块；若块大小为 `k` 和 `5-k`，则总边数最多为

`C(k,2)+C(5-k,2)+1`

其最大值是 `7`。  
所以有桥边的 5 顶点图最多只有 `7` 条边，不可能有 `8` 条边。

### (b)
一个没有 pendant vertices 的度序列可取：

`4,4,3,3,2,2,2,2,2,2`

### (c)
- “若 `G1` 和 `G2` 都二分图，则 `H` 二分图”  
  **Always true**
- “若 `G1` 和 `G2` 都有 Euler trail，则 `H` 有 Euler trail”  
  **Sometimes true and sometimes false**

### (d)
不正确。  
加桥边会让边数加 `1`，但在平面嵌入里也会相应多出一个 face，所以 Euler 公式不会被破坏。

---

## 3. Wordle

### (a)
`21*(22^4 - 21^4)`

### (b)
`8400`

### (c)
`11`

### (d)
`5250`

---

## 4. `S_n` 族

`S_n = {k∈Z | 5n+3 ≤ k ≤ 3n^2+7}`

### (a)
`|A| = 9`

### (b)
`|B| = 1387`

### (c)
`|C| = 9`

### (d)
`|D| = 483`

### (e)
`|E| = 0`

### (f)
`|F| = 605^523`

### (g)
`|G| = perm(605,523)`

### (h)
`|H| = 0`

---

## 5. 逻辑等价分组

等价类是：

- `{1}`
- `{2,5}`
- `{3,7,8}`
- `{4,6}`

---

## 6. `floor/ceil` 证明

要证：

`x∈Z ⇔ floor(0.857*x)+ceil(0.143*x)=x`

思路：

- 若 `x` 是整数，写 `0.143*x = x - 0.857*x`，用 `ceil(x-a)=x-floor(a)`。
- 反过来，左边一定是整数；若它等于 `x`，则 `x` 也必须是整数。

---

## 7. 偏序集 `P=(S,|)`

这里 `i,j∈N` 取正整数，所以

`S = {65,325,845,1625}`

### (a) minimal elements
`{65}`

### (b) maximal elements
`{845,1625}`

### (c)
最大 `n0` 使 `(T,|)` 有 least element：

`64`

### (d)
最大 `n0` 使 `(T,|)` 至多有两个 minimal elements：

`844`

---

## 8. 同余方程

### (a) `39x≡108 (mod 183)`

- 最小模数：`k = 61`
- 解集：`x∈{45,106,167} (mod183)`

### (b) `95x≡? (mod500)` 且已知一个解 `354`

完整解集：

`x∈{354,454,54,154,254} (mod500)`

### (c)
`28x≡? (mod?)` 的解的个数可能是：

`{1,2,4,7,14,28}`

---

## 9. 线性丢番图

### (a)
`4a+11b=5`

可取：

`a=4, b=-1`

### (b)
`7c-12d=23`

通解：

`c=5+12*t, d=1+7*t`

### (c)
由 `28x-12y+77z=23` 改写成

`7c-12d=23`

可取：

`c=4*x+11*z, d=y`

### (d)
通解可写成：

`x=4+11*s+3*t, y=1+7*t, z=-1-4*s`

---

## 10. 自行车齿轮

链长 `63`，齿数分别 `36,19,6`。  
最小磨损的是：

`B`

---

## 11. 图像题

以下两题需要原图，纯文本里无法可靠恢复：

- 图同构 `G` 和 `H`
- 加权图 `G` 的 MST 和 `a→z` 最短路

---

## 12. 铺砖递推

### (a)
- `a1 = 19`
- `a2 = 377`
- `a3 = 7467`

### (b)
- `C1 = 19`
- `C2 = 16`

### (c)
- `a5 = 2929667`

---

## 13. 度序列 `a,b,3,2,2,2,1,1`

### (a) multigraph 不存在

可取：

`a,b = 3,3`

### (b) graph 存在

可取：

`a,b = 4,3`

### (c) multigraph 存在但 graph 不存在

可取：

`a,b = 8,3`

---

## 14. Hasse 图

`S={1,2,4,6,10,15,16,18,20}`

### (a)
边数：`9`

### (b)
顶点 `4` 的度：`3`

### (c)
可取

`A={2,3,30,42}`

### (d)
可取

`B={1,2,3,15,30}`

### (e)
没有 3-cycle。  
原因：Hasse 图里的边是 cover relation；若有三角形，就会出现传递性导致的一条边不是 cover。

---

## 15. Truth tables

### (a)
可选：

- `p∨q`
- `q→∼p`

### (b)
满足 `Y1` 不等价于 `Y2` 的填法数：

`7168`

### (c)
满足 `Z1⇒Z2` 的填法数：

`2048`

---

## 16. Proof outline: multivalued / doubly-Euclidean

安全可选的思路：

- 直接证明
- 逆否命题
- 反证法

通常不选的有：

- 假设 `M` 然后证明 `not D`
- 假设 `not M` 然后证明 `not D`
- 假设 `M and D` 然后说矛盾
- 假设 `M or D` 然后说矛盾

这题按常规考试语境，选项里不该当作可行证明的通常是：

`2,4,6,7,8`

---

## 17. Pakamen

### (a)

1. reflexive: `{d,f}`
2. symmetric: `{a,e}`
3. transitive: `{a,b,c}`

### (b)

`(2,4)` 项为 `2` 的意思是：

从 `b` 到 `d` 有 **2 条长度为 2 的 bonus-damage 路径**。

### (c)

可能的 Type Charts 数量：

`15^5`

---

## 18. 数学归纳法

证明 `10 | 17^n - 7^n`

### (a)

- base case: `n = 1`
- `17^1 - 7^1 = 10`
- multiple of `10`
- assume `10 | 17^n - 7^n`
- then `17^(n+1) - 7^(n+1) = 17*(17^n - 7^n) + 7^n*(17-7)`
- `(a,b,c) = (17*7^n, 17, 7^n)`
- `17^n - 7^n = 10*m`
- `m' = 17*m + 7^n`

### (b)

Pranav 的方法是 **直接证明**，不是 induction proof，所以不适合替代题目要求的归纳证明。

---

## 19. 幂集 / 分配

`S = P({2,5}×{2,3,4}) × P({3,5}×{1,3,5})`

### (a)
`|S| = 4096`

### (b)
一个大小为 2 的子集可以写成例如：

`{({},{}),({(2,2)},{} )}`

---

## 20. 分配 lollies

### (a)

答案对应：

- (i) `13`
- (ii) `1`
- (iii) `2`
- (iv) `16`
- (v) `15`
- (vi) `11`

### (b)

为什么是

`(nk)!/(k!)^n`

因为先把 `nk` 个不同 lollies 排成一列，再按每个 child `k` 个切块；每个块内部顺序不重要，所以除掉每块的 `k!`，共除 `n` 次。

### (c)

`13` 个孩子，`169` 个 lollies，其中 `7` 个相同，且每个孩子要拿 `13` 个不同的 lollies：

`C(13,7) * 162! / ((12!)^7 * (13!)^6)`

---

## 21. Jury duty

### (a)

`(s→∼b)∧(d∨h)∧(h→b)∧s`

### (b)

推理顺序：

1. Modus Ponens
2. Modus Tollens
3. Elimination

### (c)

先由 `s` 和 `s→∼b` 得 `∼b`，再由 `h→b` 和 `∼b` 得 `∼h`，最后由 `d∨h` 和 `∼h` 得 `d`。  
所以结论“the criminal is dead”成立。
