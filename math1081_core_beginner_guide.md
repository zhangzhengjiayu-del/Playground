# MATH1081 Core Beginner Guide

这份文件是给“几乎从零开始”的版本。  
目标不是背答案，而是让你知道每类题在问什么、怎么想、为什么这样做。

如果你现在觉得自己什么都不会，也没关系。数学题很多时候不是靠天赋，是靠“认题型”。认出来以后，步骤就固定了。

---

## 0. 先认识最常见的符号

### 集合符号

- `x∈A`：`x` 在集合 `A` 里
- `A⊆B`：`A` 是 `B` 的子集
- `A∩B`：交集，两个都要
- `A∪B`：并集，至少在一个里
- `A^c`：补集，不在这个集合里
- `A⊖B`：对称差，只在一个里，不同时在两个里

### 逻辑符号

- `P∧Q`：P 且 Q
- `P∨Q`：P 或 Q
- `P→Q`：如果 P，那么 Q
- `¬P`：不是 P

### 图论符号

- degree：一个点连了几条边
- pendant vertex：度数是 1 的点
- bipartite：能分成两边，边只连不同边
- Euler trail：经过每条边一次的路
- planar：能画在平面上不交叉

### 数论符号

- `a|b`：a 整除 b
- `a≡b (mod m)`：a 和 b 除以 m 后余数一样

---

## 1. 集合题：怎么判断“包含关系”

你那题里定义了：

- `X = A∩B`
- `Y = A⊖B`
- `Z = A∪B`

### 1.1 先把每个集合翻成人话

把一个元素 `x` 分成四种情况：

1. 不在 A，也不在 B
2. 只在 A
3. 只在 B
4. 同时在 A 和 B

然后看：

- `X=A∩B`：只包括第 4 种
- `Y=A⊖B`：只包括第 2、3 种
- `Z=A∪B`：包括第 2、3、4 种

### 1.2 怎么做选择题

看一个命题，比如 `X⊆Y∪Z`。  
意思是：凡是在 X 里的东西，是不是一定也在 Y 或 Z 里？

因为 `X` 里面的东西一定在 `A∩B`，所以它当然也在 `A∪B`，也就是在 `Z` 里。  
所以它真。

### 1.3 这题最后哪些真

真的是：

- `X⊆Y∪Z`
- `Yc∩Z⊆X`
- `Xc∩Y⊆Z`

最稳的方法不是死背，而是先把四种元素位置想清楚。

---

## 2. 图论题：你先看“度数”

### 2.1 度数是什么意思

一个点的 degree，就是它连出去几条边。

所有点的度数加起来，一定等于边数的两倍。  
因为一条边会贡献给两个端点。

这叫握手定理：

`sum of degrees = 2|E|`

所以总度数一定是偶数。

### 2.2 桥边是什么

题目说有两个岛 `G1` 和 `G2`，再加一条桥边把它们连起来。  
这条边只有一个作用：把两个图接成一个连通图。

这条边的两个端点，各自度数都会加 1。

### 2.3 (a) 为什么不能有 `4,4,3,3,2`

先加起来：

`4+4+3+3+2 = 16`

所以如果真有这个度序列，那图里边数是 `16/2 = 8`。

但这张图是“两个岛加一座桥”拼出来的。  
如果你把桥边拿掉，图会分成两个连通部分。每一部分都必须还是一张连通图。

题目给的这个条件会逼出一个矛盾：  
在 5 个点上，想同时满足这些度数和“桥”的结构，不可能。

你现在先记住做法就行：

1. 先看度数和是不是偶数
2. 再看桥边会不会迫使某个岛上边数太少或结构不可能

### 2.4 (b) 怎么找一个没有 pendant vertices 的度序列

pendant vertex 就是度数 1 的点。  
题目要“没有 pendant vertices”，就是不要出现 1。

那就要把两个岛拼完后，让每个点度数至少 2。

一种可以用的序列是：

`4,4,3,3,2,2,2,2,2,2`

意思是 10 个点，每个点度数都至少 2。

### 2.5 bipartite 那题怎么想

二分图的本质是：

- 能把点分成两组
- 每条边都只从一组连到另一组

如果 `G1` 和 `G2` 都是 bipartite，但你把它们用一条桥连起来，桥边能不能连得“刚好不破坏分组”，要看你怎么接。

这里其实是 **always true**。  
原因是两个岛原本各自能二分染色；如果桥边两端颜色刚好一样，就把其中一个岛整体翻色，桥边就会连到不同颜色。因为两个图原本彼此不连通，所以你可以独立翻其中一个岛，不会破坏岛内的 bipartite 结构。

### 2.6 Euler trail 那题怎么想

Euler trail 的判定非常重要：

- 连通图里，奇度点个数是 `0` 或 `2`，就有 Euler trail

桥边一加，两个端点的奇偶性会翻转。  
所以原来两个岛各自有 Euler trail，不代表拼完以后还有。

所以也是 **sometimes true and sometimes false**。
原因是桥边会把两个端点的度数奇偶性翻转。  
有些连接方式会刚好让新图还有 0 个或 2 个奇度点；有些则会变成 4 个奇度点，所以结果不固定。

### 2.7 planar 那题怎么想

“加一条边”不等于“破坏平面性”。

只要那条边能绕到外面画过去，不交叉，就还是 planar。  
所以“两个 planar 图接起来后一定坏掉”是错的。

Euler 公式并没有被无缘无故打破，因为多一条边通常也会带来相应的 face 变化。

---

## 3. Wordle 计数题：先固定字母，再管位置

Wordle 题最重要的不是字典，而是“计数逻辑”。

### 3.1 第一题的意思

你猜 `DJLQG`，结果告诉你：

- `D` 在秘密词里，但不在第 1 位
- `J, L, Q, G` 都不在秘密词里

这说明：

- 这 5 个字母里只有 `D` 可能出现
- 其他 21 个字母都可以用
- 但 `D` 不能放第 1 位

### 3.2 怎么数

先想第 1 位：

- 不能是 `D`
- 也不能是 `J, L, Q, G`

所以第 1 位有 `21` 种。

后面四位，每位都不能用 `J, L, Q, G`，所以每位有 `22` 种。

于是粗计数是：

`21*22^4`

如果题目还说“某个字母恰好重复三次”之类，就要再加重复限制。

### 3.3 如果有“一个字母出现三次”

这类题通常这样想：

1. 先选那个重复字母
2. 再选它在哪 3 个位置出现
3. 剩下的位置再填别的字母

所以这题的核心不是算术，是先分情况。

### 3.4 你这份题里 Wordle 的大原则

- 先看哪些字母被排除
- 再看哪些位置被限制
- 最后看有没有重复字母

只要题里说“exactly one letter appears exactly three times”，你就知道要先把 3 个位置选出来。

---

## 4. `S_n` 这种整数区间题

题目定义：

`S_n={k∈Z | 5n+3≤k≤3n^2+7}`

这其实就是一个整数区间。

### 4.1 一个整数区间有多少个数

如果从 `L` 到 `R`，而且两端都算进去，那么元素个数是：

`R-L+1`

所以这里：

`|S_n| = (3n^2+7) - (5n+3) + 1`

### 4.2 为什么题目后面会问 A、B、C、D、E

这些其实在考“集合的集合”：

- `A={S_n | 14≤n≤22}`：一堆集合
- `B={x | x∈X for some X∈A}`：所有这些集合里出现过的元素
- `C={x | x∈X for some X⊆A}`：从任意子集里取出的元素
- `D={x | x∈X for all X∈A}`：所有集合共同都有的元素
- `E={x | x∈X for all X⊆A}`：对所有子集都成立

### 4.3 这些题的思路

先比较这些区间谁最小，谁最大。

因为 `n` 越大，下界 `5n+3` 变大，上界 `3n^2+7` 也变大。  
所以每个 `S_n` 都是一个越来越往右、越来越长的区间。

这样：

- `B` 是所有集合的并集
- `D` 是所有集合的交集
- `C` 和 `E` 这类“对子集也成立”的题，本质上常常变成“你能不能从任意子集中挑到元素”

### 4.4 函数计数

如果 `F = {functions from S14 to S15}`，那就是：

- 定义域有 `|S14|` 个元素
- 值域有 `|S15|` 个元素
- 每个定义域元素都能独立选一个值域元素

所以函数总数就是：

`|S15| ^ |S14|`

如果要单射、满射，就不是这么简单了。

---

## 5. 逻辑等价：为什么要做真值表

题目让你把 8 句话分组，意思是看哪些句子“意思完全一样”。

### 5.1 真值表是什么

真值表就是把所有可能情况都列出来：

- `X` 真/假
- `Y` 真/假

然后看每句话在所有情况里是真是假。

如果两句话每一行都一样，它们就逻辑等价。

### 5.2 这类题的思路

不要靠感觉，要靠真值表。

比如：

- `X is skew-differentiable and not quantised`
- `if X is quantised then X is skew-differentiable`

它们看起来差很多，但真值表一做就知道谁和谁等价。

### 5.3 这题的分组

等价类是：

- `{1}`
- `{2,5}`
- `{3,7,8}`
- `{4,6}`

这不是靠“语感”，是靠把 `true/false` 全部列出来。

---

## 6. `floor` 和 `ceil` 证明怎么做

题目要证：

`x` 是整数  
当且仅当  
`floor(0.857x)+ceil(0.143x)=x`

### 6.1 先搞懂 floor 和 ceil

- `floor(t)`：不超过 `t` 的最大整数
- `ceil(t)`：不小于 `t` 的最小整数

例如：

- `floor(3.7)=3`
- `ceil(3.7)=4`

### 6.2 如果 x 是整数，为什么左边会等于 x

因为 `0.857` 和 `0.143` 加起来正好接近 `1`。  
如果它们对应的是 `6/7` 和 `1/7`，那就更明显：

`floor(6x/7)+ceil(x/7)=x`

这类题的本质是“一个向下取整，一个向上取整，刚好拼回整数”。

### 6.3 反过来为什么能推出 x 是整数

左边一定是整数，因为 floor 和 ceil 的结果都是整数。  
如果它等于 `x`，那 `x` 也必须是整数。

所以这类题其实是在证明：

- 整数能被拼出来
- 只有整数才能被这样拼出来

---

## 7. 偏序和整除：这部分很重要

### 7.1 什么是偏序

偏序就是一种“部分比较”关系。  
整除 `|` 就是最常见的偏序。

例如：

- `2|6`，所以 2 比 6 “小”
- `3|6`，所以 3 也比 6 “小”
- 但 2 和 3 不一定能比

### 7.2 最小元素和极小元素

这两个词很容易混：

- minimal element：没有别的元素比它更小
- least element：它比所有元素都小

least 比 minimal 更强。

### 7.3 题目里 `S={n∈N:10≤n≤3870 and n=5^i13^j}`

这里的数都是 `5^i13^j`。

整除关系下：

`5^a13^b | 5^c13^d`

当且仅当：

- `a≤c`
- `b≤d`

所以你可以把它看成指数平面上的点。

### 7.4 (a) minimal elements 怎么找

最小的就是指数最小的那个。  
因为它不能再被别的更小的元素整除。

### 7.5 (b) maximal elements 怎么找

最大的就是不能再往上整除别人的那些。

### 7.6 `T={n∈N:n0<n and n=5^i13^j}`

这个题在问：你把小数删掉后，最小元素会不会只剩一个，还是还会剩两个。

因为 `5^i13^j` 的前几个数是：

- `1`
- `5`
- `13`
- `25`
- `65`
- `125`
- `169`

你只要看 `n0` 删到哪里，就知道剩下哪些最小元。

---

## 8. Hasse 图：它其实就是偏序图

### 8.1 Hasse 图是什么

Hasse 图是偏序关系的简化画法：

- 不画自反边
- 不画传递边
- 只画“直接覆盖”的边

比如如果 `1|2|4`，Hasse 图里只画 `1-2` 和 `2-4`，不画 `1-4`。

### 8.2 为什么没有三角形

如果有三角形 `a-b-c-a`，那说明有传递关系被重复画出来。  
但 Hasse 图只画 cover relation，所以三角形不可能出现。

### 8.3 你那道题怎么做

给定集合：

`S={1,2,4,6,10,15,16,18,20}`

你就检查谁整除谁，而且中间有没有“挡着”的元素。

例如：

- `1` 会连到 `2`
- `2` 会连到 `4,6,10`
- `4` 会连到 `20`，但如果中间有别的数挡着，就不连

### 8.4 图里边数怎么数

边数就是 Hasse diagram 上画了几条 cover edge。

### 8.5 图里某个点的度怎么数

看这个点和多少个点直接相连。

### 8.6 构造 `K2,2` 和 `C5`

这类题就是挑一组整数，让它们之间的整除关系刚好形成你要的图形。

例如 `K2,2` 需要两边各 2 个点，边只跨边界连。

---

## 9. Truth table 题

### 9.1 第一个小题怎么认公式

给部分 truth table，问哪一个 compound proposition 可能是 `X`。

这时最直接的方法是：

1. 把每个候选式写出来
2. 在四种 `p,q` 取值下逐个算
3. 看是否和表格一致

### 9.2 常见逻辑公式怎么记

- `p∨q`：只要有一个真就真
- `q→¬p`：只有 `q` 真且 `p` 真时是假
- `q→p`：只有 `q` 真但 `p` 假时是假
- `¬p∧q`：只有 `p` 假且 `q` 真时真

### 9.3 部分 truth table 填空题怎么做

如果问“有多少种填法使 `Y1` 不等价于 `Y2`”，你就：

1. 数总共有多少空格
2. 先算所有填法总数
3. 再减去那些让它们完全相同的填法

如果问 `Z1⇒Z2`，就只要避免 `Z1=T, Z2=F`。

---

## 10. 证明题：三种最常见方式

### 10.1 直接证明

比如要证：

`如果 X 是 multivalued，那么 X 是 doubly-Euclidean`

你就直接从“X 是 multivalued”开始，推出“X 是 doubly-Euclidean”。

### 10.2 逆否命题

原命题是：

`M→D`

逆否命题是：

`¬D→¬M`

这两个命题逻辑等价。

### 10.3 反证法

先假设：

`M∧¬D`

如果推出矛盾，就证明原命题成立。

### 10.4 哪些 outline 不行

不行的通常是：

- 你明明要证 `M→D`，却去证 `M→¬D`
- 或者去证 `¬M→¬D`
- 或者假设 `M∧D` 然后导出矛盾
- 或者假设 `M∨D` 然后导出矛盾
- 或者从 `M` 直接导出 `¬M`

---

## 11. Pakamen 题：矩阵里的数字是什么意思

### 11.1 关系矩阵是什么

如果 `M` 是类型之间的 bonus damage 矩阵，那么：

- 第 `i` 行第 `j` 列是 1：表示 `i` 能打 `j`
- 是 0：表示不能打

### 11.2 `M^2` 的意义

`M^2` 的第 `(i,j)` 项表示：

从类型 `i` 到类型 `j` 的长度为 2 的路径条数。

所以 `(2,4)` 项是 2，意思是：

`b` 到 `d` 一共有 2 条“中转一次”的 bonus-damage 路径。

### 11.3 reflexive / symmetric / transitive

- reflexive：每个类型都打自己
- symmetric：`xRy` 就有 `yRx`
- transitive：`xRy` 且 `yRz`，就有 `xRz`

这种题通常让你从表里挑一个子集 `S`。

---

## 12. 归纳法：为什么要有 base case 和 inductive step

### 12.1 证明什么

要证：

`10 | 17^n - 7^n`

对所有自然数 `n` 成立。

### 12.2 base case

先验证最小的那个 `n`，通常是 `n=1`。

### 12.3 inductive hypothesis

假设 `n` 的时候成立，也就是：

`17^n - 7^n = 10m`

### 12.4 inductive step

要证 `n+1` 也成立。  
关键是把它拆开：

`17^(n+1)-7^(n+1)`

写成“上一项 + 一个可整除的余项”。

### 12.5 为什么 Pranav 的方法不算归纳法

因为题目要求你证明“通过归纳”。
Pranav 的方法是直接因式分解，虽然可能更快，但它不是归纳结构，所以不能替代题目要的证明形式。

---

## 13. 线性同余：`ax≡b (mod m)`

### 13.1 先看 gcd

第一步一定看：

`g = gcd(a,m)`

如果 `g` 不能整除 `b`，那就无解。

如果能整除，就先把整个方程除以 `g`。

### 13.2 例子

`39x≡108 (mod183)`

先算：

`gcd(39,183)=3`

因为 `3|108`，所以有解。  
除以 3：

`13x≡36 (mod61)`

然后找 `13` 的逆元 mod 61。

最后先得到一个解，再把它加上模数的等差步长，得到全部解。

### 13.3 为什么原模数下有多个解

因为如果 `gcd(a,m)=d>1`，那么原模数里会有 `d` 个不同解。  
这些解之间相差 `m/d`。

---

## 14. 线性丢番图：`ax+by=c`

### 14.1 先找一个特解

比如：

`7c-12d=23`

先找一组满足它的整数 `c,d`。

### 14.2 然后写通解

通解一般是：

`c = c0 + 12t`
`d = d0 + 7t`

因为 7 和 12 的倍数会互相抵消。

### 14.3 你后面那道三元方程

`28x-12y+77z=23`

先把它整理成 `7c-12d=23` 的形式，再用上面的通解套回去。

这类题的本质是“先降维，再套通解”。

---

## 15. 铺砖递推

### 15.1 题目在数什么

`a_n` 是把 `2×n` 的长方形铺满的方法数。

因为有横砖和竖砖，所以最后一步一定有几种可能。

### 15.2 递推怎么来

看最右边最后一块怎么铺：

- 放一个竖着的 `2×1`
- 或者放两个横着的 `1×2`

所以通常得到二阶递推：

`a_{n+1} = C1*a_n + C2*a_{n-1}`

### 15.3 你这题的初值

先手算：

- `a1`
- `a2`
- `a3`

然后再用递推算到 `a5`

---

## 16. 图同构

### 16.1 什么是同构

两个图同构，就是“结构一样，只是名字不一样”。

你只要找一个顶点对应关系，让边和边一一对应就行。

### 16.2 怎么找

先比较：

- 度数
- 谁和谁相邻
- 有没有唯一的大度点

通常先把度数最大的点对应起来，再沿着邻接关系推下去。

### 16.3 这题为什么我现在不能直接给你映射

因为你发来的文本里图被压成了 `Maple plot`，看不到边。  
没有原图就不能严谨配对。

你把图单独发给我，我可以直接给你顶点映射表。

---

## 17. 你最该先背的 10 条

1. `sum of degrees = 2|E|`
2. Euler trail 条件：奇度点个数是 `0` 或 `2`
3. bipartite 图不能有奇环
4. planar 图不一定 bipartite
5. `P→Q` 只有 `P=T, Q=F` 时是假
6. `A⊖B` 是“只在一个里”
7. `|floor|` 和 `|ceil|` 都给整数
8. `ax≡b (mod m)` 先看 `gcd(a,m)`
9. `ax+by=c` 先找一个特解，再加参数
10. Hasse 图只画“直接覆盖”，不画传递边

---

## 18. 最后怎么学

你现在不是要“会全部”，而是要先会识别：

- 这是集合题
- 这是图论题
- 这是计数题
- 这是逻辑题
- 这是数论题

识别出来以后，题目就不再是一大团字，而是一个固定套路。

如果你愿意，我下一步可以继续给你做一份更具体的：

**“按题号逐题讲解版”**

就是每一道题我都用最慢、最白话的方式讲一遍，讲到你能照着自己做。  
*** End Patch
