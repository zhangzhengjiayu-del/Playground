# MATH1081 Core Detailed Guide

这是一份从零开始的讲解版。  
我不会再只给答案，我会把每题在考什么、第一步怎么想、为什么这样做讲清楚。

有些题的原图在你发来的文本里被压成了 `Maple plot`，那两题我会明确说“需要原图”，不会硬猜。

---

## 0. 先把最常见的符号看懂

### 集合

- `x ∈ A`：x 在集合 A 里
- `A ⊆ B`：A 是 B 的子集
- `A ∩ B`：交集，两个都要
- `A ∪ B`：并集，至少在一个里
- `A^c`：补集，不在这个集合里
- `A ⊖ B`：对称差，只在一个里，不同时在两个里

### 逻辑

- `P ∧ Q`：P 且 Q
- `P ∨ Q`：P 或 Q
- `P → Q`：如果 P，那么 Q
- `¬P`：不是 P

### 图论

- degree：一个点连了几条边
- pendant vertex：度数是 1 的点
- bipartite：能分成两边，边只连不同边
- Euler trail：经过每条边一次的路
- planar：能画在平面上不交叉

### 数论

- `a | b`：a 整除 b
- `a ≡ b (mod m)`：a 和 b 除以 m 余数一样

---

## 1. 集合与关系

题目定义：

- `X = A ∩ B`
- `Y = A ⊖ B`
- `Z = A ∪ B`

### 1.1 先把 A/B 分成四种情况

一个元素只可能落在四个区域之一：

1. 不在 A，也不在 B
2. 只在 A
3. 只在 B
4. 同时在 A 和 B

于是：

- `X = A ∩ B` 只包括第 4 种
- `Y = A ⊖ B` 只包括第 2、3 种
- `Z = A ∪ B` 包括第 2、3、4 种

### 1.2 逐个判断

- `X ∩ Z ⊆ Y`  
  `X` 本来就在 `Z` 里，所以左边就是 `X`，但 `X` 不在 `Y` 里，假。

- `X ⊆ Y ∪ Z`  
  `X ⊆ Z`，所以真。

- `Y^c ∩ Z ⊆ X`  
  不在 `Y` 里又在 `Z` 里，只可能是第 4 种，也就是 `X`，真。

- `Y ∩ Z ⊆ X`  
  `Y` 里的是只在一个集合里的元素，不可能全落在 `X`，假。

- `Y ⊆ X`  
  `Y` 和 `X` 是不同类型，假。

- `X^c ∩ Y ⊆ Z`  
  `Y ⊆ Z`，所以真。

- `Z ⊆ X`  
  `Z` 比 `X` 大很多，假。

### 1.3 最后真的是

- `X ⊆ Y ∪ Z`
- `Y^c ∩ Z ⊆ X`
- `X^c ∩ Y ⊆ Z`

---

## 2. Archimedean Archipelago

### (a) 为什么 H 不可能有度序列 `4,4,3,3,2`

先看握手定理：

`sum of degrees = 2|E|`

所以

`4+4+3+3+2 = 16`

说明图里有

`16/2 = 8`

条边。

但 `H` 是“两个岛加一条桥”拼出来的。桥边一删，图就分成两个连通块。  
设两个块的顶点数分别是 `k` 和 `5-k`，那么每块最多只有完全图那么多边，所以总边数最多：

`C(k,2) + C(5-k,2) + 1`

这个最大只能到 `7`。  
所以 5 个顶点、又有桥边的图，不可能有 8 条边。

### (b) 怎么找没有 pendant vertices 的度序列

pendant vertex 就是度数 1 的点。  
题目要“没有 pendant vertices”，就是所有点度数都要至少 2。

把两个 `4,3,2,2,1` 的图用桥连接，桥的两个端点度数各加 1，所以两个 1 都变成 2：

`4,4,3,3,2,2,2,2,2,2`

### (c) 两个判断

#### “如果 G1 和 G2 都是 bipartite，那么 H 是 bipartite”
这是 **always true**。

原因：两个岛本来各自都能二分染色。即使桥边两端颜色一样，只要把其中一个岛整体翻色，桥边就会连到不同颜色。因为两个岛原先彼此不连通，所以这样不会破坏岛内结构。

#### “如果 G1 和 G2 都有 Euler trail，那么 H 也有 Euler trail”
这是 **sometimes true and sometimes false**。

原因：加桥会翻转桥端点的奇偶性。  
有时新图会刚好只剩 0 个或 2 个奇度顶点，所以有 Euler trail；有时会变成 4 个或更多奇度顶点，所以没有。

### (d) planar 会不会被 Euler 公式打坏

不会。  
两个 planar 图可以分别画在平面上，然后用桥边从外面绕过去连起来，不必交叉。  
所以 H 仍然可以 planar，Euler 公式没有被“新增一条边”直接破坏。

---

## 3. Wordle 计数

### 基本规则

你只要记住三件事：

1. 哪些字母绝对不能出现
2. 哪些字母必须出现
3. 哪些位置不能放某字母

然后再看有没有重复字母。

这类题最容易错的地方是：  
**黄色提示只说明“这个字母在词里，但不在这个位置”，不说明它只出现一次。**  
除非题目额外说“没有重复”，否则同一个字母可以重复出现。

---

### (a) 第一局

猜 `DJLQG` 后知道：

- `D` 在秘密词里，但不在第 1 位
- `J, L, Q, G` 都不在秘密词里

#### 第一步：剩下多少字母可用

26 个字母里去掉 4 个不能用的字母，剩 22 个可用字母。  
其中 `D` 是可用的，但要满足位置限制。

#### 第二步：先数所有符合“字母限制”的 5 位串

第 1 位不能是 `D`，也不能是 `J, L, Q, G`，所以有 21 种。

第 2 到第 5 位，每位都不能是 `J, L, Q, G`，所以每位有 22 种。

先得到：

`21 * 22^4`

#### 第三步：扣掉没有 D 的情况

上一步只保证了：

- 没有 `J,L,Q,G`
- 第 1 位不是 D

但它还没有保证 D 一定出现。

如果 5 位里根本没有 D，那么每位都只能从其余 21 个字母里选。  
这里为什么是 21？

原本可用的 22 个字母是：

- D
- 另外 21 个不是 `J,L,Q,G,D` 的字母

没有 D，就只剩那 21 个。

`21^5`

所以答案是：

`21 * (22^4 - 21^4)`

也可以理解为：

- 第 1 位先选：21 种
- 后 4 位必须至少出现一次 D
- 后 4 位每位有 22 种，总共 `22^4`
- 后 4 位完全没有 D 的情况是 `21^4`

所以：

`21*(22^4 - 21^4)`

---

### (b) 再加上“恰好有一个字母出现 3 次，且没有其他重复”

题目新增条件：

> exactly one letter appears exactly three times and no other repetitions

意思是秘密词的结构一定是：

`AAA B C`

其中：

- A 出现 3 次
- B 出现 1 次
- C 出现 1 次
- A、B、C 互不相同

还要同时满足上一问的信息：

- D 必须出现
- D 不能在第 1 位
- J,L,Q,G 不能出现

现在关键问题是：  
出现 3 次的那个字母，有可能是 D，也有可能不是 D。  
所以必须分两类。

#### 情况 1：重复 3 次的是 D

D 出现 3 次，而且 D 不能在第 1 位。  
所以这 3 个 D 只能放在第 2、3、4、5 位里面。

从后 4 位里选 3 个位置放 D：

`C(4,3) = 4`

放完 3 个 D 之后，还剩 2 个位置。  
这 2 个位置要放两个不同字母，而且不能是 D，也不能是 `J,L,Q,G`。

可用的非 D 字母有 21 个，按顺序放进去：

`P(21,2) = 21 * 20`

这里用 `P(21,2)` 而不是 `C(21,2)`，因为两个剩余位置是有顺序的。  
比如 `A,B` 和 `B,A` 是两个不同的 Wordle 字符串。

所以这一类共有：

`4 * 21 * 20 = 1680`

#### 情况 2：重复 3 次的是某个非 D 字母

设重复 3 次的字母叫 `R`。  
它不能是 D，也不能是 `J,L,Q,G`。

所以 `R` 有 21 种选择。

因为 D 必须出现，但 D 不是重复 3 次的字母，所以 D 只能是那两个单次字母之一。

另一个单次字母叫 `S`。  
`S` 不能等于 D，也不能等于 R，也不能是 `J,L,Q,G`。  
所以 `S` 有 20 种选择。

现在要排列多重集合 `{r,r,r,D,s}`，并且 D 不能在第 1 位。

总排列数：

`5!/3! = 20`

为什么除以 `3!`？  
因为 3 个 `r` 长得一样，交换它们不产生新字符串。

如果 D 在第 1 位，剩下 4 位是 `r,r,r,s`，只要把 `s` 放在 4 个位置之一就行，所以有 4 种。

所以合法排列数：

`20 - 4 = 16`

这一小步很重要：  
题目只禁止 D 在第 1 位，不禁止 R 或 S 在第 1 位。

这一类共有：

`21 * 20 * 16 = 6720`

#### 总数

两种情况不会重叠，因为“重复 3 次的是 D”和“重复 3 次的不是 D”不可能同时发生。  
所以直接相加：

`1680 + 6720 = 8400`

---

### (c) 第二天

这次反馈是：

- `S` 在词里，而且在第 1 位
- `Z` 在词里，但不在第 2 位
- `Y` 在词里，但不在第 3 位
- `K` 在词里，但不在第 4 位
- `F` 不在词里

再加上朋友告诉你：**开头字母会重复**，也就是 `S` 至少再出现一次。

#### 关键观察

你已经知道词长是 5，且必须包含：

- 第 1 位是 S
- 还要再有一个 S
- Z、Y、K 各至少出现一次

这四个字母 `S, Z, Y, K` 已经占满 4 个剩余位置，所以它们必须刚好各出现一次。

也就是说，秘密词一定长这样：

`S _ _ _ _`

后面四个空格里，必须放：

`S, Z, Y, K`

不能再放别的字母。  
原因是：

- S 要重复，所以后面必须再有一个 S
- Z、Y、K 又都必须出现
- 后面一共只有 4 个位置

所以这 4 个字母正好填满。

#### 只数位置排列

把 `S, Z, Y, K` 放进第 2 到第 5 位，一共 `4! = 24` 种。

排除不合法的位置：

- Z 不能在第 2 位
- Y 不能在第 3 位
- K 不能在第 4 位

用容斥：

`24 - 3*6 + 3*2 - 1 = 11`

这一行每一项是什么意思：

- `24`：先不管限制，4 个不同字母随便排
- `3*6`：扣掉三个坏事件
  - Z 在第 2 位：剩下 3 个字母随便排，`3! = 6`
  - Y 在第 3 位：也是 6
  - K 在第 4 位：也是 6
- `3*2`：刚才两个坏事件同时发生的情况被扣了两次，要加回来
  - Z 在第 2 位且 Y 在第 3 位：剩下 2 个字母随便排，`2! = 2`
  - Z 在第 2 位且 K 在第 4 位：2
  - Y 在第 3 位且 K 在第 4 位：2
- `-1`：三个坏事件同时发生，被加回来太多一次，所以再扣掉
  - Z 在第 2 位，Y 在第 3 位，K 在第 4 位时，剩下 S 只能放第 5 位：1 种

所以答案是 `11`。

---

### (d) 第三天

已知：

- 词里有 `V, X, K`
- 不含 `I, N, T, U, W, S, E, C`
- `V` 不在第 1 或第 2 位
- `X` 不在第 3 位
- `K` 不在第 4 位
- 没有重复字母

#### 第一步：先选另外两个字母

总共排除 8 个字母，剩 18 个可用字母。  
其中 `V, X, K` 已经必须出现，所以还要再选 2 个别的字母：

`C(15,2)`

为什么是 15？

26 个字母里：

- 8 个明确不能出现：`I,N,T,U,W,S,E,C`
- 3 个必须出现：`V,X,K`

剩下可以作为“额外两个字母”的数量是：

`26 - 8 - 3 = 15`

因为没有重复字母，所以额外两个字母只需要从这 15 个里面选出来。  
顺序还没排，所以用组合：

`C(15,2)`

#### 第二步：排位置

一共有 5 个不同字母要排。  
先不管限制，总排列数是 `5! = 120`。

限制是：

- `V` 不能在 1,2
- `X` 不能在 3
- `K` 不能在 4

用容斥：

`120 - (48 + 24 + 24) + (12 + 12 + 6) - 4 = 50`

这一行也慢慢拆。

设三个坏事件：

- A：V 在第 1 或第 2 位
- B：X 在第 3 位
- C：K 在第 4 位

先数所有排列：

`5! = 120`

#### 扣掉单个坏事件

- A：V 有 2 个坏位置可以放，第 1 或第 2 位；V 放好后，剩下 4 个字母随便排  
  所以 A 有 `2*4! = 48` 种
- B：X 固定在第 3 位，剩下 4 个字母随便排  
  所以 B 有 `4! = 24` 种
- C：K 固定在第 4 位，剩下 4 个字母随便排  
  所以 C 有 `4! = 24` 种

所以先减：

`48 + 24 + 24`

#### 加回两个坏事件同时发生

刚才如果一个排列同时违反两个条件，它被减了两次，所以要加回来一次。

- A 且 B：V 在第 1 或第 2 位，X 在第 3 位  
  V 有 2 种位置，剩下 3 个字母随便排，所以 `2*3! = 12`
- A 且 C：V 在第 1 或第 2 位，K 在第 4 位  
  同理 `2*3! = 12`
- B 且 C：X 在第 3 位，K 在第 4 位  
  剩下 3 个字母随便排，所以 `3! = 6`

所以加：

`12 + 12 + 6`

#### 再扣三个坏事件同时发生

A、B、C 同时发生：

- V 在第 1 或第 2 位：2 种
- X 在第 3 位
- K 在第 4 位
- 剩下 2 个字母填剩下 2 个位置：`2! = 2`

所以一共有：

`2*2! = 4`

容斥最后要减掉这 4 种。

所以每组选出的 2 个额外字母有 50 种排法。

#### 总数

`C(15,2) * 50 = 5250`

---

### 第 3 题完整做题过程版

这一段是专门写给你看“做题过程”的。  
前面是解释，这里是像在纸上一步步算。

---

#### (a) 做题过程

题目告诉我们：

- D 在单词里
- D 不在第 1 位
- J、L、Q、G 不在单词里

先把不能用的字母去掉。  
不能用的是：

`J,L,Q,G`

所以还能用：

`26-4=22`

个字母。  
这 22 个里面包括 D。

我们要数 5 个位置：

`_ _ _ _ _`

第 1 位不能放 D，因为 D 是黄色提示，不在第 1 位。  
第 1 位也不能放 J、L、Q、G。  
所以第 1 位可选：

`26 - 5 = 21`

种。

第 2 到第 5 位只需要避开 J、L、Q、G，所以每位有：

`26 - 4 = 22`

种。

如果暂时不管“D 必须出现”，共有：

`21*22^4`

种。

但这里面包含一种坏情况：D 根本没有出现。  
如果 D 不出现，那么每一位都只能从：

`26 - 5 = 21`

个字母里选，也就是不能用 `D,J,L,Q,G`。

坏情况有：

`21^5`

种。

所以答案是：

`21*22^4 - 21^5`

也可以写成：

`21*(22^4 - 21^4)`

---

#### (b) 做题过程

这一问在 (a) 的基础上又加了一句：

> exactly one letter appears exactly three times and no other repetitions

意思是 5 个字母的结构一定是：

`AAA B C`

其中：

- A 出现 3 次
- B 出现 1 次
- C 出现 1 次
- A、B、C 三个字母互不相同

上一问还告诉我们：

- D 必须出现
- D 不能在第 1 位
- J、L、Q、G 不能出现

现在分情况。

---

##### 情况 1：出现 3 次的字母是 D

也就是单词结构是：

`D D D B C`

因为 D 不能在第 1 位，所以 3 个 D 必须从第 2、3、4、5 位里选 3 个位置。

选 D 的位置：

`C(4,3)`

剩下 2 个位置放 B 和 C。

B 和 C 必须：

- 不是 D
- 不是 J、L、Q、G
- 互不相同

除了 D 和 J、L、Q、G 之外，还有：

`26 - 5 = 21`

个字母。

两个位置有顺序，所以填法是：

`P(21,2)=21*20`

所以情况 1：

`C(4,3)*P(21,2)`

代入：

`4*21*20=1680`

---

##### 情况 2：出现 3 次的字母不是 D

设出现 3 次的字母叫 R。

R 不能是：

- D
- J、L、Q、G

所以 R 有：

`26 - 5 = 21`

种选择。

因为 D 必须出现，而且 D 不是重复 3 次的字母，所以 D 是单次字母之一。  
另一个单次字母叫 S。

S 不能是：

- R
- D
- J、L、Q、G

所以 S 有：

`26 - 6 = 20`

种选择。

现在要排列：

`R,R,R,D,S`

先不管 D 的位置限制。  
因为 3 个 R 一样，所以排列数是：

`5!/3! = 20`

但是 D 不能在第 1 位。  
坏情况：D 固定在第 1 位。

剩下四个位置放：

`R,R,R,S`

只需要决定 S 放哪一个位置，有：

`4`

种。

所以合法排列数是：

`20-4=16`

情况 2：

`21*20*16=6720`

---

##### 合并

两种情况互不重叠，所以相加：

`1680+6720=8400`

答案：

`8400`

---

#### (c) 做题过程

题目告诉我们：

- 第 1 位是 S
- Z 在单词里，但不在第 2 位
- Y 在单词里，但不在第 3 位
- K 在单词里，但不在第 4 位
- F 不在单词里
- starting letter repeats，也就是 S 会重复

先写出位置：

`S _ _ _ _`

因为 S 要重复，所以后面四个空位里必须再放一个 S。

同时，Z、Y、K 都必须出现。

所以后四个位置必须正好放：

`S,Z,Y,K`

没有空间再放别的字母。

现在问题变成：

把 `S,Z,Y,K` 排进第 2、3、4、5 位。

先不管限制，一共有：

`4! = 24`

种。

限制是：

- Z 不能在第 2 位
- Y 不能在第 3 位
- K 不能在第 4 位

设坏事件：

- A：Z 在第 2 位
- B：Y 在第 3 位
- C：K 在第 4 位

我们要算：

`总数 - 至少一个坏事件`

用容斥。

总数：

`24`

单个坏事件：

- A：Z 固定第 2 位，剩下 3 个字母随便排：`3! = 6`
- B：Y 固定第 3 位，剩下 3 个字母随便排：`3! = 6`
- C：K 固定第 4 位，剩下 3 个字母随便排：`3! = 6`

所以先减：

`6+6+6 = 18`

两个坏事件同时发生：

- A 且 B：Z 在第 2 位，Y 在第 3 位，剩下 2 个字母随便排：`2! = 2`
- A 且 C：`2`
- B 且 C：`2`

所以加回来：

`2+2+2 = 6`

三个坏事件同时发生：

- Z 在第 2 位
- Y 在第 3 位
- K 在第 4 位
- S 只能在第 5 位

所以有：

`1`

种。

容斥结果：

`24 - 18 + 6 - 1 = 11`

答案：

`11`

---

#### (d) 做题过程

题目告诉我们：

必须出现：

`V, X, K`

不能出现：

`I,N,T,U,W,S,E,C`

位置限制：

- V 不在第 1 或第 2 位
- X 不在第 3 位
- K 不在第 4 位

并且：

- 没有重复字母

因为单词有 5 个位置，已经固定要出现 3 个不同字母：

`V,X,K`

所以还需要再选 2 个不同字母。

不能用的字母有 8 个。  
必须用的字母有 3 个。  
剩下可以任选的字母数：

`26 - 8 - 3 = 15`

从这 15 个里面选 2 个：

`C(15,2)`

现在固定了 5 个不同字母：

`V,X,K,A,B`

其中 A、B 是刚才选出来的额外两个字母。

接下来数它们能排成多少个合法 5 字母串。

先不管限制，5 个不同字母排列：

`5! = 120`

设坏事件：

- A：V 在第 1 或第 2 位
- B：X 在第 3 位
- C：K 在第 4 位

单个坏事件数量：

1. V 在第 1 或第 2 位  
   V 有 2 个坏位置，剩下 4 个字母随便排：

   `2*4! = 48`

2. X 在第 3 位  
   X 固定，剩下 4 个字母随便排：

   `4! = 24`

3. K 在第 4 位  
   K 固定，剩下 4 个字母随便排：

   `4! = 24`

所以先减：

`48+24+24`

两个坏事件同时发生：

1. V 在第 1 或第 2 位，同时 X 在第 3 位  
   V 有 2 种坏位置，剩下 3 个字母随便排：

   `2*3! = 12`

2. V 在第 1 或第 2 位，同时 K 在第 4 位：

   `2*3! = 12`

3. X 在第 3 位，同时 K 在第 4 位：

   `3! = 6`

所以加回：

`12+12+6`

三个坏事件同时发生：

- V 在第 1 或第 2 位：2 种
- X 固定第 3 位
- K 固定第 4 位
- 剩下两个字母填剩下两个位置：`2!`

所以：

`2*2! = 4`

容斥得到合法排列数：

`120 - (48+24+24) + (12+12+6) - 4`

`= 120 - 96 + 30 - 4`

`= 50`

最后乘上额外两个字母的选择数：

`C(15,2)*50`

`= 105*50`

`= 5250`

答案：

`5250`

---

## 4. `S_n` 这一题

定义：

`S_n = {k ∈ Z | 5n+3 ≤ k ≤ 3n^2+7}`

这是一个整数区间，所以元素个数直接用：

`上界 - 下界 + 1`

得到：

`|S_n| = 3n^2 - 5n + 5`

### 4.1 先算几个关键值

- `|S_14| = 523`
- `|S_15| = 605`
- `|S_22| = 1347`

而 `n = 14,15,...,22` 一共 9 个值，所以：

`A = {S_14, S_15, ..., S_22}`

共有 9 个元素。

### 4.2 B 是什么

`B = {x | x ∈ X for some X ∈ A}`

这里 `X ∈ A` 表示 X 是某个集合 `S_n`，而 `x ∈ X` 表示 x 是里面的整数。  
所以 B 就是这些区间的并集。

这些区间首尾连起来，最后就是：

`73` 到 `1459`

所以：

`|B| = 1459 - 73 + 1 = 1387`

### 4.3 C 是什么

`C = {x | x ∈ X for some X ⊆ A}`

这里的 `X ⊆ A`，意思是 X 是若干个 `S_n` 组成的子集合。  
所以 `x ∈ X` 的时候，x 不是整数，而是某个 `S_n` 这个集合本身。

换句话说，C 里装的其实就是 A 里的那些集合。

所以：

`|C| = |A| = 9`

### 4.4 D 是什么

`D = {x | x ∈ X for all X ∈ A}`

这是所有 `S_n` 的公共部分，也就是交集。

最大下界来自 `S_22`：

`5*22+3 = 113`

最小上界来自 `S_14`：

`3*14^2+7 = 595`

所以交集是：

`{113,114,...,595}`

大小：

`595 - 113 + 1 = 483`

### 4.5 E 为什么是空集

`E = {x | x ∈ X for all X ⊆ A}`

这里 `X ⊆ A` 包括空集。  
但空集里没有任何元素，所以“对所有 X ⊆ A 都成立”不可能。

因此：

`|E| = 0`

### 4.6 F、G、H

- `F`：从 `S_14` 到 `S_15` 的函数数目  
  定义域有 523 个元素，值域有 605 个元素，所以：

  `605^523`

- `G`：单射数  
  从 523 个元素单射到 605 个元素：

  `P(605,523)`

- `H`：满射数  
  定义域 523，小于值域 605，不可能满射：

  `0`

---

## 5. 逻辑等价分组

题目里有 8 句话。最稳的方法是把每句翻成逻辑式。

设：

- S = skew-differentiable
- Q = quantised

### 5.1 逐句翻译

1. `S and not Q`

2. `if Q then S`  
   也就是 `Q → S`

3. `if S then Q`  
   也就是 `S → Q`

4. `if not Q then S`  
   也就是 `¬Q → S`

5. `S or not Q`

6. `S or Q`

7. `Q if S`  
   也是 `S → Q`

8. `Q only if S`  
   也是 `S → Q`

### 5.2 化简

用 `P → Q ≡ ¬P ∨ Q`：

- 2 = `¬Q ∨ S`，等于 5
- 3 = `¬S ∨ Q`
- 4 = `Q ∨ S`，等于 6
- 7、8 都和 3 一样

### 5.3 最终分组

- `{1}`
- `{2,5}`
- `{3,7,8}`
- `{4,6}`

---

## 6. `floor` 和 `ceil` 的证明

要证明：

`x 是整数  <=>  floor(0.857x) + ceil(0.143x) = x`

### 6.1 先看左边为什么总是整数

`floor(...)` 和 `ceil(...)` 的结果一定是整数。  
所以左边是整数。

如果左边等于 x，那 x 也必须是整数。  
所以“如果等式成立，则 x 是整数”这一边很短。

### 6.2 反方向：如果 x 是整数，为什么等式成立

设 `x = n`，其中 n 是整数。  
令：

`a = 0.857n`

那么：

`0.143n = n - a`

于是

`ceil(0.143n) = ceil(n-a) = n - floor(a)`

因为 n 是整数。

所以：

`floor(a) + ceil(0.143n) = floor(a) + n - floor(a) = n`

也就是：

`floor(0.857n) + ceil(0.143n) = n`

所以 x 是整数时，等式成立。

---

## 7. 偏序集 `(S, |)`

集合：

`S = { n ∈ N : 10 ≤ n ≤ 3870 and n = 5^i 13^j }`

这里实际只会出现：

- `65 = 5*13`
- `325 = 5^2*13`
- `845 = 5*13^2`
- `1625 = 5^3*13`

因为下一项 `5^2*13^2 = 4225` 已经超过 3870 了。

所以：

`S = {65, 325, 845, 1625}`

### (a) minimal elements

在整除偏序里，minimal 的意思是：没有别的元素能整除它。

这里：

- 65 能整除 325、845、1625
- 没有别的更小元素在 S 里能整除 65

所以 minimal elements 是：

`{65}`

### (b) maximal elements

maximal 的意思是：它不能再整除集合里别的更大元素。

- `845` 不能再往上整除别的 S 元素
- `1625` 也不能再往上整除别的 S 元素

所以 maximal elements 是：

`{845,1625}`

### (c) `T = { n ∈ N : n0 < n and n = 5^i 13^j }`

这里想要 “least element”。

least element 的要求很强：它必须整除 T 里所有元素。

只要 `n0 < 65`，最小的元素 65 还在集合里，而且 65 又整除后面所有 `5^i13^j` 型的数，所以 least element 是 65。

但一旦 `n0 ≥ 65`，65 被删掉，最小的那个就不再整除所有后面的数了。

所以最大的 `n0` 是：

`64`

### (d) 最大的 `n0`，使得 minimal elements 最多两个

看 `T` 里最先出现的几个数：

`65, 325, 845, 1625, 4225, ...`

如果 `n0 = 844`，那么 65 和 325 都被删掉，剩下的最小候选是 845 和 1625，正好两个。

再把 `n0` 提高到 845，就会让 845 本身也消失，minimal elements 的个数会变化，不再是“最多两个”的最大边界。

所以答案是：

`844`

---

## 8. 同余方程

### (a) `39x ≡ 108 (mod 183)`

第一步永远先算 gcd：

`gcd(39,183) = 3`

因为 `3 | 108`，所以有解。

把方程除以 3：

`13x ≡ 36 (mod 61)`

再求 13 在 mod 61 下的逆元。

欧几里得算法：

`61 = 4*13 + 9`
`13 = 1*9 + 4`
`9 = 2*4 + 1`

回代：

`1 = 9 - 2*4`
`  = 9 - 2*(13-9)`
`  = 3*9 - 2*13`
`  = 3*(61-4*13) - 2*13`
`  = 3*61 - 14*13`

所以 `13^{-1} ≡ 47 (mod 61)`。

于是：

`x ≡ 36*47 ≡ 45 (mod 61)`

原模数 183 下有 3 个解，相差 `61`：

`{45, 106, 167} (mod 183)`

### (b) `95x ≡ ? (mod 500)`，已知一个解是 354

`gcd(95,500)=5`

所以如果它有解，就会有 5 个解，彼此相差：

`500/5 = 100`

从 354 出发：

`354, 454, 54, 154, 254`

所以完整解集是：

`{54,154,254,354,454} (mod 500)`

### (c) `28x ≡ ? (mod ?)` 的解的个数可能有哪些

线性同余 `ax ≡ b (mod m)`：

- 要么无解
- 要么有 `gcd(a,m)` 个解

这里 `a = 28`，所以可能的解个数就是 28 的所有正因数：

`{1,2,4,7,14,28}`

---

## 9. 线性丢番图方程

### (a) `4a + 11b = 5`

我们要找整数 `a,b` 使等式成立。

试一个简单组合：

`4*4 + 11*(-1) = 16 - 11 = 5`

所以可以取：

`a = 4, b = -1`

### (b) `7c - 12d = 23`

先找一个特解。  
试 `c = 5, d = 1`：

`7*5 - 12*1 = 35 - 12 = 23`

所以一组特解是 `(5,1)`。

一般解要加上“不会改变左边”的那部分：

`c = 5 + 12t`
`d = 1 + 7t`

因为：

`7(12t) - 12(7t) = 0`

### (c) 把 `28x - 12y + 77z = 23` 改写成 `7c - 12d = 23`

观察：

`28x - 12y + 77z = 7(4x + 11z) - 12y`

所以可以取：

`c = 4x + 11z`
`d = y`

### (d) 求原方程全部整数解

由 (b) 的通解：

`c = 5 + 12t`
`d = 1 + 7t`

代回 (c)：

`4x + 11z = 5 + 12t`
`y = 1 + 7t`

先解 `4x + 11z = 5 + 12t`。

一个方便的特解是：

`x = 4 + 3t`
`z = -1`

因为：

`4(4+3t) + 11(-1) = 16 + 12t - 11 = 5 + 12t`

再加上齐次解：

`4x + 11z = 0`

的一般形式是：

`x = 11s`
`z = -4s`

所以总解：

`x = 4 + 3t + 11s`
`y = 1 + 7t`
`z = -1 - 4s`

---

## 10. 自行车链条和齿轮

链有 63 个 link。  
齿轮分别有 36、19、6 个齿。

题目说：磨损最小的时候，是“每个 link 都会碰到每个 tooth”。

这等价于：

`gcd(链长, 齿数) = 1`

因为每次前进会按模齿数循环；如果 gcd 不是 1，只会落在某几个位置上。

算：

- `gcd(63,36) = 9`
- `gcd(63,19) = 1`
- `gcd(63,6) = 3`

所以只有 `B` 最小磨损。

---

## 11. 图同构和加权图

这两题都需要原图。

### (a) 图同构怎么做

方法是：

1. 先看每个顶点的 degree
2. degree 唯一的点先配对
3. 再看邻接结构
4. 一步步把映射补完

图同构要求：

`uv 是 G 的边 iff φ(u)φ(v) 是 H 的边`

### (b) 加权图的 MST 和最短路怎么做

- MST：Kruskal
  - 把边按权重从小到大排
  - 依次选，不成环就保留
  - 选够 `V-1` 条边

- 最短路：Dijkstra
  - 从起点开始
  - 每次扩展当前最近的点
  - 最后得到起点到各点的最短距离

如果你把那两张图单独发我，我可以把这两题完整算出来。

---

## 12. 2×n 铺砖递推

题目里有：

- 1×2 横砖：4 种
- 2×1 竖砖：19 种

定义 `a_n` 为铺满 `2×n` 的方法数。

### 12.1 先算小的

#### `a1`

只能放一块竖砖，所以：

`a1 = 19`

#### `a2`

有两种情况：

1. 两块竖砖：`19^2`
2. 两块横砖上下叠着放：`4^2`

所以：

`a2 = 19^2 + 4^2 = 361 + 16 = 377`

#### `a3`

递推也可以直接算：

`a3 = 19*a2 + 16*a1`

`= 19*377 + 16*19`

`= 7467`

### 12.2 为什么递推是这个样子

看最右边：

- 如果最后一列放一块竖砖：剩下 `2×(n-1)`，方法数是 `a_{n-1}`，再乘 19
- 如果最后两列放两块横砖：剩下 `2×(n-2)`，方法数是 `a_{n-2}`，再乘 `4^2 = 16`

所以：

`a_n = 19*a_{n-1} + 16*a_{n-2}`

如果题目写成

`a_{n+1} = C1*a_n + C2*a_{n-1}`

那么：

- `C1 = 19`
- `C2 = 16`

### 12.3 `a5`

继续算：

- `a4 = 19*a3 + 16*a2 = 147905`
- `a5 = 19*a4 + 16*a3 = 2929667`

所以：

`a5 = 2929667`

---

## 13. 度序列 `a,b,3,2,2,2,1,1`

这里给的是 8 个点，且 `a ≥ b ≥ 3`。

### 13.1 (a) 一个 multigraph 都不可能存在

举例可以取：

`a = 3, b = 3`

这时度序列总和：

`3+3+3+2+2+2+1+1 = 17`

总和是奇数，不可能是任何图的度数和，所以 multigraph 和 graph 都不可能。

### 13.2 (b) graph 存在

可取：

`a = 4, b = 3`

这时度序列是：

`4,3,3,2,2,2,1,1`

它是图形的。  
如果你想验证，就用 Havel-Hakimi：

1. 去掉 4，把后面 4 个数各减 1
2. 重排
3. 继续做

最后不会出负数，所以可实现。

### 13.3 (c) multigraph 存在，但 graph 不存在

可取：

`a = 8, b = 3`

这时度序列是：

`8,3,3,2,2,2,1,1`

#### 为什么 simple graph 不存在

8 个点的 simple graph 中，一个点最大度数只能是 7。  
但这里有一个点度数是 8，所以 simple graph 不可能。

#### 为什么 multigraph 可以

multigraph 允许重边，所以一个点的度数可以超过 7。  
只要总度数是偶数，并且不违背 multigraph 的基本条件，就可以构造出来。

---

## 14. Hasse 图

集合：

`S = {1,2,4,6,10,15,16,18,20}`

Hasse 图只画“cover relation”。

### 14.1 边怎么找

如果 `a | b`，但中间没有别的元素 `c` 满足 `a | c | b`，那 `a` 和 `b` 在 Hasse 图里相连。

这题的边是：

- `1-2`
- `1-15`
- `2-4`
- `2-6`
- `2-10`
- `4-16`
- `4-20`
- `6-18`
- `10-20`

所以边数是：

`9`

### 14.2 顶点 4 的 degree

顶点 4 连着：

- 2
- 16
- 20

所以 degree 是：

`3`

### 14.3 找一个 A，使 Hasse 图是 K2,2

K2,2 是“左右各两个点，中间四条边”的结构。

可取：

`A = {2,3,30,42}`

因为：

- `2 | 30, 2 | 42`
- `3 | 30, 3 | 42`

而且这四个数之间没有更小的中间数，所以 Hasse 图就是 K2,2。

### 14.4 找一个 B，使 Hasse 图是 C5

可取：

`B = {1,2,3,4,12}`

Hasse 边是：

- `1-2`
- `1-3`
- `2-4`
- `4-12`
- `3-12`

于是无向图正好是一个五边形：

`1 - 2 - 4 - 12 - 3 - 1`

所以它是 `C5`。

### 14.5 为什么没有 3-cycle

Hasse 图里的边表示 cover relation。  
如果出现三角形，就会有三个元素两两可比。  
这会迫使其中一条边不是“直接覆盖”，因为中间一定会夹着第三个元素，和 Hasse 图定义冲突。

所以 Hasse 图不可能有 3-cycle。

---

## 15. Truth table 题

### (a) 部分真值表反推命题

表格是：

- `p=T, q=F` 时，X = T
- `p=F, q=T` 时，X = T
- 另外两行空着

候选命题里：

- `p ∨ q` 可以
- `q → ¬p` 可以
- `q → p` 不行
- `¬p ∧ q` 不行

#### 为什么

- `p ∨ q` 在 `T,F` 和 `F,T` 都是 T
- `q → ¬p` 在 `T,F` 和 `F,T` 都是 T
- `q → p` 在 `T,F` 是 F，和表冲突
- `¬p ∧ q` 在 `T,F` 是 F，和表冲突

所以正确的是：

- `p∨q`
- `q→¬p`

### (b) 填部分 truth table，使 `Y1` 不等价于 `Y2`

总共有 8 行，每行两个空格，共 16 格。  
题目里已经给了 3 个固定值，所以还有 13 个空格可填。

所有填法总数：

`2^13 = 8192`

如果要求 `Y1` 和 `Y2` 逻辑等价，那么每一行都必须一样。  
那 3 个已知格已经强制了 3 行的配对，剩下 5 行每行只有 2 种选择，所以等价的填法有：

`2^5 = 32`

因此“不等价”的填法数：

`8192 - 32 = 8160`

### (c) 填部分 truth table，使 `Z1 ⇒ Z2`

`Z1 ⇒ Z2` 只有一种情况是假：

`Z1 = T, Z2 = F`

题目中已经有 4 个固定格，其中有一行把 `Z1 = T` 固定了，所以那一行的 `Z2` 必须填 T。

剩下 11 个空格都可自由填：

`2^11 = 2048`

所以答案是：

`2048`

---

## 16. 证明 outlines：哪些不可能证明 `如果 X multivalued，那么 X doubly-Euclidean`

原命题是：

`M → D`

### 可以证明原命题的三种经典路线

1. 直接证：假设 M，推出 D
2. 逆否证：假设 `¬D`，推出 `¬M`
3. 反证：假设 `M ∧ ¬D`，导出矛盾

### 哪些 outline 不可能

- `Suppose X is not doubly-Euclidean ... Therefore X is not multivalued.`  
  这是逆否，**可以**。

- `Suppose X is both multivalued and doubly-Euclidean ... This is a contradiction.`  
  这只能说明 `¬(M ∧ D)`，不是 `M → D`，**不行**。

- `Suppose X is multivalued ... Therefore X is doubly-Euclidean.`  
  这是直接证，**可以**。

- `Suppose X is multivalued ... Therefore X is not doubly-Euclidean.`  
  这是反着推，**不行**。

- `Suppose X is multivalued and not doubly-Euclidean ... This is a contradiction.`  
  这是反证法，**可以**。

- `Suppose X is not multivalued ... Therefore X is not doubly-Euclidean.`  
  这是 `¬M → ¬D`，不是原命题，**不行**。

- `Suppose either X is multivalued or X is doubly-Euclidean ... This is a contradiction.`  
  这是证明 `¬(M ∨ D)`，和原命题无关，**不行**。

- `Suppose X is multivalued ... Therefore X is not multivalued.`  
  自相矛盾，**不行**。

所以不可能的有：

`2,4,6,7,8`

---

## 17. Pakamen

类型分别是 `a,b,c,d,e,f`。

### (a) 找满足条件的子集 S

#### (i) `|S| ≥ 2` 且关系在 S 上是 reflexive

reflexive 要求每个类型都打自己。  
查看表格，只有 `d` 和 `f` 打自己。

所以可取：

`{d,f}`

#### (ii) `|S| ≥ 2` 且关系在 S 上是 symmetric

要满足：如果 x 打 y，那么 y 也要打 x。

看表格可以发现 `a` 和 `e` 互相都能打，所以可取：

`{a,e}`

#### (iii) `|S| ≥ 3` 且关系在 S 上是 transitive

一个好例子是：

`{a,b,c}`

因为在这个子集里，若 `aRb` 且 `bRc` 之类的链条都能保持传递性。

### (b) `M^2` 的 `(2,4)` 项是 2 是什么意思

`M^2_{2,4} = 2` 的意思是：

从类型 `b` 到类型 `d`，存在 **2 条长度为 2 的 bonus-damage 路径**。

也就是有 2 个中间类型 `x`，使得：

`bRx` 且 `xRd`

### (c) 新 Type Chart 的总数

现在只有 5 种类型。  
每一种类型都要能打 1 或 2 种别的类型。

对某一行来说，能选的目标集合数是：

`C(5,1) + C(5,2) = 5 + 10 = 15`

5 行独立，所以总数是：

`15^5`

---

## 18. 数学归纳法

要证：

`10 | 17^n - 7^n`

### 18.1 base case

取 `n = 1`：

`17^1 - 7^1 = 10`

显然是 10 的倍数。

### 18.2 inductive hypothesis

假设对某个自然数 n，已经有：

`17^n - 7^n = 10m`

### 18.3 inductive step

要证 n+1 时也成立：

`17^(n+1) - 7^(n+1)`

把它拆开：

`17^(n+1) - 7^(n+1) = 17(17^n - 7^n) + 7^n(17-7)`

这一步的目的，是把“已知可被 10 整除的部分”单独拎出来。

代入归纳假设：

`17(17^n - 7^n) = 17 * 10m = 10*(17m)`

而：

`7^n(17-7) = 10*7^n`

所以整个表达式仍是 10 的倍数。

如果题目要填空，常见形式是：

- `n = 1`
- `17^1 - 7^1 = 10`
- 乘数是 `10`
- 假设 `10 | 17^n - 7^n`
- 取 `(a,b,c) = (17*7^n, 17, 7^n)`

### 18.4 为什么 Pranav 的方法不能直接替代归纳法

因为题目要求的是 induction proof。  
Pranav 的方法是直接因式分解，虽然可能更短，但它不是“先证 base case，再证 n→n+1”的归纳结构，所以不符合题目要求。

---

## 19. 幂集与笛卡尔积

`S = P({2,5} × {2,3,4}) × P({3,5} × {1,3,5})`

### 19.1 先算两个底集合有多少元素

- `{2,5} × {2,3,4}` 有 `2*3 = 6` 个元素
- `{3,5} × {1,3,5}` 有 `2*3 = 6` 个元素

所以每个幂集都有：

`2^6 = 64`

个子集。

### 19.2 S 的大小

S 是两个幂集的笛卡尔积，所以：

`|S| = 64 * 64 = 4096`

### 19.3 写一个大小为 2 的子集

比如可以写成：

`{({},{}), ({(2,2)}, {})}`

这里每个元素本身是一个有序对，第一项来自第一个幂集，第二项来自第二个幂集。

---

## 20. 分配 lollies

### (a) 把每个小题对应到答案

#### (i) 41 个不同 lollies，分给 54 个孩子

每个 lolly 都可以去 54 个孩子中的任意一个：

`54^41`

对应选项 13。

#### (ii) 41 个不同 lollies，且没有孩子拿超过 1 个

这是从 54 个孩子里选 41 个不同位置给 41 个不同物品：

`P(54,41)`

对应选项 1。

#### (iii) 79 个不同 lollies，且至少有一个孩子拿不到

总分法数是：

`54^79`

要“至少一个孩子空手”，用容斥：

`C(54,1)53^79 - C(54,2)52^79 + C(54,3)51^79 - ...`

对应选项 2。

#### (iv) 79 个相同 lollies

把 79 个相同球放进 54 个盒子里，stars and bars：

`C(79+54-1,54-1) = C(132,53)`

对应选项 16。

#### (v) 178 个相同 lollies，且任意两孩子拿到的数量相差不超过 1

平均数是：

`178/54 = 3` 余 16

所以：

- 16 个孩子拿 4 个
- 38 个孩子拿 3 个

只要选出哪 16 个孩子拿 4 个：

`C(54,16)`

对应选项 15。

#### (vi) 41 种 lollies，每种无限，且每个孩子拿 16 种不同的 lollies

每个孩子都从 41 种里选 16 种：

`C(41,16)`

54 个孩子独立，所以：

`C(41,16)^54`

对应选项 11。

### (b) 为什么 `(nk)!/(k!)^n`

有 n 个孩子，每个孩子要拿 k 个不同的、且总共 `nk` 个都不同的 lollies。

先把 `nk` 个不同 lollies 排成一列，有：

`(nk)!`

种。

然后把这一列切成 n 组，每组 k 个。  
但每一组内部顺序不重要，所以每组要除以 `k!`，共除 n 次。

所以总数是：

`(nk)! / (k!)^n`

### (c) 13 个孩子，169 个 lollies，其中 7 个相同，其余不同，每个孩子拿 13 个不同 lollies

先选哪 7 个孩子各拿到 1 个相同的 lolly：

`C(13,7)`

剩下 162 个都是不同的，分配成：

- 7 个孩子各拿 12 个不同的
- 6 个孩子各拿 13 个不同的

所以排列数是：

`162! / (12!)^7 (13!)^6`

总数：

`C(13,7) * 162! / ((12!)^7 * (13!)^6)`

---

## 21. Jury duty

前提：

- `P1: s → ¬b`
- `P2: d ∨ h`
- `P3: h → b`
- `P4: s`

结论要证的是：

`d`

### 21.1 先翻译成逻辑式

所以整句是：

`(s→¬b) ∧ (d∨h) ∧ (h→b) ∧ s`

### 21.2 推理顺序

1. 由 `s` 和 `s→¬b`，用 **Modus Ponens** 得到 `¬b`
2. 由 `h→b` 和 `¬b`，用 **Modus Tollens** 得到 `¬h`
3. 由 `d∨h` 和 `¬h`，用 **Elimination** 得到 `d`

### 21.3 为什么 verdict “the criminal is dead” 正确

因为我们已经从已知前提出发，合法推出 `d`。  
所以你在陪审团里说“the criminal is dead”是正确的。

---

## 22. 最后一句实话

这份 guide 现在是按“从零开始能学”的思路写的。  
如果你要，我下一步可以继续把它变成：

**“逐题讲解版 + 每题最后标准答案版”**

那样你就可以一题一题照着学。
