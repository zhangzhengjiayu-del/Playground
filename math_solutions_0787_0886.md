# IMG_0787 到 IMG_0886 题目讲解

说明：下面按图片顺序整理。重点放在“为什么这样做”和“以后遇到同类题怎么想”，不是只给答案。

## IMG_0787/IMG_0789：向量表达式是否 well-defined

题意：设 \(x,y,z\) 是 \(\mathbb R^3\) 中的非零向量，\(t\) 是非零实数。判断哪些表达式是有定义的。

答案：

- \(y-x\times z\)
- \(y\cdot(x-2z)\)
- \((x\times tz)\cdot y\)
- \(\sqrt{x\cdot x}\)

知识点：

1. 点积 \(u\cdot v\)：输入两个同维向量，输出一个实数。
2. 叉积 \(u\times v\)：在 \(\mathbb R^3\) 中输入两个向量，输出一个向量。
3. 向量加减只能和向量加减，标量加减只能和标量加减，不能把“标量 + 向量”混在一起。
4. 标量可以乘向量，所以 \(tz\)、\(2z\) 都是向量。
5. \(\sqrt{\cdot}\)、\(\sin(\cdot)\) 在这类基础题中默认作用在实数上，不作用在向量上。

逐项判断：

- \(\sqrt{y\times y}\)：\(y\times y\) 是向量，不能对向量开平方，所以无定义。
- \(x\cdot y+x\times y\)：\(x\cdot y\) 是标量，\(x\times y\) 是向量，标量不能和向量相加，所以无定义。
- \(t\sin^3(z)\)：\(z\) 是向量，\(\sin(z)\) 默认无定义，所以无定义。
- \(y-x\times z\)：\(x\times z\) 是向量，\(y\) 也是向量，向量减向量有定义。
- \(y\cdot(x-2z)\)：\(x-2z\) 是向量，和 \(y\) 做点积有定义。
- \((x\times tz)\cdot y\)：\(tz\) 是向量，\(x\times tz\) 是向量，再和 \(y\) 点积有定义。
- \(\sqrt{x\cdot x}\)：\(x\cdot x=\|x\|^2\) 是非负实数，所以平方根有定义。
- \(x\cdot y+tz\)：\(x\cdot y\) 是标量，\(tz\) 是向量，不能相加，所以无定义。

做这类题的通用方法：先不要急着算，先给每一小块标类型：scalar 还是 vector。只要出现“标量和向量相加”“函数套向量”“点积/叉积输入不是向量”等，就不是 well-defined。

## IMG_0790/IMG_0791：平面、标量三重积、距离与介值定理

题意：平面 \(\Pi\) 经过
\[
A(0,3,-1),\quad B(-2,2,-1),\quad C(4,4,-2).
\]
额外点集合
\[
S=\{P_1(-4,-3,2),P_2(1,-3,4),P_3(-2,4,-2),P_4(-6,1,0),P_5(-2,2,-3),P_6(6,5,-2)\}.
\]
已知三角形 \(ABC\) 面积为 \(3/2\)，并给出
\[
\overrightarrow{AP_i}\cdot(\overrightarrow{AB}\times\overrightarrow{AC})
\]
的值：
\[
P_1:14,\quad P_2:23,\quad P_3:-6,\quad P_4:0,\quad P_5:-4,\quad P_6:0.
\]

### (a) 哪些点在平面 \(\Pi\) 上？

答案：\(P_4,P_6\)。

知识点：若
\[
n=\overrightarrow{AB}\times\overrightarrow{AC},
\]
则 \(n\) 是平面 \(\Pi\) 的法向量。点 \(P\) 在平面 \(\Pi\) 上当且仅当
\[
\overrightarrow{AP}\cdot n=0.
\]

这里题目已经给出各点的 \(\overrightarrow{AP_i}\cdot n\)。值为 0 的点就是在平面上的点，所以是 \(P_4,P_6\)。

### (b) 不在平面上的点中，哪个离平面最近？距离是多少？

答案：最近的是 \(P_5\)，距离是
\[
\frac{4}{3}.
\]

知识点：点 \(P\) 到平面 \(\Pi\) 的距离公式是
\[
d=\frac{|\overrightarrow{AP}\cdot n|}{\|n\|}.
\]

又因为三角形 \(ABC\) 的面积满足
\[
\text{Area}(ABC)=\frac12\|\overrightarrow{AB}\times\overrightarrow{AC}\|
=\frac12\|n\|.
\]
题目给 \(\text{Area}(ABC)=3/2\)，所以
\[
\|n\|=3.
\]

不在平面上的点对应的绝对值分别是
\[
|14|,\ |23|,\ |-6|,\ |-4|.
\]
最小非零值是 \(4\)，对应 \(P_5\)。因此距离是
\[
d=\frac{4}{3}.
\]

### (c) 哪些点在平面 \(\Pi\) 的、相对于 \(P_1\) 的另一侧？

答案：\(P_3,P_5\)。

知识点：\(\overrightarrow{AP}\cdot n\) 的正负号表示点在平面的哪一侧。

- \(P_1\) 的值是 \(14>0\)，所以 \(P_1\) 在“正侧”。
- 与 \(P_1\) 相反一侧的点要满足值 \(<0\)。
- 给出的负值是 \(P_3:-6\)、\(P_5:-4\)。

所以答案是 \(P_3,P_5\)。注意值为 0 的点在平面上，不属于任何一侧。

### (d) 用介值定理证明路径上某点落在平面上

题意大意：\(x,y,z:[0,1]\to\mathbb R\) 连续，定义
\[
R(t)=(x(t),y(t),z(t)).
\]
假设起点 \(P=R(0)\) 和终点 \(Q=R(1)\) 在平面 \(\Pi\) 的两侧。定义
\[
f(t)=\overrightarrow{AR(t)}\cdot(\overrightarrow{AB}\times\overrightarrow{AC}).
\]
要求用介值定理说明存在 \(c\in(0,1)\)，使得 \(R(c)\) 在平面 \(\Pi\) 上。

答案使用的定理：Intermediate Value Theorem，介值定理。

完整论述：

因为 \(x(t),y(t),z(t)\) 都是连续函数，所以
\[
R(t)=(x(t),y(t),z(t))
\]
随 \(t\) 连续变化。向量 \(\overrightarrow{AR(t)}=R(t)-A\) 的每个坐标也都是连续函数。再与固定向量
\[
n=\overrightarrow{AB}\times\overrightarrow{AC}
\]
做点积，只是坐标函数的线性组合，因此
\[
f(t)=\overrightarrow{AR(t)}\cdot n
\]
是 \([0,1]\) 上的连续函数。

又因为 \(R(0)\) 和 \(R(1)\) 在平面 \(\Pi\) 的两侧，所以
\[
f(0)\quad\text{和}\quad f(1)
\]
异号。也就是说，一个正、一个负。由于 \(f\) 在闭区间 \([0,1]\) 上连续，根据介值定理，0 介于 \(f(0)\) 和 \(f(1)\) 之间，因此存在某个
\[
c\in(0,1)
\]
使得
\[
f(c)=0.
\]
而
\[
f(c)=\overrightarrow{AR(c)}\cdot n=0
\]
正是点 \(R(c)\) 在平面 \(\Pi\) 上的判定条件。因此路径中必有一点落在平面 \(\Pi\) 上。

这题的核心思想：用一个“带符号距离函数” \(f(t)\) 把三维几何问题变成一维连续函数问题。两端在平面两侧表示函数值异号；连续路径不能从正跳到负而不经过 0，所以中间一定穿过平面。

## IMG_0792/IMG_0793/IMG_0794：含参数 \(m\) 的线性方程组

题意：讨论下面方程组在不同 \(m\) 下的解的情况：
\[
\begin{cases}
mx+9y+3z=-m^3+m^2-8m+15,\\
x+my+z=-m^2,\\
4x+y+16mz=-m.
\end{cases}
\]

由图片里的行化简可得到一个关键的阶梯形增广矩阵：
\[
\begin{bmatrix}
1&m&1&|-m^2\\
0&-1&4&|m\\
0&0&4m+13&|m^2+2m+5
\end{bmatrix}.
\]

但是要特别注意：图片中的化简过程曾经除以 \(4m-1\) 和 \(3-m\)，所以
\[
m=\frac14,\quad m=3
\]
不能直接套最后这个矩阵，必须单独检查。

### 结论

- 无解：\[
m=-\frac{13}{4}.
\]
- 无穷多解：\[
m=3,\quad m=\frac14.
\]
- 唯一解：\[
m\in\mathbb R\setminus\left\{-\frac{13}{4},\frac14,3\right\}.
\]

当 \(m=3\) 时，解集可以写成直线：
\[
\mathbf x
=
\begin{bmatrix}
-39/4\\
0\\
3/4
\end{bmatrix}
+s
\begin{bmatrix}
-13/4\\
1\\
1/4
\end{bmatrix},
\quad s\in\mathbb R.
\]

### 为什么这样分类？

知识点 1：线性方程组的解的个数看“主元”和“矛盾行”。

行化简后，如果出现
\[
[0\quad 0\quad 0\ |\ c],\quad c\ne0,
\]
就表示 \(0=c\)，这是矛盾，所以无解。

如果没有矛盾行，并且每个未知数列都有主元，那么唯一解。

如果没有矛盾行，但至少有一个自由变量，那么无穷多解。

知识点 2：含参数题最容易漏掉“除数为 0”的特殊值。

普通化简过程中如果除以了某个含 \(m\) 的式子，例如 \(4m-1\)，那就默认了 \(4m-1\ne0\)。因此 \(m=1/4\) 必须拿回原方程或拿回除法前的矩阵重新判断。\(3-m\) 也是同理，所以 \(m=3\) 也要单独讨论。

知识点 3：最后一行决定一般情况下的特殊值。

最后一行对应
\[
(4m+13)z=m^2+2m+5.
\]
如果 \(4m+13\ne0\)，那么 \(z\) 有唯一值，继续回代得到 \(y,x\)，所以唯一解。

如果
\[
4m+13=0,
\]
即
\[
m=-\frac{13}{4},
\]
则左边变成 0。此时右边为
\[
m^2+2m+5
=\left(-\frac{13}{4}\right)^2+2\left(-\frac{13}{4}\right)+5
=\frac{169}{16}-\frac{104}{16}+\frac{80}{16}
=\frac{145}{16}\ne0.
\]
所以最后一行变成
\[
0=\frac{145}{16},
\]
矛盾，因此无解。

而 \(m=3\) 和 \(m=1/4\) 是除法中丢出来的特殊值。单独代回去检查后，它们没有矛盾，但秩小于未知数个数，因此有自由变量，所以是无穷多解。

做这种题的通用流程：

1. 先行化简，找到主元表达式。
2. 凡是化简中除过的参数式，都列为“特殊值”。
3. 对普通情况用阶梯矩阵分类。
4. 对特殊值逐个代回原方程或除法前矩阵，判断无解、唯一解、无穷多解。

## IMG_0795：矩阵乘法中某个元素 \(C_{2,3}\)

题意：给定
\[
A=
\begin{bmatrix}
5&6&4&3&2\\
4&4&2&5&3\\
5&8&7&9&3\\
7&5&2&9&3
\end{bmatrix},
\quad
B=
\begin{bmatrix}
4&9&4\\
4&2&2\\
6&7&6\\
3&8&9\\
8&4&5
\end{bmatrix}.
\]
令 \(C=AB\)，求 \(C_{2,3}\)。

答案：
\[
C_{2,3}=96.
\]

知识点：矩阵乘法的第 \(i,j\) 个元素等于“左矩阵第 \(i\) 行”点乘“右矩阵第 \(j\) 列”：
\[
C_{i,j}=\text{row}_i(A)\cdot \text{col}_j(B).
\]

这里
\[
\text{row}_2(A)=(4,4,2,5,3),
\]
\[
\text{col}_3(B)=
\begin{bmatrix}
4\\2\\6\\9\\5
\end{bmatrix}.
\]
所以
\[
C_{2,3}
=4\cdot4+4\cdot2+2\cdot6+5\cdot9+3\cdot5
=16+8+12+45+15
=96.
\]

做这类题不要把行列位置弄反：\(C_{2,3}\) 的 “2” 找 \(A\) 的第 2 行，“3” 找 \(B\) 的第 3 列。

## IMG_0796：行化简中断后的线性方程组判断

题意：原增广矩阵为
\[
\begin{bmatrix}
1&2&6&|&53\\
1&1&2&|&20\\
1&3&10&|&86
\end{bmatrix},
\]
中断时得到等价矩阵
\[
\begin{bmatrix}
1&2&6&|&53\\
0&1&4&|&a\\
0&b&4&|&33
\end{bmatrix}.
\]
求 \(a,b\)，并判断系统解的情况。

答案：
\[
a=33,\quad b=1.
\]
几何上，方程 \(x+2y+6z=53\) 表示 \(\mathbb R^3\) 中的一个平面。

化为行阶梯形后，左边有 2 个 leading columns，右边增广列不是 leading column，所以系统 consistent with infinitely many solutions。

知识点：

1. 三个未知数的一条线性方程
\[
ax+by+cz=d
\]
通常表示 \(\mathbb R^3\) 中的平面。
2. 行变换不改变线性方程组的解集。
3. 若增广列不是主元列，说明没有出现矛盾行；若未知数列主元少于未知数个数，说明有自由变量，所以无穷多解。

为什么 \(a=33,b=1\)：

用第一行消去下面两行的第一列：
\[
R_2\leftarrow R_1-R_2
\]
得到
\[
[0,1,4\ |\ 33],
\]
所以 \(a=33\)。

同理
\[
R_3\leftarrow R_3-R_1
\]
得到
\[
[0,1,4\ |\ 33],
\]
所以 \(b=1\)。

继续化简时，第二行和第三行相同，因此第三行可以变成零行：
\[
\begin{bmatrix}
1&2&6&|&53\\
0&1&4&|&33\\
0&0&0&|&0
\end{bmatrix}.
\]
没有矛盾，但只有两个主元，对三个未知数来说会有一个自由变量，因此无穷多解。几何上可以理解为多个平面的交集是一条直线。

## IMG_0797/IMG_0798/IMG_0799：分部积分与反常积分判断

### (a)(i) 分部积分：\(\int e^{11x}\cos(3x)\,dx\)

题中要把
\[
\int e^{11x}\cos(3x)\,dx
\]
写成
\[
p(x)-\int -\frac{3}{11}e^{11x}\sin(3x)\,dx.
\]

答案：
\[
p(x)=\frac1{11}e^{11x}\cos(3x),\quad u(x)=\cos(3x).
\]

知识点：分部积分公式
\[
\int u(x)v'(x)\,dx=u(x)v(x)-\int v(x)u'(x)\,dx.
\]

这里取
\[
u(x)=\cos(3x),\quad v'(x)=e^{11x}.
\]
那么
\[
u'(x)=-3\sin(3x),\quad v(x)=\frac1{11}e^{11x}.
\]
所以
\[
\int e^{11x}\cos(3x)\,dx
=\frac1{11}e^{11x}\cos(3x)
-\int \frac1{11}e^{11x}(-3\sin(3x))\,dx,
\]
即
\[
=\frac1{11}e^{11x}\cos(3x)
-\int -\frac3{11}e^{11x}\sin(3x)\,dx.
\]

### (a)(ii) 分部积分：\(\int \cos^{-1}(3x)\,dx\)

题中形式为
\[
\int \cos^{-1}(3x)\,dx
=x\cos^{-1}(3x)-\int q(x)\,dx.
\]

答案：
\[
q(x)=-\frac{3x}{\sqrt{1-9x^2}},\quad u(x)=\cos^{-1}(3x).
\]

知识点：遇到反三角函数积分，常取
\[
u=\text{反三角函数},\quad v'=1.
\]

取
\[
u(x)=\cos^{-1}(3x),\quad v'(x)=1.
\]
则
\[
v(x)=x.
\]
又因为
\[
\frac{d}{dx}\cos^{-1}(3x)
=-\frac{3}{\sqrt{1-9x^2}},
\]
所以
\[
\int \cos^{-1}(3x)\,dx
=x\cos^{-1}(3x)-\int x\left(-\frac{3}{\sqrt{1-9x^2}}\right)\,dx.
\]
因此
\[
q(x)=-\frac{3x}{\sqrt{1-9x^2}}.
\]

### (b) 用分部积分计算 \(\int_0^8(4x^2+5x+9)f'(x)\,dx\)

已知
\[
\int_0^8x^2f(x)\,dx=-3,\quad
\int_0^8xf(x)\,dx=8,\quad
\int_0^8f(x)\,dx=-8,
\]
\[
f(0)=4,\quad f(8)=9.
\]
求
\[
\int_0^8(4x^2+5x+9)f'(x)\,dx.
\]

答案：
\[
2685.
\]

令
\[
P(x)=4x^2+5x+9.
\]
分部积分给出
\[
\int_0^8P(x)f'(x)\,dx
=\left[P(x)f(x)\right]_0^8-\int_0^8P'(x)f(x)\,dx.
\]

先算边界项：
\[
P(8)=4\cdot64+5\cdot8+9=305,\quad P(0)=9.
\]
所以
\[
\left[P(x)f(x)\right]_0^8
=305\cdot9-9\cdot4
=2745-36
=2709.
\]

再算积分项：
\[
P'(x)=8x+5.
\]
因此
\[
\int_0^8P'(x)f(x)\,dx
=8\int_0^8xf(x)\,dx+5\int_0^8f(x)\,dx
=8\cdot8+5(-8)
=64-40
=24.
\]

所以
\[
\int_0^8P(x)f'(x)\,dx=2709-24=2685.
\]

### (c) 带权函数的分部积分与反常积分

题意：设
\[
h(x)=e^{-4x^2+5x}g'(x),
\]
并已知
\[
\int_3^6xe^{-4x^2+5x}g(x)\,dx=2,\quad
\int_3^6e^{-4x^2+5x}g(x)\,dx=9,
\]
且 \(g(x)=0\) for \(x\notin(3,6)\)。

#### (i) 求 \(\int_3^6h(x)\,dx\)

答案：
\[
-29.
\]

令
\[
w(x)=e^{-4x^2+5x}.
\]
则
\[
h(x)=w(x)g'(x).
\]
分部积分：
\[
\int_3^6w(x)g'(x)\,dx
=\left[w(x)g(x)\right]_3^6-\int_3^6w'(x)g(x)\,dx.
\]

因为 \(g(x)=0\) 在区间外，且 \(g\) 连续，所以端点处可看作
\[
g(3)=g(6)=0,
\]
边界项为 0。

又
\[
w'(x)=(-8x+5)e^{-4x^2+5x}.
\]
所以
\[
\int_3^6h(x)\,dx
=-\int_3^6(-8x+5)e^{-4x^2+5x}g(x)\,dx.
\]
展开：
\[
=8\int_3^6xe^{-4x^2+5x}g(x)\,dx
-5\int_3^6e^{-4x^2+5x}g(x)\,dx.
\]
代入已知：
\[
=8\cdot2-5\cdot9
=16-45
=-29.
\]

#### (ii) 反常积分

\[
\int_0^\infty h(x)\,dx
\]
收敛，并且等于 (c)(i) 的值，也就是 \(-29\)。

原因：因为 \(g(x)=0\) 在 \((3,6)\) 外，连续可导时在外面也有 \(g'(x)=0\)，所以
\[
h(x)=0
\]
在主要区间外为 0。因此从 \(0\) 到 \(\infty\) 的积分实际上只剩下从 \(3\) 到 \(6\) 的部分。

#### (iii) 正确判断语句

正确的是：

- \(\displaystyle \int_6^R h(x)\,dx=0\) for \(R>6\)。
- \(\displaystyle \int_0^\infty h(x)\,dx=\lim_{R\to\infty}\int_0^R h(x)\,dx\)。
- \(\displaystyle \int_0^3 h(x)\,dx=0\)。

不正确的是：

- \(\displaystyle \int_0^\infty h(x)\,dx=\lim_{R\to\infty}h(R)\)。积分的极限不是函数值的极限。
- \(\displaystyle \int_3^R h(x)\,dx=0\) for \(R>3\)。如果 \(R\) 在 \(3\) 到 \(6\) 之间，积分一般不为 0。
- \(\displaystyle \int_0^6h(x)\,dx=0\)。事实上它等于 \(\int_3^6h(x)\,dx=-29\)。

这类题的核心：不要被复杂指数吓到。把指数因子命名成 \(w(x)\)，题目就变成标准的 \(\int w g'\) 分部积分。

## IMG_0800/IMG_0801：由图像计算积分函数 \(G(x)=\int_1^x g(t)\,dt\)

图像给出 \(g\) 是三段线段，关键点为
\[
(-1,0),\quad (1,5),\quad (2,1),\quad (6,-2).
\]
定义
\[
G(x)=\int_1^xg(t)\,dt.
\]

### (i) 求 \(G(-1)\) 和 \(G(6)\)

答案：
\[
G(-1)=-5,\quad G(6)=1.
\]

知识点：定积分是“带符号面积”。并且
\[
\int_a^b=-\int_b^a.
\]

先算
\[
G(-1)=\int_1^{-1}g(t)\,dt=-\int_{-1}^1g(t)\,dt.
\]
从 \(-1\) 到 \(1\)，图像是一条从 0 到 5 的线段，面积是梯形面积：
\[
\frac{0+5}{2}\cdot2=5.
\]
所以
\[
G(-1)=-5.
\]

再算
\[
G(6)=\int_1^6g(t)\,dt
=\int_1^2g(t)\,dt+\int_2^6g(t)\,dt.
\]
从 \(1\) 到 \(2\)，高度从 5 到 1：
\[
\frac{5+1}{2}\cdot1=3.
\]
从 \(2\) 到 \(6\)，高度从 1 到 \(-2\)：
\[
\frac{1+(-2)}{2}\cdot4=-2.
\]
所以
\[
G(6)=3+(-2)=1.
\]

### (ii) 求 \(x=1\) 处 \(G\) 的瞬时变化率

答案：
\[
G'(1)=5.
\]

知识点：微积分基本定理：
\[
\frac{d}{dx}\int_1^xg(t)\,dt=g(x).
\]
因此
\[
G'(1)=g(1)=5.
\]

### (iii) 求 \(G\) 的全局最大值 \(M\)

答案：
\[
M=\frac{11}{3}.
\]

知识点：因为
\[
G'(x)=g(x),
\]
所以 \(G\) 在 \(g(x)>0\) 的地方递增，在 \(g(x)<0\) 的地方递减。全局最大值通常出现在 \(g\) 从正变负的位置，或者区间端点。

第三段线段从
\[
(2,1)
\]
到
\[
(6,-2).
\]
斜率是
\[
\frac{-2-1}{6-2}=-\frac34.
\]
该段方程可写为
\[
g(x)=1-\frac34(x-2).
\]
令 \(g(x)=0\)：
\[
1-\frac34(x-2)=0
\quad\Rightarrow\quad
x=\frac{10}{3}.
\]

所以最大值在 \(x=10/3\) 处。先有
\[
G(2)=\int_1^2g(t)\,dt=3.
\]
从 \(2\) 到 \(10/3\)，图像在 \(x\)-轴上方，是一个三角形，底为
\[
\frac{10}{3}-2=\frac43,
\]
高为 \(1\)，面积为
\[
\frac12\cdot\frac43\cdot1=\frac23.
\]
因此
\[
G\left(\frac{10}{3}\right)
=3+\frac23
=\frac{11}{3}.
\]

这题的通用方法：先看 \(G'(x)=g(x)\)，再看 \(g\) 的正负决定 \(G\) 增减；最后用图像面积算具体函数值。

## IMG_0802/IMG_0803/IMG_0804/IMG_0805：复数的根、模长与辐角运算

题意：左图给出 \(z\) 的 \(n\) 次根，右图给出 \(w\) 的 \(m\) 次根。绿色方块表示其中一个根 \(\alpha\)，粉色菱形表示其中一个根 \(\beta\)。

### (a) 求 \(n,m\)

答案：
\[
n=7,\quad m=11.
\]

知识点：一个非零复数的 \(n\) 次根一共有 \(n\) 个，并且在复平面上均匀分布在同一个圆上。图中数出左图有 7 个点，所以 \(n=7\)；右图有 11 个点，所以 \(m=11\)。

### (b) 求 \(\alpha\) 的模长和主辐角

从图上读得
\[
|\alpha|=0.4.
\]
图片中给出的角度为
\[
\Arg(\alpha)=\left(\frac1{12}+\frac67\right)\pi
=\frac{79\pi}{84}.
\]

知识点：极坐标形式
\[
re^{i\theta}
\]
中，\(r\) 是到原点的距离，\(\theta\) 是从正实轴逆时针转到该点的角。读图时先看点落在哪个半径圆上，再看它落在哪条射线上。

### (c) 求 \(w\) 的模长和主辐角

因为 \(\beta\) 是 \(w\) 的 11 次根，所以
\[
\beta^{11}=w.
\]
从图上读得
\[
|\beta|=0.6.
\]
于是
\[
|w|=|\beta|^{11}=0.6^{11}.
\]

角度方面，若 \(\Arg(\beta)=\theta\)，则
\[
\Arg(w)\equiv 11\theta \pmod{2\pi}.
\]
图片中填写的是
\[
\Arg(w)=\frac{11\pi}{6}.
\]
如果题目严格要求 Principal Argument，通常要化到 \((-\pi,\pi]\) 中，所以
\[
\frac{11\pi}{6}\equiv -\frac{\pi}{6}\pmod{2\pi}.
\]
也就是说，\(\frac{11\pi}{6}\) 和 \(-\frac{\pi}{6}\) 表示同一方向；是否需要写成 \(-\pi/6\) 要看系统对 principal argument 的格式要求。

### (d) 判断 \(\alpha\beta,\alpha/\beta,\beta/\alpha\) 对应哪个点

答案：

- \(\alpha\beta\) labelled \(M\)。
- \(\alpha/\beta\) labelled \(K\)。
- \(\beta/\alpha\) labelled \(G\)。

知识点：复数乘除在极坐标下最简单。

若
\[
\alpha=r_1e^{i\theta_1},\quad \beta=r_2e^{i\theta_2},
\]
则
\[
\alpha\beta=r_1r_2e^{i(\theta_1+\theta_2)}.
\]
也就是说：模长相乘，角度相加。

而
\[
\frac{\alpha}{\beta}=\frac{r_1}{r_2}e^{i(\theta_1-\theta_2)},\quad
\frac{\beta}{\alpha}=\frac{r_2}{r_1}e^{i(\theta_2-\theta_1)}.
\]
也就是说：模长相除，角度相减。

因此读图时不是先看坐标，而是先算“半径变化”和“旋转角变化”，再找对应点。

### (e) 判断 \(\overline\beta,i\alpha,\alpha^2\) 对应哪个点

答案：

- \(\overline\beta\) labelled \(J\)。
- \(i\alpha\) labelled \(B\)。
- \(\alpha^2\) labelled \(C\)。

知识点：

1. 共轭 \(\overline\beta\)：关于实轴反射，模长不变，角度变号：
\[
re^{i\theta}\mapsto re^{-i\theta}.
\]
2. 乘以 \(i\)：逆时针旋转 \(\pi/2\)，模长不变：
\[
i\alpha=|\alpha|e^{i(\Arg(\alpha)+\pi/2)}.
\]
3. 平方：模长平方，角度翻倍：
\[
\alpha^2=|\alpha|^2e^{i2\Arg(\alpha)}.
\]

这类复数图题的核心：一律先把点看成“半径 + 角度”。乘法加角度，除法减角度，共轭角度取反，乘 \(i\) 加 \(\pi/2\)，幂运算则模长取幂、角度乘幂次。

## IMG_0806/IMG_0807：幂函数极限 \(x^{6/(1-x)}\)

题意：设
\[
g(x)=x^{6/(1-x)},\quad x>1.
\]
求
\[
\lim_{x\to1^+}g(x).
\]

### (a) 判断不定式类型

当
\[
x\to1^+,
\]
底数
\[
x\to1^+,
\]
指数
\[
\frac6{1-x}\to-\infty.
\]
因此形式是
\[
1^{-\infty}.
\]

注意：这类 \(1^\infty\)、\(1^{-\infty}\)、\(0^0\)、\(\infty^0\) 都属于需要进一步处理的幂型不定式。

### (b) 写成指数形式 \(g(x)=e^{h(x)}\)

答案：
\[
h(x)=\frac6{1-x}\ln x.
\]

知识点：
\[
a^b=e^{b\ln a}
\]
适用于 \(a>0\)。这里 \(x>1\)，所以可以取对数。

因此
\[
x^{6/(1-x)}
=e^{\frac6{1-x}\ln x}.
\]

### (c) 求 \(\lim_{x\to1^+}h(x)\)

答案：
\[
\lim_{x\to1^+}h(x)=-6.
\]

计算：
\[
h(x)=\frac{6\ln x}{1-x}.
\]
当 \(x\to1^+\) 时，分子 \(\ln x\to0\)，分母 \(1-x\to0\)，是 \(0/0\) 型，可以用洛必达法则：
\[
\lim_{x\to1^+}\frac{6\ln x}{1-x}
=\lim_{x\to1^+}\frac{6/x}{-1}
=-6.
\]

也可以用标准极限
\[
\ln x\sim x-1\quad (x\to1)
\]
得到
\[
\frac{6\ln x}{1-x}
\sim \frac{6(x-1)}{1-x}
=-6.
\]

### (d) 求原极限

答案：
\[
\lim_{x\to1^+}g(x)=e^{-6}.
\]

因为指数函数 \(e^x\) 连续，所以
\[
\lim_{x\to1^+}g(x)
=\lim_{x\to1^+}e^{h(x)}
=e^{\lim_{x\to1^+}h(x)}
=e^{-6}.
\]

这类题的通用方法：

1. 遇到 \(f(x)^{p(x)}\)，先取对数或写成 \(e^{p(x)\ln f(x)}\)。
2. 先求指数里的极限 \(p(x)\ln f(x)\)。
3. 最后利用指数函数连续性，把极限放回 \(e^{(\cdot)}\)。

## IMG_0808 / IMG_0809：中值定理与导数上界

题目给出：

- \(f\) 在 \([26,32]\) 上连续；
- \(f\) 在 \((26,32)\) 上可导；
- 对所有 \(x\in(26,32)\)，有 \(f'(x)\le 70\)；
- \(f(31)=5\)。

### (a) 求 \(f(32)\) 的一个上界

答案：
\[
f(32)\le 75.
\]

理由：

在区间 \([31,32]\) 上，\(f\) 连续且在 \((31,32)\) 上可导，所以由中值定理，存在
\[
c\in(31,32)
\]
使得
\[
\frac{f(32)-f(31)}{32-31}=f'(c).
\]

因为 \(f'(c)\le70\)，所以
\[
f(32)-f(31)\le70.
\]
又因为 \(f(31)=5\)，所以
\[
f(32)\le5+70=75.
\]

### (b) 证明 \(f(26)\ge -345\)

证明：

因为 \(f\) 在 \([26,31]\) 上连续，并且在 \((26,31)\) 上可导，所以由中值定理，存在某个
\[
c\in(26,31)
\]
使得
\[
\frac{f(31)-f(26)}{31-26}=f'(c).
\]

代入 \(f(31)=5\)，得到
\[
\frac{5-f(26)}5=f'(c).
\]

题目已知对所有区间内的点都有 \(f'(x)\le70\)，所以特别地
\[
f'(c)\le70.
\]

因此
\[
\frac{5-f(26)}5\le70.
\]

两边乘以 \(5\)，得到
\[
5-f(26)\le350.
\]

移项：
\[
-f(26)\le345,
\]
所以
\[
f(26)\ge -345.
\]

这就证明了题目要求的结论。

知识点：

中值定理的核心作用是把「平均变化率」和「某一点的瞬时变化率」联系起来：
\[
\frac{f(b)-f(a)}{b-a}=f'(c).
\]

如果题目给的是导数上界 \(f'(x)\le M\)，通常可以推出函数值增长量的上界：
\[
f(b)-f(a)\le M(b-a).
\]

这类题一定要先检查中值定理条件：

1. 闭区间上连续；
2. 开区间上可导；
3. 然后套
   \[
   \frac{f(b)-f(a)}{b-a}=f'(c).
   \]

证明题写作模板：

先说明函数满足中值定理条件，再写「存在 \(c\) 使得……」，最后把导数范围代进去推出结论。证明题不能只写计算式，必须把存在性和定理名称说清楚。

## IMG_0810 / IMG_0811：边界三角形上的最大值和最小值为什么在顶点

题目给出一个可导函数
\[
f:\mathbb R\to\mathbb R
\]
并且 \(f\) 没有 stationary points，也就是说对所有 \(x\in\mathbb R\)，
\[
f'(x)\ne0.
\]

题目要求解释为什么
\[
f(4x+3y+z)
\]
在三角形 \(ABC\) 的边界上的最大值和最小值一定发生在顶点。

已知边 \(AB\) 的参数形式是
\[
\begin{pmatrix}x\\y\\z\end{pmatrix}
=
\begin{pmatrix}2\\1\\4\end{pmatrix}
+t\begin{pmatrix}-1\\-4\\-8\end{pmatrix},
\qquad 0\le t\le1.
\]

所以
\[
x=2-t,\qquad y=1-4t,\qquad z=4-8t.
\]

于是
\[
4x+3y+z
=4(2-t)+3(1-4t)+(4-8t)
=15-24t.
\]

因此在边 \(AB\) 上，原函数变成一元函数
\[
F_{AB}(t)=f(15-24t),\qquad 0\le t\le1.
\]

对它求导：
\[
F_{AB}'(t)=-24f'(15-24t).
\]

因为 \(f\) 没有 stationary points，所以
\[
f'(15-24t)\ne0.
\]
因此
\[
F_{AB}'(t)\ne0
\]
在边 \(AB\) 的内部 \(0<t<1\) 恒成立。

也就是说，边 \(AB\) 的内部没有临界点。由题目给出的定理，连续函数在闭区间上最大值和最小值一定存在，而且最大值点和最小值点都必须是 critical points。由于内部没有 critical point，所以最大值和最小值只能发生在端点，也就是 \(A\) 或 \(B\)。

同理，对边 \(AC\) 和边 \(BC\) 也可以作类似的参数化。每条边上都会得到形如
\[
F(t)=f(\alpha t+\beta)
\]
的一元函数，并且
\[
F'(t)=\alpha f'(\alpha t+\beta).
\]
只要该边不是让 \(4x+3y+z\) 恒定的边，且题目说明可作类似计算，就能得到边内部没有临界点。因此每条边上的最大值和最小值都只能发生在端点。

三角形边界由三条边 \(AB,AC,BC\) 组成。每条边的端点都是三角形的顶点，所以整个边界上的最大值和最小值只能发生在
\[
A,\quad B,\quad C
\]
这些顶点处。

知识点：

1. 多变量边界问题经常要先把边参数化，变成一元函数。
2. 线段参数化通常写成
   \[
   \mathbf r(t)=\mathbf A+t(\mathbf B-\mathbf A),\qquad 0\le t\le1.
   \]
3. 极值定理告诉我们闭区间上的连续函数有最大值和最小值。
4. 如果最大/最小值发生在内部，那么内部点必须是临界点。
5. 本题的关键是：因为 \(f'(x)\ne0\)，所以复合函数在边的内部也没有导数为 \(0\) 的点，因此极值只能在端点。

证明题写法重点：

不要只说「显然在顶点」。要写出一条边上的参数化，说明内部无临界点，再用题目给出的 critical point 定义和极值定理，把结论推广到三条边。

## IMG_0812：无穷积分比较判别

题目考虑 improper integral：
\[
\int_5^\infty \frac{\sin x+8}{x-4}\,dx.
\]

### 正确的比较思路

因为
\[
-1\le \sin x\le1,
\]
所以
\[
7\le \sin x+8\le9.
\]

当 \(x\ge5\) 时，\(x-4>0\)，所以
\[
\frac{\sin x+8}{x-4}\ge \frac7{x-4}.
\]

为了用更简单的 \(p\)-积分比较，也可以注意到当 \(x\ge5\) 时
\[
x>x-4,
\]
所以
\[
\frac7x\le\frac7{x-4}\le \frac{\sin x+8}{x-4}.
\]

因此可以选择
\[
g(x)=\frac7x.
\]

也就是题目中的
\[
g(x)=\frac{a}{x^p}
\]
满足
\[
a=7,\qquad p=1.
\]

### 结论

因为
\[
\int_5^\infty \frac7x\,dx
\]
发散，而
\[
0\le \frac7x\le \frac{\sin x+8}{x-4}
\]
对足够大的 \(x\) 成立，所以由比较判别法，
\[
\int_5^\infty \frac{\sin x+8}{x-4}\,dx
\]
发散。

知识点：

比较判别法有两个常用方向：

- 若 \(0\le f(x)\le g(x)\)，且 \(\int g(x)\,dx\) 收敛，则 \(\int f(x)\,dx\) 收敛。
- 若 \(0\le g(x)\le f(x)\)，且 \(\int g(x)\,dx\) 发散，则 \(\int f(x)\,dx\) 发散。

本题要证明发散，所以要找一个「更小但已经发散」的函数 \(g(x)\)。这就是为什么选
\[
0\le g(x)\le \frac{\sin x+8}{x-4}
\]
而不是选一个更大的函数。

## IMG_0813 / IMG_0814：导数、单调区间、反函数

题目给出
\[
f(x)=e^{(10x+8)^2}+(10x+8)^2.
\]

为了简化，令
\[
u=10x+8.
\]
那么
\[
f(x)=e^{u^2}+u^2.
\]

### (a) 求导并写成指定形式

由链式法则：
\[
\frac{d}{dx}e^{(10x+8)^2}
=e^{(10x+8)^2}\cdot 2(10x+8)\cdot10.
\]

同时
\[
\frac{d}{dx}(10x+8)^2=2(10x+8)\cdot10.
\]

所以
\[
f'(x)=20(10x+8)e^{(10x+8)^2}+20(10x+8).
\]

提取公因式：
\[
f'(x)=20(10x+8)\left(e^{(10x+8)^2}+1\right).
\]

也就是
\[
f'(x)=(200x+160)\left(e^{(10x+8)^2}+1\right).
\]

因此
\[
g(x)=200x+160.
\]

知识点：

这题的结构是「外函数 \(e^{(\cdot)}\) + 平方函数」，核心是链式法则：
\[
\frac{d}{dx}e^{h(x)}=e^{h(x)}h'(x).
\]

### (b) 求 \(J=\left(-\frac{21}{10},-\frac1{10}\right)\) 上的值域

当
\[
x\in\left(-\frac{21}{10},-\frac1{10}\right)
\]
时，
\[
u=10x+8.
\]

代入端点：
\[
x\to-\frac{21}{10}\quad\Rightarrow\quad u\to -13,
\]
\[
x\to-\frac1{10}\quad\Rightarrow\quad u\to7.
\]

所以
\[
u\in(-13,7).
\]

函数变成
\[
e^{u^2}+u^2.
\]

因为它只依赖 \(u^2\)，最小值发生在
\[
u=0.
\]
而 \(0\in(-13,7)\)，所以最小值是
\[
e^0+0=1.
\]

最大方向看两端。由于区间是开区间，\(u=-13\) 取不到，但可以无限接近，所以函数值可以无限接近
\[
e^{169}+169.
\]
而 \(u=7\) 方向只接近
\[
e^{49}+49,
\]
比 \(e^{169}+169\) 小。

因此值域是
\[
\left[1,e^{169}+169\right).
\]

注意左端是方括号，因为 \(u=0\) 能取到；右端是圆括号，因为 \(u=-13\) 不能取到。

### (c) 含有 \(\frac15\) 的最大可逆区间

由 (a)：
\[
f'(x)=(200x+160)\left(e^{(10x+8)^2}+1\right).
\]

其中
\[
e^{(10x+8)^2}+1>0
\]
恒成立，所以导数符号只由
\[
200x+160=200\left(x+\frac45\right)
\]
决定。

临界点是
\[
x=-\frac45.
\]

当 \(x>-\frac45\) 时，\(f'(x)>0\)，所以 \(f\) 严格递增。因为
\[
\frac15>-\frac45,
\]
所以含有 \(\frac15\) 的最大单调区间是
\[
\left[-\frac45,\infty\right).
\]

在这个区间上 \(f\) 一一对应，因此有反函数。

知识点：

函数有反函数的常见充分条件：在某个区间上严格单调。这里通过导数符号判断单调性。

### (d) 解 \(f(c)=e+1\)，取较大的解

要求
\[
e^{(10c+8)^2}+(10c+8)^2=e+1.
\]

令
\[
u=10c+8.
\]
则
\[
e^{u^2}+u^2=e+1.
\]

显然
\[
u^2=1
\]
满足，因为
\[
e^1+1=e+1.
\]

所以
\[
u=\pm1.
\]

即
\[
10c+8=1
\quad\text{或}\quad
10c+8=-1.
\]

得到
\[
c=-\frac7{10}
\quad\text{或}\quad
c=-\frac9{10}.
\]

较大的解是
\[
c_u=-\frac7{10}.
\]

### (e) 求反函数导数 \(h'(e+1)\)

反函数求导公式：
\[
h'(y)=\frac1{f'(h(y))}.
\]

这里 \(h\) 是 \(f\) 在包含 \(c_u\) 的区间上的反函数，并且
\[
f(c_u)=e+1,
\]
所以
\[
h(e+1)=c_u=-\frac7{10}.
\]

因此
\[
h'(e+1)=\frac1{f'\left(-\frac7{10}\right)}.
\]

由 (a)：
\[
f'(x)=(200x+160)\left(e^{(10x+8)^2}+1\right).
\]

代入 \(x=-\frac7{10}\)：
\[
200x+160=200\left(-\frac7{10}\right)+160=-140+160=20.
\]

并且
\[
10x+8=10\left(-\frac7{10}\right)+8=-7+8=1.
\]

所以
\[
f'\left(-\frac7{10}\right)
=20(e^1+1)
=20(e+1).
\]

因此
\[
h'(e+1)=\frac1{20(e+1)}.
\]

知识点总结：

1. 求反函数导数，不需要先把反函数显式求出来。
2. 只要知道 \(f(c)=y\)，就有
   \[
   (f^{-1})'(y)=\frac1{f'(c)}.
   \]
3. 本题关键是先从 \(f(c)=e+1\) 找到对应的 \(c\)，再代入 \(f'(c)\)。

## IMG_0815 / IMG_0816：参数曲线、奇偶性、单射和切线

题目给出
\[
x(t)=7t-\gamma\tanh(3t),
\qquad
y(t)=7\operatorname{sech}^2(5t),
\qquad t\in\mathbb R.
\]

其中 \(\gamma\) 是实数。

### (a) 求 \(x\) 和 \(y\) 的值域

答案：
\[
\operatorname{Range}(x)=\mathbb R,
\]
\[
\operatorname{Range}(y)=(0,7].
\]

解释：

因为
\[
\tanh(3t)\in(-1,1),
\]
而主导项 \(7t\) 会随着 \(t\to\infty\) 和 \(t\to-\infty\) 分别趋向正无穷和负无穷，所以 \(x(t)\) 的值域是整个实数集：
\[
\mathbb R.
\]

对于
\[
y(t)=7\operatorname{sech}^2(5t),
\]
要知道
\[
\operatorname{sech}(u)=\frac1{\cosh u}.
\]
因为
\[
\cosh u\ge1,
\]
所以
\[
0<\operatorname{sech}^2(u)\le1.
\]

当 \(t=0\) 时，
\[
\operatorname{sech}^2(0)=1,
\]
所以
\[
y(0)=7.
\]

当 \(t\to\pm\infty\) 时，
\[
\operatorname{sech}^2(5t)\to0,
\]
但永远不会真正等于 \(0\)。所以
\[
\operatorname{Range}(y)=(0,7].
\]

知识点：

\(\tanh\) 的值域是 \((-1,1)\)，\(\operatorname{sech}^2\) 的值域是 \((0,1]\)。这两条在双曲函数题里很常用。

### (b) 判断奇偶性

答案：

\[
x(t)\text{ 是奇函数},\qquad y(t)\text{ 是偶函数}.
\]

解释：

\(\tanh t\) 是奇函数，所以
\[
\tanh(-3t)=-\tanh(3t).
\]

因此
\[
x(-t)=7(-t)-\gamma\tanh(-3t)
=-7t+\gamma\tanh(3t)
=-\bigl(7t-\gamma\tanh(3t)\bigr)
=-x(t).
\]

所以 \(x\) 是奇函数。

\(\cosh t\) 是偶函数，所以 \(\operatorname{sech}t=1/\cosh t\) 也是偶函数，平方后仍然是偶函数：
\[
y(-t)=7\operatorname{sech}^2(-5t)
=7\operatorname{sech}^2(5t)
=y(t).
\]

所以 \(y\) 是偶函数。

### (c) \(x\) 什么时候 one-to-one？

答案：
\[
\gamma\in(-\infty,7/3].
\]

理由：

如果 \(x(t)\) 在整个实数轴上单调，那么它就是一一对应。求导：
\[
x'(t)=7-\gamma\cdot 3\operatorname{sech}^2(3t).
\]

即
\[
x'(t)=7-3\gamma\operatorname{sech}^2(3t).
\]

因为
\[
0<\operatorname{sech}^2(3t)\le1.
\]

若 \(\gamma\le0\)，则
\[
-3\gamma\operatorname{sech}^2(3t)\ge0,
\]
所以
\[
x'(t)>0.
\]
此时 \(x\) 严格递增。

若 \(\gamma>0\)，要保证 \(x'(t)\ge0\) 对所有 \(t\) 成立，最危险的位置是
\[
\operatorname{sech}^2(3t)=1,
\]
也就是 \(t=0\)。因此需要
\[
7-3\gamma\ge0.
\]

所以
\[
\gamma\le\frac73.
\]

合并得到
\[
\gamma\in(-\infty,7/3].
\]

知识点：

判断函数是否一一对应，常用方法是证明它严格单调。这里通过导数符号判断单调性。

### (d) 参数曲线什么时候可以看成 \(y=h(x)\) 的图像？

答案：

\[
\gamma\in C=(-\infty,7/3].
\]

原因：

参数曲线能写成
\[
y=h(x)
\]
的图像，本质上要求同一个 \(x\) 值不能对应两个不同的 \(y\) 值。最直接的充分条件是 \(x(t)\) 对 \(t\) 一一对应。

由 (c)，当
\[
\gamma\in(-\infty,7/3]
\]
时，\(x(t)\) 一一对应，所以每个 \(x\) 对应唯一的参数 \(t\)，进而对应唯一的 \(y(t)\)。因此曲线可以看成某个函数 \(y=h(x)\) 的图像。

### (e) 参数曲线什么时候是一个 invertible function 的图像？

答案：
\[
\varnothing.
\]

解释：

即使曲线可以写成 \(y=h(x)\)，这个 \(h\) 也不可能是一一对应的。

原因是：

- \(x(t)\) 是奇函数；
- \(y(t)\) 是偶函数。

所以
\[
x(-t)=-x(t),
\]
但
\[
y(-t)=y(t).
\]

这意味着图像关于 \(y\)-轴对称。除了 \(t=0\) 之外，通常会有两个不同的 \(x\) 值：
\[
x(t)\quad\text{和}\quad -x(t)
\]
对应同一个 \(y\) 值。

因此 \(h\) 不能通过水平线测试，也就不是一一对应函数。所以没有任何 \(\gamma\) 可以让这条曲线成为可逆函数的图像。

### (f) 什么时候没有水平切线？

答案：
\[
\gamma=\frac73.
\]

解释：

参数曲线的斜率是
\[
\frac{dy}{dx}=\frac{dy/dt}{dx/dt}.
\]

水平切线通常发生在
\[
\frac{dy}{dt}=0
\quad\text{且}\quad
\frac{dx}{dt}\ne0
\]
的时候。

先求
\[
y(t)=7\operatorname{sech}^2(5t).
\]
它在 \(t=0\) 处达到最大值 \(7\)，所以
\[
y'(0)=0.
\]

更具体地说，\(y'(t)=0\) 的唯一点是
\[
t=0.
\]

再看
\[
x'(t)=7-3\gamma\operatorname{sech}^2(3t).
\]

代入 \(t=0\)，因为 \(\operatorname{sech}^2(0)=1\)，所以
\[
x'(0)=7-3\gamma.
\]

如果
\[
\gamma\ne\frac73,
\]
那么
\[
x'(0)\ne0,
\]
于是 \(t=0\) 给出一条水平切线。

要让曲线没有水平切线，就必须让
\[
x'(0)=0.
\]
所以
\[
7-3\gamma=0,
\]
得到
\[
\gamma=\frac73.
\]

知识点：

参数曲线判断切线类型：

- 水平切线：\(dy/dt=0\)，且 \(dx/dt\ne0\)；
- 垂直切线：\(dx/dt=0\)，且 \(dy/dt\ne0\)；
- 如果两者都为 \(0\)，需要进一步看高阶行为，不能直接说水平。

本题在 \(\gamma=7/3\) 时，\(t=0\) 处 \(dx/dt\) 和 \(dy/dt\) 都为 \(0\)，因此它不是普通水平切线。

## IMG_0817：换元积分

题目要求给出换元
\[
u=g(x)
\]
使第一个积分变成第二个积分。

### (a)

\[
\int \frac1{x-1}\,dx
\]
要变成
\[
\int \frac1u\,du.
\]

答案：
\[
u=x-1.
\]

因为
\[
du=dx.
\]
所以
\[
\frac1{x-1}\,dx=\frac1u\,du.
\]

### (b)

\[
\int \sin x\tan x\,dx
\]
要变成
\[
\int \frac{u^2}{1-u^2}\,du.
\]

答案：
\[
u=\sin x.
\]

解释：

先把 integrand 改写：
\[
\sin x\tan x
=\sin x\cdot\frac{\sin x}{\cos x}
=\frac{\sin^2 x}{\cos x}.
\]

令
\[
u=\sin x,
\]
则
\[
du=\cos x\,dx.
\]

所以
\[
dx=\frac{du}{\cos x}.
\]

于是
\[
\sin x\tan x\,dx
=\frac{\sin^2 x}{\cos x}\cdot \frac{du}{\cos x}
=\frac{\sin^2 x}{\cos^2 x}\,du.
\]

因为
\[
\sin x=u,\qquad \cos^2x=1-\sin^2x=1-u^2,
\]
所以
\[
\frac{\sin^2 x}{\cos^2 x}\,du
=\frac{u^2}{1-u^2}\,du.
\]

### (c)

\[
\int 18x^{20}\,dx
\]
要变成
\[
\int 6u^6\,du.
\]

答案：
\[
u=x^3.
\]

因为
\[
du=3x^2\,dx.
\]

同时
\[
u^6=(x^3)^6=x^{18}.
\]

所以
\[
6u^6\,du
=6x^{18}\cdot 3x^2\,dx
=18x^{20}\,dx.
\]

知识点：

换元不是乱猜，而是看目标积分里出现的 \(u\)、\(du\) 和原积分之间的结构：

- 分母 \(x-1\) 直接令 \(u=x-1\)；
- 出现 \(1-u^2\) 常常想到 \(u=\sin x\) 或 \(u=\cos x\)；
- 幂次从 \(x^{20}\) 变成 \(u^6\)，可以试 \(u=x^3\)，因为 \(x^{18}\) 加上 \(du=3x^2dx\) 正好凑成 \(x^{20}dx\)。

## IMG_0818：行操作对行列式的影响

题目考虑 \(n\times n\) 矩阵 \(A,B,H,P\)。

### (a) 交换两行

\(B\) 由 \(A\) 交换 row 1 和 row 6 得到。

答案：
\[
\det(B)=-\det(A).
\]

等价地，
\[
\det(A)=-\det(B).
\]

知识点：

交换两行，行列式变号。

### (b) 一行加上另一行的倍数

\(H\) 由 \(A\) 把 \(2\) 倍 row 1 加到 row 3 得到。

答案：
\[
\det(H)=\det(A).
\]

知识点：

把某一行加上另一行的倍数，不改变行列式。

### (c) 某一行乘以常数

\(P\) 由 \(A\) 把 row 5 乘以 \(2\) 得到。

答案：
\[
\det(P)=2\det(A).
\]

知识点：

把某一行乘以 \(k\)，行列式也乘以 \(k\)。

行列式行操作总结：

1. 交换两行：\(\det\) 乘以 \(-1\)；
2. 某行乘以 \(k\)：\(\det\) 乘以 \(k\)；
3. 某行加上另一行的倍数：\(\det\) 不变。

## IMG_0819：矩阵关系化简

题目给出 \(2\times2\) 矩阵 \(A,B\)，满足
\[
A^2=-3A-8I,
\qquad
B^2=-B+9I,
\]
并定义
\[
C=AB.
\]

还给出
\[
BA=-A-3B-C-7I,
\]
\[
ABA=-4A+8B+8I.
\]

### (a) 求 \(A^3\) 和 \(A^{-1}\)

由
\[
A^2=-3A-8I.
\]

两边左乘 \(A\)：
\[
A^3=A(-3A-8I)
=-3A^2-8A.
\]

代入 \(A^2=-3A-8I\)：
\[
A^3=-3(-3A-8I)-8A
=9A+24I-8A
=A+24I.
\]

所以
\[
A^3=1\cdot A+24I.
\]

因此
\[
\alpha=1,\qquad \beta=24.
\]

求 \(A^{-1}\)：

从
\[
A^2=-3A-8I
\]
移项：
\[
A^2+3A+8I=0.
\]

提取左边的 \(A\)：
\[
A(A+3I)=-8I.
\]

所以
\[
A\left(-\frac18(A+3I)\right)=I.
\]

因此
\[
A^{-1}=-\frac18A-\frac38I.
\]

所以
\[
\gamma=-\frac18,\qquad \delta=-\frac38.
\]

### (b) 求 \(AC\) 和 \(CB\)

因为
\[
C=AB.
\]

所以
\[
AC=A(AB)=A^2B.
\]

代入 \(A^2=-3A-8I\)：
\[
AC=(-3A-8I)B
=-3AB-8B
=-3C-8B.
\]

因此
\[
AC=-3C-8B.
\]

再看
\[
CB=(AB)B=AB^2.
\]

代入 \(B^2=-B+9I\)：
\[
CB=A(-B+9I)
=-AB+9A
=-C+9A.
\]

因此
\[
CB=-C+9A.
\]

### (c) 求 \(C^2=aC+bI\)

由
\[
C^2=(AB)(AB)=A(BA)B.
\]

代入
\[
BA=-A-3B-C-7I.
\]

得到
\[
C^2=A(-A-3B-C-7I)B.
\]

展开：
\[
C^2=-A^2B-3AB^2-ACB-7AB.
\]

逐项化简：

\[
A^2B=AC=-3C-8B,
\]
所以
\[
-A^2B=3C+8B.
\]

\[
AB^2=CB=-C+9A,
\]
所以
\[
-3AB^2=3C-27A.
\]

又因为
\[
AC=-3C-8B,
\]
所以
\[
ACB=(-3C-8B)B=-3CB-8B^2.
\]

代入
\[
CB=-C+9A,\qquad B^2=-B+9I,
\]
得到
\[
ACB=-3(-C+9A)-8(-B+9I)
=3C-27A+8B-72I.
\]

所以
\[
-ACB=-3C+27A-8B+72I.
\]

最后
\[
-7AB=-7C.
\]

合并：
\[
C^2=(3C+8B)+(3C-27A)+(-3C+27A-8B+72I)-7C.
\]

\(A\) 和 \(B\) 项抵消，剩下
\[
C^2=-4C+72I.
\]

所以
\[
a=-4,\qquad b=72.
\]

知识点：

矩阵不一定可交换，所以 \(AB\) 和 \(BA\) 不能随便交换。本题所有化简都要严格保持乘法顺序。看到 \(C=AB\)，就把 \(AC\)、\(CB\)、\(C^2\) 变成含 \(AB\)、\(A^2\)、\(B^2\)、\(BA\) 的表达，再用题目给出的关系替换。

### (d) 判断 \(X\) 和 \(Y\) 哪个可逆

题目给出：
\[
X=-2A+B+C+4I,
\]
\[
Y=A+4B+C-2I.
\]

并给出化简结果：
\[
X(pX+qI)
=A(-18p-2q)+B(q+9p)+C(q+9p)+I(4q+36p),
\]
\[
Y(pY+qI)
=A(q-15p)+B(4q-60p)+C(q-15p)+I(-2q+84p).
\]

定理说：若能找到 \(p,q\)，使
\[
M(pM+qI)=I,
\]
那么 \(M\) 可逆，并且
\[
M^{-1}=pM+qI.
\]

#### 先看 \(X\)

要让
\[
X(pX+qI)=I,
\]
必须让 \(A,B,C\) 的系数都是 \(0\)，而 \(I\) 的系数是 \(1\)。

所以需要
\[
-18p-2q=0,
\]
\[
q+9p=0,
\]
\[
4q+36p=1.
\]

前两个条件都给出
\[
q=-9p.
\]

但代入 \(I\) 的系数：
\[
4q+36p=4(-9p)+36p=0.
\]

不可能等于 \(1\)。所以 \(X\) 不可逆。

#### 再看 \(Y\)

要让
\[
Y(pY+qI)=I,
\]
需要
\[
q-15p=0,
\]
\[
4q-60p=0,
\]
\[
-2q+84p=1.
\]

前两个条件等价于
\[
q=15p.
\]

代入 \(I\) 的系数：
\[
-2(15p)+84p=1.
\]

即
\[
54p=1,
\]
所以
\[
p=\frac1{54}.
\]

于是
\[
q=15p=\frac{15}{54}=\frac5{18}.
\]

因此可逆的是
\[
Y.
\]

并且
\[
Y^{-1}=pY+qI
=\frac1{54}Y+\frac5{18}I.
\]

题目若只要 \(p,q\)，答案是
\[
p=\frac1{54},\qquad q=\frac{15}{54}.
\]

知识点：

这类题其实是在用「待定系数法」找逆矩阵。因为题目已经把 \(M(pM+qI)\) 展开成了
\[
A(\cdots)+B(\cdots)+C(\cdots)+I(\cdots),
\]
要等于 \(I\)，就必须：

- \(A,B,C\) 的系数全为 \(0\)；
- \(I\) 的系数为 \(1\)。

然后解关于 \(p,q\) 的方程即可。

---

### IMG_0821 / IMG_0822

题目给出两个平面：

\[
\Pi_1:\mathbf x=
\begin{pmatrix}-2\\2\\-1\end{pmatrix}
+\lambda_1
\begin{pmatrix}2\\-2\\-2\end{pmatrix}
+\lambda_2
\begin{pmatrix}2\\-1\\2\end{pmatrix},
\qquad \lambda_1,\lambda_2\in\mathbb R,
\]

\[
\Pi_2:6x+8y-2z=-2.
\]

#### (a) 说明怎样检查两个平面平行

对 \(\Pi_1\)，它是参数式平面，所以有两个方向向量：

\[
\mathbf v_1=(2,-2,-2),\qquad \mathbf v_2=(2,-1,2).
\]

平面的法向量可以用叉积求：

\[
\mathbf n_1=\mathbf v_1\times \mathbf v_2.
\]

计算得

\[
\mathbf v_1\times \mathbf v_2=(-6,-8,2).
\]

也可以写成

\[
(6,8,-2),
\]

因为法向量乘以非零常数仍然是同一个方向。

对 \(\Pi_2\)，它是标准式

\[
6x+8y-2z=-2,
\]

所以法向量直接是

\[
\mathbf n_2=(6,8,-2).
\]

因此两个平面的法向量平行，甚至可以取成相同：

\[
\mathbf n_1=(6,8,-2),\qquad \mathbf n_2=(6,8,-2).
\]

所以两个平面平行。

可以写进论述框的文字：

先从 \(\Pi_1\) 的两个方向向量取叉积，得到 \(\Pi_1\) 的一个法向量。再从 \(\Pi_2\) 的方程 \(6x+8y-2z=-2\) 中读出它的法向量 \((6,8,-2)\)。如果两个法向量互为非零倍数，那么两个平面的垂直方向相同，因此两个平面平行。本题中 \(\Pi_1\) 的法向量可以取为 \((6,8,-2)\)，与 \(\Pi_2\) 的法向量相同，所以它们平行。

知识点：

参数式平面

\[
\mathbf x=\mathbf a+s\mathbf u+t\mathbf v
\]

的两个方向向量是 \(\mathbf u,\mathbf v\)，法向量是

\[
\mathbf u\times \mathbf v.
\]

标准式平面

\[
ax+by+cz=d
\]

的法向量是

\[
(a,b,c).
\]

两个平面平行的常用判据是：两个法向量平行。

#### (b) 求 \(z_0\)

点

\[
B(1,1,z_0)
\]

在 \(\Pi_2\) 上，所以代入

\[
6x+8y-2z=-2.
\]

得到

\[
6(1)+8(1)-2z_0=-2.
\]

即

\[
14-2z_0=-2.
\]

所以

\[
-2z_0=-16,
\]

\[
z_0=8.
\]

答案：

\[
\boxed{z_0=8}.
\]

知识点：

判断一个点是否在平面上，或者求点中未知坐标，直接把点的坐标代入平面方程即可。

#### (c) 求两个平面的最短距离

因为 \(\Pi_1\) 与 \(\Pi_2\) 平行，所以从 \(\Pi_1\) 上任意一点到 \(\Pi_2\) 的垂直距离都相同。取 \(\Pi_1\) 参数式中的基点

\[
A=(-2,2,-1).
\]

把 \(\Pi_2\) 写成

\[
6x+8y-2z+2=0.
\]

点到平面距离公式是

\[
d=\frac{|ax_0+by_0+cz_0+d|}{\sqrt{a^2+b^2+c^2}}.
\]

于是

\[
d=
\frac{|6(-2)+8(2)-2(-1)+2|}
{\sqrt{6^2+8^2+(-2)^2}}.
\]

分子为

\[
-12+16+2+2=8.
\]

分母为

\[
\sqrt{36+64+4}=\sqrt{104}=2\sqrt{26}.
\]

所以

\[
d=\frac{8}{2\sqrt{26}}
=\frac4{\sqrt{26}}
=\frac{2\sqrt{26}}{13}.
\]

答案：

\[
\boxed{\operatorname{dist}(\Pi_1,\Pi_2)=\frac4{\sqrt{26}}}.
\]

用 Maple 语法可写：

```text
4/sqrt(26)
```

或者

```text
2*sqrt(26)/13
```

可以写进论述框的文字：

因为两个平面平行，所以它们之间的最短距离就是从其中一个平面上任意一点到另一个平面的垂直距离；不管在 \(\Pi_1\) 上选哪个点，垂直方向的距离都相同。可以选 \(\Pi_1\) 参数式中的基点 \((-2,2,-1)\)。再取 \(\Pi_2\) 的法向量 \((6,8,-2)\)。从所选点出发，沿着法向量方向投影到 \(\Pi_2\)，这个投影长度就是点到平面的垂直距离，也就是两个平面之间的最短距离。

知识点：

两平行平面的距离，本质上是「点到平面距离」。做法是：

1. 先确认两个平面平行；
2. 在其中一个平面上任选一点；
3. 用点到另一个平面的距离公式；
4. 这个距离就是两个平面的距离。

---

### IMG_0823 / IMG_0824 / IMG_0825

题目给出极坐标曲线

\[
r=f(\theta),
\]

其中

\[
f(\theta)=7+\frac1{2+\sin(\theta-\frac{\pi}{3})}
-6\sin\left(\theta-\frac{\pi}{3}\right),
\qquad 0\le \theta\le 2\pi.
\]

#### (a) 离原点最远和最近的点

极坐标中，点到原点的距离就是

\[
|r|.
\]

本题图像的 \(r\) 为正，所以离原点最远就是让 \(r=f(\theta)\) 最大，离原点最近就是让 \(r=f(\theta)\) 最小。

令

\[
u=\theta-\frac{\pi}{3},
\qquad s=\sin u.
\]

因为

\[
-1\le s\le 1,
\]

函数可以看成

\[
r=7+\frac1{2+s}-6s.
\]

把它看作关于 \(s\) 的函数：

\[
R(s)=7+\frac1{2+s}-6s.
\]

求导：

\[
R'(s)=-\frac1{(2+s)^2}-6.
\]

由于

\[
-\frac1{(2+s)^2}-6<0,
\]

所以 \(R(s)\) 随 \(s\) 增大而减小。

因此：

- 最大值在 \(s=-1\) 处；
- 最小值在 \(s=1\) 处。

##### 最远点

\[
s=-1
\]

即

\[
\sin\left(\theta-\frac{\pi}{3}\right)=-1.
\]

所以

\[
\theta-\frac{\pi}{3}=\frac{3\pi}{2}.
\]

得到

\[
\theta=\frac{11\pi}{6}.
\]

此时

\[
r=7+\frac1{2-1}-6(-1)=7+1+6=14.
\]

极坐标转直角坐标：

\[
x=r\cos\theta,\qquad y=r\sin\theta.
\]

所以

\[
x=14\cos\frac{11\pi}{6}=14\cdot\frac{\sqrt3}{2}=7\sqrt3,
\]

\[
y=14\sin\frac{11\pi}{6}=14\cdot\left(-\frac12\right)=-7.
\]

最远点为

\[
\boxed{(7\sqrt3,-7)}.
\]

所以

\[
\boxed{a=7\sqrt3,\qquad b=-7}.
\]

Maple 语法：

```text
a = 7*sqrt(3)
b = -7
```

##### 最近点

\[
s=1
\]

即

\[
\sin\left(\theta-\frac{\pi}{3}\right)=1.
\]

所以

\[
\theta-\frac{\pi}{3}=\frac{\pi}{2}.
\]

得到

\[
\theta=\frac{5\pi}{6}.
\]

此时

\[
r=7+\frac1{2+1}-6(1)
=7+\frac13-6
=\frac43.
\]

转成直角坐标：

\[
x=\frac43\cos\frac{5\pi}{6}
=\frac43\left(-\frac{\sqrt3}{2}\right)
=-\frac{2\sqrt3}{3},
\]

\[
y=\frac43\sin\frac{5\pi}{6}
=\frac43\cdot\frac12
=\frac23.
\]

最近点为

\[
\boxed{\left(-\frac{2\sqrt3}{3},\frac23\right)}.
\]

所以

\[
\boxed{c=-\frac{2\sqrt3}{3},\qquad d=\frac23}.
\]

Maple 语法：

```text
c = -2*sqrt(3)/3
d = 2/3
```

题中填空里的定理解释：

要找全局最大值和最小值，需要研究

\[
r \text{ as a function of } \theta.
\]

最大最小值定理保证它们存在，因为 \(f\) 是

```text
continuous
```

而区间 \([0,2\pi]\) 是

```text
closed
```

and bounded。

知识点：

极坐标中：

\[
x=r\cos\theta,\qquad y=r\sin\theta.
\]

离原点的距离是 \(|r|\)。如果 \(r\ge0\)，那么离原点最远/最近就是找 \(r\) 的最大/最小值。

这里的关键技巧是：函数只通过

\[
\sin\left(\theta-\frac{\pi}{3}\right)
\]

出现，所以可以令

\[
s=\sin\left(\theta-\frac{\pi}{3}\right),
\]

把问题变成单变量 \(s\in[-1,1]\) 上的最大最小值问题。

#### (b) 新曲线 \(g(\theta)=\dfrac7{2+\cos\theta}\)

题目给出

\[
g(\theta)=\frac7{2+\cos\theta},
\qquad 0\le \theta\le 2\pi.
\]

要找第二象限中离 \(x\)-axis 最远的点。

离 \(x\) 轴的距离是

\[
|y|.
\]

第二象限中 \(y>0\)，所以只需要最大化

\[
y.
\]

极坐标中

\[
y=r\sin\theta.
\]

这里

\[
r=\frac7{2+\cos\theta}.
\]

所以

\[
y(\theta)=\frac{7\sin\theta}{2+\cos\theta}.
\]

在第二象限：

\[
\frac{\pi}{2}\le \theta\le \pi.
\]

求导：

\[
y'(\theta)
=
\frac{7\cos\theta(2+\cos\theta)-7\sin\theta(-\sin\theta)}
{(2+\cos\theta)^2}.
\]

整理分子：

\[
7\left(2\cos\theta+\cos^2\theta+\sin^2\theta\right)
=7(2\cos\theta+1).
\]

所以

\[
y'(\theta)=\frac{7(2\cos\theta+1)}{(2+\cos\theta)^2}.
\]

令

\[
y'(\theta)=0,
\]

得到

\[
2\cos\theta+1=0.
\]

即

\[
\cos\theta=-\frac12.
\]

第二象限中对应

\[
\theta=\frac{2\pi}{3}.
\]

此时

\[
r=\frac7{2+\cos\frac{2\pi}{3}}
=\frac7{2-\frac12}
=\frac7{\frac32}
=\frac{14}{3}.
\]

转成直角坐标：

\[
x=r\cos\theta
=\frac{14}{3}\left(-\frac12\right)
=-\frac73,
\]

\[
y=r\sin\theta
=\frac{14}{3}\cdot\frac{\sqrt3}{2}
=\frac{7\sqrt3}{3}.
\]

所以

\[
\boxed{(p,q)=\left(-\frac73,\frac{7\sqrt3}{3}\right)}.
\]

即

\[
\boxed{p=-\frac73,\qquad q=\frac{7\sqrt3}{3}}.
\]

Maple 语法：

```text
p = -7/3
q = 7*sqrt(3)/3
```

#### (c) 怎样得到 (b) 的答案

可以写进论述框：

The distance from a point to the \(x\)-axis is \(|y|\). In the second quadrant \(y>0\), so we need to maximise the \(y\)-coordinate. For a polar curve, \(y=r\sin\theta\). Since \(r=g(\theta)=7/(2+\cos\theta)\), we maximise \(y(\theta)=7\sin\theta/(2+\cos\theta)\) on the second quadrant interval \(\pi/2\le\theta\le\pi\). Differentiating gives a critical point when \(2\cos\theta+1=0\), so \(\theta=2\pi/3\). Substituting this value into \(r=g(\theta)\) gives \(r=14/3\), and converting from polar to Cartesian coordinates gives \((-7/3,7\sqrt3/3)\).

中文理解：

要离 \(x\) 轴最远，不是最大化 \(r\)，而是最大化 \(y\)。这是极坐标题很容易错的地方。先写

\[
y=r\sin\theta,
\]

再把题目给的 \(r=g(\theta)\) 代进去，最后对 \(\theta\) 求最大值。

---

### IMG_0826

题目给出

\[
A=
\begin{pmatrix}
34&27&26&21\\
40&23&33&32\\
25&48&22&47\\
30&36&20&41
\end{pmatrix},
\]

并且

\[
\det(A)=-96549.
\]

#### (a) 沿第 2 行展开

沿第 2 行展开时，用到第 2 行的元素：

\[
40,\quad 23,\quad 33,\quad 32.
\]

余子式符号由

\[
(-1)^{i+j}
\]

决定。第 2 行的符号是

\[
-,\quad +,\quad -,\quad +.
\]

所以形式是

\[
\det(A)
=-40M_{21}+23M_{22}-33M_{23}+32M_{24}.
\]

题目把四个 \(3\times3\) 行列式命名为 \(a,b,c,d\)，但顺序不是 \(M_{21},M_{22},M_{23},M_{24}\) 这样排的，而是：

\[
a=M_{21},
\]

\[
b=M_{24},
\]

\[
c=M_{22},
\]

\[
d=M_{23}.
\]

因此

\[
\det(A)
=-40a+23c-33d+32b.
\]

按题目要求写成 \(a,b,c,d\) 的倍数和：

\[
\boxed{\det(A)=-40a+32b+23c-33d}.
\]

Maple 语法：

```text
-40*a+32*b+23*c-33*d
```

知识点：

沿第 \(i\) 行展开：

\[
\det(A)=\sum_j (-1)^{i+j}a_{ij}M_{ij}.
\]

其中 \(M_{ij}\) 是删去第 \(i\) 行、第 \(j\) 列后得到的行列式。

符号棋盘是：

\[
\begin{pmatrix}
+&-&+&-\\
-&+&-&+\\
+&-&+&-\\
-&+&-&+
\end{pmatrix}.
\]

第 2 行因此是

\[
-,+,-,+.
\]

#### (b) 沿第 1 列展开

沿第 1 列展开时，用到第 1 列的元素：

\[
34,\quad 40,\quad 25,\quad 30.
\]

第 1 列的符号是

\[
,\quad -,\quad +,\quad -.
\]

所以

\[
\det(A)
=34M_{11}-40M_{21}+25M_{31}-30M_{41}.
\]

题目给出的 \(w,x,y,z\) 对应关系是：

\[
w=M_{41},
\]

\[
x=M_{21},
\]

\[
y=M_{11},
\]

\[
z=M_{31}.
\]

所以

\[
\det(A)=34y-40x+25z-30w.
\]

答案：

\[
\boxed{\det(A)=34y-40x+25z-30w}.
\]

Maple 语法：

```text
34*y-40*x+25*z-30*w
```

知识点：

无论沿行展开还是沿列展开，步骤都一样：

1. 选定那一行或那一列；
2. 每个元素乘上它的余子式；
3. 再乘上符号 \((-1)^{i+j}\)；
4. 把所有项相加。

这类题特别容易错在「题目给的字母顺序」和「余子式顺序」不一致。不要按字母顺序猜，要逐个看每个 \(3\times3\) 行列式到底删去了哪一行哪一列。

---

### IMG_0827 / IMG_0828 / IMG_0829

题目把 \(I_3\) 拼到 \(A\) 右边，得到

\[
B=(A\mid I_3).
\]

经过初等行变换后得到

\[
M=
\left[
\begin{array}{ccc|ccc}
1&0&-m&0&0&-1\\
0&1&m+4&1&0&1\\
0&0&m(m+2)&0&1&m+2
\end{array}
\right].
\]

#### (a)(i) 当 \(m=1\) 时，把 \(P\) 化成 RREF

代入 \(m=1\)，得到

\[
P=
\left[
\begin{array}{ccc|ccc}
1&0&-1&0&0&-1\\
0&1&5&1&0&1\\
0&0&3&0&1&3
\end{array}
\right].
\]

目标是把左边变成 \(I_3\)。

先把第 3 行的主元 \(3\) 变成 \(1\)：

\[
R_3\leftarrow \frac13R_3.
\]

Maple：

```text
RowOperation(P,3,1/3);
```

然后消掉第 2 行第 3 列的 \(5\)：

\[
R_2\leftarrow R_2-5R_3.
\]

Maple：

```text
RowOperation(%,[2,3],-5);
```

最后消掉第 1 行第 3 列的 \(-1\)：

\[
R_1\leftarrow R_1+R_3.
\]

Maple：

```text
RowOperation(%,[1,3],1);
```

所以正确选项是：

```text
RowOperation(P,3,1/3);
RowOperation(%,[2,3],-5);
RowOperation(%,[1,3],1);
```

知识点：

Maple 中：

```text
RowOperation(M,i,c)
```

表示

\[
R_i\leftarrow cR_i.
\]

而

```text
RowOperation(M,[i,j],c)
```

表示

\[
R_i\leftarrow R_i+cR_j.
\]

这里 `%` 表示上一行 Maple 输出的结果。

#### (a)(ii) 求 \((A^T)^{-1}\)，其中 \(m=1\)

继续从 \(P\) 化简。

先做

\[
R_3\leftarrow \frac13R_3,
\]

得到第 3 行右边：

\[
(0,\frac13,1).
\]

再做

\[
R_2\leftarrow R_2-5R_3,
\]

第 2 行右边变成

\[
(1,-\frac53,-4).
\]

再做

\[
R_1\leftarrow R_1+R_3,
\]

第 1 行右边变成

\[
(0,\frac13,0).
\]

因此

\[
A^{-1}=
\begin{pmatrix}
0&\frac13&0\\
1&-\frac53&-4\\
0&\frac13&1
\end{pmatrix}.
\]

题目要的是

\[
(A^T)^{-1}.
\]

利用性质：

\[
(A^T)^{-1}=(A^{-1})^T.
\]

所以

\[
(A^T)^{-1}=
\begin{pmatrix}
0&1&0\\
\frac13&-\frac53&\frac13\\
0&-4&1
\end{pmatrix}.
\]

Maple 矩阵语法可以写成列向量形式：

```text
<<0,1/3,0>|<1,-5/3,-4>|<0,1/3,1>>
```

注意：这个 Maple 写法看起来像在输入 \(A^{-1}\) 的行，但它实际是按列拼矩阵。上面三个尖括号向量是三列，所以得到的正是

\[
\begin{pmatrix}
0&1&0\\
\frac13&-\frac53&\frac13\\
0&-4&1
\end{pmatrix}.
\]

知识点：

如果把 \((A\mid I)\) 行化简成 \((I\mid A^{-1})\)，右半边就是 \(A^{-1}\)。如果题目问 \((A^T)^{-1}\)，不要忘了转置：

\[
(A^T)^{-1}=(A^{-1})^T.
\]

#### (b)(i) 什么时候 \(A\mathbf x=\mathbf e_1\) inconsistent

在 \(M\) 中，左半边是

\[
\begin{pmatrix}
1&0&-m\\
0&1&m+4\\
0&0&m(m+2)
\end{pmatrix}.
\]

右半边第 1 列对应 \(\mathbf e_1\) 经过同样行变换后的结果：

\[
\begin{pmatrix}0\\1\\0\end{pmatrix}.
\]

所以最后一行对应方程是

\[
m(m+2)x_3=0.
\]

无论 \(m\) 是什么，这一行都不会变成

\[
0=\text{非零数}.
\]

因此系统永远不会 inconsistent。

答案：

```text
none
```

知识点：

线性方程组无解的典型信号是行化简后出现：

\[
0x+0y+0z=c,\qquad c\ne0.
\]

也就是

\[
0=c.
\]

本题对 \(\mathbf e_1\) 不会出现这种情况。

#### (b)(ii) 什么时候 \(A\mathbf x=\mathbf e_3\) consistent

右半边第 3 列对应 \(\mathbf e_3\) 经过同样行变换后的结果：

\[
\begin{pmatrix}-1\\1\\m+2\end{pmatrix}.
\]

最后一行对应方程：

\[
m(m+2)x_3=m+2.
\]

分情况讨论。

如果

\[
m\ne0,\quad m\ne -2,
\]

左边系数非零，所以一定能解。

如果

\[
m=-2,
\]

则

\[
m(m+2)=0,\qquad m+2=0,
\]

最后一行变成

\[
0=0,
\]

仍然 consistent。

如果

\[
m=0,
\]

则

\[
m(m+2)=0,\qquad m+2=2,
\]

最后一行变成

\[
0=2,
\]

不可能，所以 inconsistent。

因此 consistent 的所有 \(m\) 是除了 \(0\) 以外的所有实数：

```text
not 0
```

知识点：

有参数的方程组，最关键的是看「可能消失的主元」。这里可能出问题的是

\[
m(m+2).
\]

所以一定要检查

\[
m=0,\qquad m=-2
\]

这两个特殊值。

#### (c) 什么时候 \(AC\mathbf x=0\) 与 \(C\mathbf x=0\) 的解集不同

题目说 \(C\) 是可逆矩阵。

因为 \(C\) 可逆，所以

\[
C\mathbf x=0
\]

只有零解：

\[
\mathbf x=0.
\]

再看

\[
AC\mathbf x=0.
\]

令

\[
\mathbf y=C\mathbf x.
\]

由于 \(C\) 可逆，\(\mathbf x\) 与 \(\mathbf y\) 是一一对应的。所以 \(AC\mathbf x=0\) 的解集是否比零解更多，本质上取决于

\[
A\mathbf y=0
\]

是否有非零解。

\[
A\mathbf y=0
\]

有非零解，当且仅当 \(A\) 不可逆，也就是 \(A\) 奇异。

从 \(M\) 的左半边可以看出，\(A\) 的主元与

\[
1,\quad 1,\quad m(m+2)
\]

有关。所以 \(A\) 不可逆当且仅当

\[
m(m+2)=0.
\]

解得

\[
m=0\quad\text{or}\quad m=-2.
\]

答案：

```text
0,-2
```

知识点：

对于齐次方程

\[
A\mathbf x=0,
\]

如果 \(A\) 可逆，那么只有零解；如果 \(A\) 不可逆，就有非零解。

本题因为 \(C\) 可逆，乘上 \(C\) 不会改变“是否存在非零自由方向”的本质，真正决定差异的是 \(A\) 是否奇异。

---

### IMG_0830

#### (a) 让矩阵对称

对称矩阵的定义是

\[
A^T=A.
\]

也就是说，主对角线两边对应位置的元素必须相等。

题目给出

\[
A=
\begin{pmatrix}
1&a&y\\
x&5&-2\\
c&z&b
\end{pmatrix}.
\]

比较对称位置：

\[
A_{12}=A_{21}
\]

所以

\[
a=x.
\]

因此

\[
\boxed{x=a}.
\]

再比较

\[
A_{13}=A_{31}.
\]

所以

\[
y=c.
\]

因此

\[
\boxed{y=c}.
\]

再比较

\[
A_{23}=A_{32}.
\]

所以

\[
-2=z.
\]

因此

\[
\boxed{z=-2}.
\]

题目中 \(B\) 是

\[
B=
\begin{pmatrix}
3&-d\\
-d&d\\
s&3
\end{pmatrix}.
\]

它是 \(3\times2\) 矩阵，不是方阵。对称矩阵必须是方阵，所以没有任何 \(s\) 可以让 \(B\) 成为对称矩阵。

因此

\[
\boxed{s=\text{none}}.
\]

题目中

\[
C=
\begin{pmatrix}
pqt-(p+q+t)&p+19\\
t-8&q
\end{pmatrix}.
\]

这是 \(2\times2\) 矩阵。要对称，只需要非对角线元素相等：

\[
p+19=t-8.
\]

所以

\[
t=p+27.
\]

因此

\[
\boxed{t=p+27}.
\]

答案汇总：

```text
x = a
y = c
z = -2
s = none
t = p+27
```

知识点：

对称矩阵一定要满足两个条件：

1. 必须是方阵；
2. 关于主对角线对称的元素相等。

例如 \(3\times3\) 矩阵只需要检查：

\[
(1,2)\text{ 与 }(2,1),
\quad
(1,3)\text{ 与 }(3,1),
\quad
(2,3)\text{ 与 }(3,2).
\]

主对角线上的元素不用检查，因为它们和自己相等。

#### (b) 判断 \(M=\alpha K+\beta K^T+\gamma KK^T\) 何时对称

题目给出

\[
M=\alpha K+\beta K^T+\gamma KK^T.
\]

要 \(M\) 对称，需要

\[
M^T=M.
\]

先转置：

\[
M^T=(\alpha K+\beta K^T+\gamma KK^T)^T.
\]

利用转置性质：

\[
(A+B)^T=A^T+B^T,
\]

\[
(cA)^T=cA^T,
\]

\[
(AB)^T=B^TA^T.
\]

所以

\[
M^T=\alpha K^T+\beta K+\gamma (KK^T)^T.
\]

而

\[
(KK^T)^T=(K^T)^TK^T=KK^T.
\]

所以 \(KK^T\) 本身一定是对称矩阵。

因此真正需要比较的是

\[
\alpha K+\beta K^T
\]

和

\[
\alpha K^T+\beta K.
\]

如果

\[
\alpha=\beta,
\]

那么

\[
\alpha K+\beta K^T=\alpha K+\alpha K^T,
\]

这个部分转置后不变。

因此只要

\[
\boxed{\alpha=\beta,\quad \gamma\text{ 任意}},
\]

\[
M
\]

就是对称矩阵。

所以一定应选择：

```text
alpha = beta and gamma is any real number
```

截图底部还有一个选项：

```text
gamma can be any real number, alpha = -19*gamma^2 and beta = -19*gamma^2
```

这个也要选。原因是它强制

\[
\alpha=\beta=-19\gamma^2,
\]

所以它是 \(\alpha=\beta\) 的一个特殊情况，也一定能让 \(M\) 对称。

因此本题多选应选：

```text
alpha = beta and gamma is any real number
```

以及

```text
gamma can be any real number, alpha = -19*gamma^2 and beta = -19*gamma^2
```

知识点：

两个非常常用的事实：

\[
K+K^T
\]

一定是对称矩阵，因为

\[
(K+K^T)^T=K^T+K=K+K^T.
\]

以及

\[
KK^T
\]

一定是对称矩阵，因为

\[
(KK^T)^T=KK^T.
\]

所以本题中 \(\gamma KK^T\) 不会制造不对称；问题只在 \(\alpha K+\beta K^T\)。要它对称，就让两个系数相同，即 \(\alpha=\beta\)。

---

### IMG_0832

题目给出

\[
f:\mathbb R\to \mathbb R,
\qquad
f(x)=x^3+5x-3.
\]

#### (a) 解释为什么 \(f\) 有反函数

先求导：

\[
f'(x)=3x^2+5.
\]

因为

\[
3x^2\ge0,
\]

所以

\[
3x^2+5>0
\]

对所有实数 \(x\) 都成立。

因此 \(f\) 在整个 \(\mathbb R\) 上严格递增。严格递增函数一定是一对一的。

另外，由于最高次项是 \(x^3\)，所以

\[
\lim_{x\to\infty}f(x)=\infty,
\]

\[
\lim_{x\to-\infty}f(x)=-\infty.
\]

再加上 \(f\) 连续，所以它的值域是整个 \(\mathbb R\)。也就是说 \(f\) 从 \(\mathbb R\) 到 \(\mathbb R\) 是 onto。

所以 \(f\) 既是一对一又是满射，因此有反函数

\[
f^{-1}.
\]

可以写进 essay box 的文字：

The derivative is \(f'(x)=3x^2+5\), which is always positive. Hence \(f\) is strictly increasing on all of \(\mathbb R\), so it is one-to-one. Also, since \(f\) is a cubic with positive leading term, \(f(x)\to\infty\) as \(x\to\infty\) and \(f(x)\to-\infty\) as \(x\to-\infty\). Because \(f\) is continuous, its range is all of \(\mathbb R\). Therefore \(f:\mathbb R\to\mathbb R\) is bijective and must have an inverse function.

知识点：

函数 \(f:A\to B\) 有反函数，需要它是 bijective：

- one-to-one：不同输入不会给同一个输出；
- onto：每个目标集合里的值都能被取到。

对实函数，常用方法是：

1. 用导数判断严格单调，从而说明 one-to-one；
2. 用极限和连续性判断值域，从而说明 onto。

#### (b) 求 \(f^{-1}(3)\)

\[
f^{-1}(3)
\]

的意思是：找一个 \(x\)，使得

\[
f(x)=3.
\]

观察：

\[
f(1)=1^3+5(1)-3=1+5-3=3.
\]

所以

\[
\boxed{f^{-1}(3)=1}.
\]

答案：

```text
1
```

#### 求 \(\dfrac{d}{dx}f^{-1}(3)\)

反函数求导公式：

\[
(f^{-1})'(y)=\frac1{f'(f^{-1}(y))}.
\]

这里

\[
f^{-1}(3)=1.
\]

所以

\[
(f^{-1})'(3)=\frac1{f'(1)}.
\]

而

\[
f'(x)=3x^2+5,
\]

所以

\[
f'(1)=3(1)^2+5=8.
\]

因此

\[
\boxed{(f^{-1})'(3)=\frac18}.
\]

答案：

```text
1/8
```

知识点：

反函数值：

\[
f^{-1}(a)=b
\]

等价于

\[
f(b)=a.
\]

反函数导数：

\[
(f^{-1})'(a)=\frac1{f'(b)},
\]

其中 \(b=f^{-1}(a)\)。

---

### IMG_0833：变上限积分求 \(g(3)\)

题目给出连续函数 \(g:\mathbb R\to\mathbb R\)，并且

\[
\int_0^{x^3+5x-3} g(t)\,dt=3x.
\]

要求求 \(g(3)\)。

#### 解法

对等式两边关于 \(x\) 求导。左边是“变上限积分”，要用微积分基本定理和链式法则：

\[
\frac d{dx}\int_0^{u(x)}g(t)\,dt=g(u(x))u'(x).
\]

这里

\[
u(x)=x^3+5x-3,
\]

所以

\[
u'(x)=3x^2+5.
\]

因此

\[
g(x^3+5x-3)(3x^2+5)=3.
\]

我们要的是 \(g(3)\)，所以要找使

\[
x^3+5x-3=3
\]

的 \(x\)。前一问已经知道

\[
f(1)=1^3+5(1)-3=3.
\]

所以令 \(x=1\)，得到

\[
g(3)(3(1)^2+5)=3.
\]

也就是

\[
8g(3)=3.
\]

因此

\[
\boxed{g(3)=\frac38}.
\]

答案：

```text
3/8
```

知识点：

若

\[
F(x)=\int_a^{u(x)}g(t)\,dt,
\]

则

\[
F'(x)=g(u(x))u'(x).
\]

这是“微积分基本定理 + 链式法则”。遇到要求 \(g(\text{某个数})\) 时，关键不是直接代那个数到 \(x\)，而是先让上限 \(u(x)\) 等于那个数。

---

### IMG_0834 / IMG_0835：Upper Riemann Sum

题目给出 \(f,g,h\) 在 \([2,9]\) 上连续，分割为

\[
P_n=\left\{
\frac{2n}{n},\frac{2n+7}{n},\frac{2n+14}{n},\ldots,\frac{9n}{n}
\right\}.
\]

当 \(n=4\) 时，分割点是

\[
2,\ 3.75,\ 5.50,\ 7.25,\ 9.
\]

所以每个小区间宽度为

\[
\Delta x=\frac{9-2}{4}=\frac74.
\]

Upper Riemann sum 的意思是：每个小区间上取函数的最大值，再乘以小区间宽度，最后相加。

#### (a) \(f\) 严格递增

如果 \(f\) 在 \([2,9]\) 上严格递增，那么每个小区间最大值都出现在右端点。

四个小区间分别是

\[
[2,3.75],\ [3.75,5.50],\ [5.50,7.25],\ [7.25,9].
\]

右端点分别是

\[
3.75,\ 5.50,\ 7.25,\ 9.
\]

所以 upper Riemann sum 是

\[
\boxed{
\frac74\bigl(f(3.75)+f(5.50)+f(7.25)+f(9.0)\bigr)
}.
\]

知识点：

递增函数在闭区间 \([a,b]\) 上最大值在右端点 \(b\)。所以上和取右端点，下面积和取左端点。

#### (b) \(g\) 严格递减

如果 \(g\) 在 \([2,9]\) 上严格递减，那么每个小区间最大值都出现在左端点。

左端点分别是

\[
2,\ 3.75,\ 5.50,\ 7.25.
\]

所以 upper Riemann sum 是

\[
\boxed{
\frac74\bigl(g(2.0)+g(3.75)+g(5.50)+g(7.25)\bigr)
}.
\]

知识点：

递减函数在闭区间 \([a,b]\) 上最大值在左端点 \(a\)。所以不要看到 “upper” 就机械取右端点；要看函数是递增还是递减。

#### (c) 根据图像和给定数值求 \(h\) 的 upper Riemann sum

分割点仍然是

\[
2,\ 3.75,\ 5.50,\ 7.25,\ 9.
\]

已知驻点值：

\[
h(4.231)=2.2700,\qquad h(7.431)=5.0790.
\]

已知端点/分割点处的值：

\[
h(2.00)=8.2700,
\]

\[
h(3.75)=2.4795,
\]

\[
h(5.50)=3.2449,
\]

\[
h(7.25)=5.0531,
\]

\[
h(9.00)=2.3909.
\]

逐段取最大值：

\[
[2,3.75]:\quad \max=8.2700.
\]

\[
[3.75,5.50]:\quad \max=3.2449.
\]

因为这个区间内的驻点 \(4.231\) 是低点，值为 \(2.2700\)，不是最大值。

\[
[5.50,7.25]:\quad \max=5.0531.
\]

\[
[7.25,9]:\quad \max=5.0790.
\]

因为驻点 \(7.431\) 落在 \([7.25,9]\) 内，而且图像显示它是这一段的最高点。

所以 upper Riemann sum 为

\[
\frac74(8.2700+3.2449+5.0531+5.0790).
\]

计算：

\[
8.2700+3.2449+5.0531+5.0790=21.6470.
\]

因此

\[
\frac74\cdot 21.6470=37.88225.
\]

所以答案是

\[
\boxed{37.8822}
\]

至少保留三位小数也可以写作

```text
37.882
```

知识点：

对于不是单调的函数，upper Riemann sum 不能只看左右端点。每个小区间都要找该区间内的最大值；候选点包括：

1. 小区间左端点；
2. 小区间右端点；
3. 落在该小区间内部的驻点。

本题最容易错的是最后一段 \([7.25,9]\)：最大值不是 \(h(7.25)\)，而是内部驻点 \(h(7.431)=5.0790\)。

---

### IMG_0836：极限比较与反常积分

题目给出

\[
f(x)=\frac1{(7x^7+6x^5+9x+2)^{1/4}}.
\]

#### (a) 找 \(g(x)=\dfrac1{ax^b}\)，使

\[
\lim_{x\to\infty}\frac{f(x)}{g(x)}=1.
\]

当 \(x\to\infty\) 时，只看最高次项：

\[
7x^7+6x^5+9x+2\sim 7x^7.
\]

所以

\[
(7x^7+6x^5+9x+2)^{1/4}
\sim
(7x^7)^{1/4}.
\]

即

\[
(7x^7)^{1/4}=7^{1/4}x^{7/4}.
\]

因此

\[
f(x)\sim \frac1{7^{1/4}x^{7/4}}.
\]

所以

\[
g(x)=\frac1{7^{1/4}x^{7/4}}.
\]

答案：

\[
\boxed{a=7^{1/4}},\qquad \boxed{b=\frac74}.
\]

输入形式：

```text
a = 7^(1/4)
b = 7/4
```

知识点：

做 \(x\to\infty\) 的极限比较时，多项式或根式表达式通常先取最高次项。这里最高次项是 \(7x^7\)，再被整体开 \(1/4\) 次方，所以幂指数也要乘 \(1/4\)：

\[
x^7 \mapsto x^{7/4}.
\]

#### (b) 判断

\[
I=\int_1^\infty f(x)\,dx
\]

是否收敛。

由 (a) 可知

\[
f(x)\sim \frac1{7^{1/4}x^{7/4}}.
\]

而

\[
\int_1^\infty \frac1{x^p}\,dx
\]

在 \(p>1\) 时收敛，在 \(p\le 1\) 时发散。

这里

\[
p=b=\frac74>1.
\]

因此由极限比较判别法，

\[
\boxed{I\text{ converges}}.
\]

对应选项：

```text
Since b > 1, by the Limit Comparison Theorem the integral I is convergent.
```

知识点：

极限比较法不是比较 \(a\) 是否大于 \(1\)，而是比较幂函数中的指数 \(b\)。因为反常积分

\[
\int_1^\infty \frac1{x^b}\,dx
\]

的收敛性由指数 \(b\) 决定。

---

### IMG_0837 / IMG_0838 / IMG_0839：双曲函数方程化为二次方程

#### (a) 从二次方程得到双曲函数方程

题目从

\[
4x^2-20x+21=0
\]

开始，令

\[
x=e^t.
\]

代入得

\[
4e^{2t}-20e^t+21=0.
\]

两边除以 \(e^t\)：

\[
4e^t-20+21e^{-t}=0.
\]

利用

\[
e^t=\cosh t+\sinh t,
\]

\[
e^{-t}=\cosh t-\sinh t.
\]

于是

\[
4(\cosh t+\sinh t)-20+21(\cosh t-\sinh t)=0.
\]

合并同类项：

\[
(4+21)\cosh t+(4-21)\sinh t-20=0.
\]

所以

\[
25\cosh t-17\sinh t-20=0.
\]

答案输入形式：

```text
25*cosh(t)-17*sinh(t)-20
```

或等价地：

```text
-17*sinh(t)+25*cosh(t)-20
```

知识点：

双曲函数的基本定义是

\[
\cosh t=\frac{e^t+e^{-t}}2,\qquad
\sinh t=\frac{e^t-e^{-t}}2.
\]

反过来记更方便：

\[
e^t=\cosh t+\sinh t,\qquad
e^{-t}=\cosh t-\sinh t.
\]

#### (b)(i) 一般方程什么时候对应实根

一般方程为

\[
a\cosh t+b\sinh t+c=0.
\]

令

\[
x=e^t.
\]

则

\[
\cosh t=\frac{x+x^{-1}}2,\qquad
\sinh t=\frac{x-x^{-1}}2.
\]

代入：

\[
a\frac{x+x^{-1}}2+b\frac{x-x^{-1}}2+c=0.
\]

两边乘以 \(2x\)：

\[
a(x^2+1)+b(x^2-1)+2cx=0.
\]

整理得到二次方程：

\[
(a+b)x^2+2cx+(a-b)=0.
\]

这个二次方程有实根的条件是判别式非负：

\[
(2c)^2-4(a+b)(a-b)\ge 0.
\]

化简：

\[
4c^2-4(a^2-b^2)\ge 0.
\]

\[
c^2-a^2+b^2\ge 0.
\]

也就是

\[
\boxed{b^2+c^2-a^2\ge 0}.
\]

对应选项：

```text
b^2 + c^2 - a^2 >= 0
```

论述：

我先把 \(\cosh t,\sinh t\) 用 \(x=e^t\) 表示，使原来的双曲函数方程变成关于 \(x\) 的二次方程

\[
(a+b)x^2+2cx+(a-b)=0.
\]

二次方程有实根当且仅当判别式非负，因此计算判别式并化简，就得到

\[
b^2+c^2-a^2\ge0.
\]

#### (b)(ii) 为什么还需要 \(x>0\)

因为我们令

\[
x=e^t.
\]

而指数函数永远为正，所以

\[
\boxed{x>0}.
\]

仅仅有实根还不够；如果二次方程的实根是负数或零，就不能写成 \(e^t\)。

知识点：

代换 \(x=e^t\) 会把问题转成二次方程，但这个代换不是所有实数都能取到。它的值域是

\[
(0,\infty).
\]

所以二次方程不仅要有实根，还要有正实根。

#### (b)(iii) 保证二次方程根为正的条件

二次方程是

\[
(a+b)x^2+2cx+(a-b)=0.
\]

对一般二次方程

\[
Ax^2+Bx+C=0,
\]

两根之和为

\[
r_1+r_2=-\frac BA,
\]

两根之积为

\[
r_1r_2=\frac CA.
\]

要两根都为正，需要：

\[
r_1+r_2>0,
\]

且

\[
r_1r_2>0.
\]

本题中

\[
A=a+b,\qquad B=2c,\qquad C=a-b.
\]

所以根之和：

\[
r_1+r_2=-\frac{2c}{a+b}.
\]

要求它大于 \(0\)，等价于

\[
\frac{-2c}{a+b}>0.
\]

也就是

\[
\boxed{c(a+b)<0}.
\]

根之积：

\[
r_1r_2=\frac{a-b}{a+b}.
\]

要求它大于 \(0\)，等价于分子分母同号：

\[
\frac{a-b}{a+b}>0.
\]

这等价于

\[
(a-b)(a+b)>0.
\]

即

\[
\boxed{a^2-b^2>0}.
\]

所以应选：

```text
c(a+b) < 0
a^2 - b^2 > 0
```

知识点：

“有实根”只看判别式；“根为正”还要看根的和与积。

对二次方程：

\[
Ax^2+Bx+C=0,
\]

若两个根都为正，常用条件是：

\[
-\frac BA>0,\qquad \frac CA>0.
\]

本题还要结合 \(x=e^t>0\)，所以正根条件是关键。

---

### IMG_0840：夹逼定理能不能用

题目给出

\[
f(x)\le g(x)\le h(x)\qquad \text{for all }x\in\mathbb R,
\]

并且

\[
\lim_{x\to\infty}f(x)=1,
\qquad
\lim_{x\to\infty}h(x)=5.
\]

#### (a) 关于 \(g(x)\) 的极限能推出什么

因为 \(g(x)\) 被夹在 \(f(x)\) 和 \(h(x)\) 中间，而下界趋向 \(1\)，上界趋向 \(5\)。但是这两个极限不相等，所以不能用夹逼定理推出 \(g(x)\) 的极限。

因此唯一可靠结论是：

```text
We don't know if lim_{x->infty} g(x) exists.
```

知识点：

夹逼定理的标准形式是：

\[
f(x)\le g(x)\le h(x),
\]

如果

\[
\lim_{x\to a}f(x)=\lim_{x\to a}h(x)=L,
\]

那么

\[
\lim_{x\to a}g(x)=L.
\]

注意：上下界必须趋向同一个值。本题上下界分别趋向 \(1\) 和 \(5\)，所以不能推出 \(g\) 的极限存在。

#### (b) 哪些说法不可能为真

因为 \(f(x)\to 1\)，所以当 \(x\) 足够大时，\(f(x)\) 会接近 \(1\)。又因为

\[
f(x)\le g(x),
\]

所以 \(g(x)\) 不可能趋向 \(-\infty\)。

因为 \(h(x)\to 5\)，所以当 \(x\) 足够大时，\(h(x)\) 会接近 \(5\)。又因为

\[
g(x)\le h(x),
\]

所以 \(g(x)\) 不可能趋向 \(+\infty\)。

因此以下说法不能为真：

```text
g(x) -> infinity as x -> infinity
```

如果选项中还有

```text
g(x) -> -infinity as x -> infinity
```

也不能为真。

但下面这些都可能为真：

\[
\lim_{x\to\infty}g(x)=1,\quad
\lim_{x\to\infty}g(x)=3,\quad
\lim_{x\to\infty}g(x)=5.
\]

例如 \(g=f\) 时可以趋向 \(1\)，\(g=h\) 时可以趋向 \(5\)，取合适的中间函数也可以趋向 \(3\)。

知识点：

上下界极限不同的时候，中间函数可能有极限，也可能没有极限。它只是不可能最终跑到上下界之外，比如趋向 \(+\infty\) 或 \(-\infty\)。

---

### IMG_0841 / IMG_0842 / IMG_0843：由 \(x(t),y(t)\) 判断 \(y\) 关于 \(x\) 的图像

题目给了两个参数图：

- 左图是 \(x\) 随 \(t\) 的变化；
- 右图是 \(y\) 随 \(t\) 的变化；
- 要选择 \(y\) 作为 \(x\) 的函数时的图像。

从图像可以读出：

1. \(x(t)\) 在某个 \(t\) 值附近有竖直渐近线；
2. \(x(t)\) 的水平渐近线大约是 \(x=0\)；
3. \(y(t)\) 是随 \(t\) 递减的一条直线；
4. 当 \(t\) 趋近 \(x(t)\) 的竖直渐近线两侧时，\(x\) 会分别趋向 \(+\infty\) 和 \(-\infty\)；
5. 当 \(t\to\pm\infty\) 时，\(x(t)\to 0\)，所以合成后的 \(y(x)\) 会在 \(x=0\) 附近出现竖直渐近行为。

如果按图中大致关系读，可以把它理解成类似

\[
x(t)\approx \frac1{t+5},
\]

而 \(y(t)\) 是一条下降直线，类似

\[
y(t)\approx -\frac43t-4.
\]

于是

\[
t\approx \frac1x-5.
\]

代入 \(y(t)\)：

\[
y\approx -\frac43\left(\frac1x-5\right)-4.
\]

化简：

\[
y\approx \frac83-\frac4{3x}.
\]

所以 \(y(x)\) 的图像应当像一个有水平渐近线和竖直渐近线的双曲线：

- 竖直渐近线在 \(x=0\)；
- 水平渐近线大约在 \(y=\frac83\)；
- 当 \(x\to0^-\) 时，\(y\to+\infty\)；
- 当 \(x\to0^+\) 时，\(y\to-\infty\)；
- 当 \(x\to\pm\infty\) 时，\(y\) 接近同一条水平渐近线。

因此应选择截图中第一个图像，也就是：

```text
左支在 x<0 且向 x=0^- 时上升到 +infty，
右支在 x>0 且向 x=0^+ 时下降到 -infty，
两端都趋向同一条水平渐近线。
```

知识点：

参数曲线题不要急着把 \(y\) 当成 \(x\) 的普通函数。先看：

1. 哪些 \(t\) 对应 \(x>0\)、哪些对应 \(x<0\)；
2. 当 \(t\) 增大时，点在图像上往哪个方向走；
3. \(x(t)\) 的渐近线会变成 \(y(x)\) 图像里的哪种渐近线；
4. 如果能从图像读出近似公式，可以用“消去参数”的方法确认形状。

这里最重要的是 \(x=e^t\) 或 \(x=1/(t+c)\) 这类代换会限制 \(x\) 的取值和渐近线位置，不能只看 \(y(t)\) 是直线就误以为 \(y(x)\) 也是直线。

---

### IMG_0844：矩阵转置与逆矩阵化简

题目给出 \(A,B,C\) 是同阶可逆方阵，要求化简：

\[
(C^TA^T)^TC^{-1}BB^T.
\]

#### 解法

先处理转置部分。

注意：

\[
C^TA^T=(AC)^T.
\]

所以

\[
(C^TA^T)^T=AC.
\]

原式变成

\[
ACC^{-1}BB^T.
\]

因为

\[
CC^{-1}=I,
\]

所以

\[
ACC^{-1}BB^T=AIBB^T=ABB^T.
\]

因此答案是

\[
\boxed{ABB^T}.
\]

按照题目的输入格式：

```text
A.B.BT
```

知识点：

矩阵乘法中最重要的规则是顺序：

\[
(XY)^T=Y^TX^T.
\]

也就是说，转置一个乘积时，顺序会反过来。

另外：

\[
CC^{-1}=I,
\]

单位矩阵 \(I\) 乘在中间不会改变结果。

---

### IMG_0845-0846：复数的极形式、共轭根与实系数多项式

题目给出

\[
z_k=5e^{i(2\pi k/8)},\qquad k=0,1,\ldots,7.
\]

#### (a) 这些复数在复平面上的位置

由欧拉公式

\[
e^{i\theta}=\cos\theta+i\sin\theta
\]

可知

\[
5e^{i(2\pi k/8)}
\]

表示模长为 \(5\)、辐角为 \(\frac{2\pi k}{8}\) 的复数。

因为 \(k=0,1,\ldots,7\)，角度每次增加

\[
\frac{2\pi}{8}=\frac{\pi}{4},
\]

所以这 8 个点均匀分布在以 \(0\) 为圆心、半径为 \(5\) 的圆周上。

答案：

\[
\boxed{\text{evenly spaced around the perimeter of a circle with centre }0\text{ and radius }5}
\]

并且 \(k=0\) 时

\[
z_0=5e^{0}=5,
\]

所以有一个点在正实轴上，这句话为 true。

#### (b) 多项式 \(p(z)=(z-z_2)(z-z_6)\)

先算两个根。

\[
z_2=5e^{i(2\pi\cdot 2/8)}=5e^{i\pi/2}=5i,
\]

\[
z_6=5e^{i(2\pi\cdot 6/8)}=5e^{i3\pi/2}=-5i.
\]

所以

\[
p(z)=(z-5i)(z+5i)=z^2-(5i)^2=z^2+25.
\]

因此 \(p(z)\) 的系数是

\[
1,\ 0,\ 25,
\]

全部是实数。由于实数也是复数的一种，所以“coefficients are all complex” 也为 true；但它们不是全部 purely imaginary。

正确判断：

\[
\boxed{\text{all complex：true；all real：true；purely imaginary：false}.}
\]

知识点：

“实数是复数”这一点在选择题里很容易被忽略。集合关系是

\[
\mathbb{R}\subset \mathbb{C}.
\]

所以如果系数全是实数，那么它们当然也全是复数。

#### (c) 最小次数的实系数多项式 \(r(z)\)

题目给出

\[
q(z)=(z-z_0)^5(z-z_1)^6(z-z_2)^9(z-z_3)^9(z-z_4)^3
(z-z_5)^5(z-z_6)^4(z-z_7)^2.
\]

要求找一个非零实系数多项式 \(r(z)\)，使得 \(q(z)\mid r(z)\)，并且 \(r(z)\) 的次数最小。

关键知识点：

如果一个多项式的系数全是实数，那么它的非实复根必须成共轭对出现，而且重数也必须相同。

也就是说，如果 \(a+bi\) 是根，重数为 \(m\)，那么 \(a-bi\) 也必须至少有重数 \(m\)。

这 8 个点的共轭关系是：

\[
z_0=5,\qquad z_4=-5
\]

这两个本身就是实数，不需要配对。

非实根成对为：

\[
z_1\leftrightarrow z_7,\qquad
z_2\leftrightarrow z_6,\qquad
z_3\leftrightarrow z_5.
\]

逐对看重数：

\[
z_1^6,\ z_7^2
\]

为了实系数，需要二者重数都至少为 \(6\)，所以这一对贡献

\[
2\cdot 6=12.
\]

\[
z_2^9,\ z_6^4
\]

需要二者重数都至少为 \(9\)，贡献

\[
2\cdot 9=18.
\]

\[
z_3^9,\ z_5^5
\]

需要二者重数都至少为 \(9\)，贡献

\[
2\cdot 9=18.
\]

实根部分贡献：

\[
z_0^5,\ z_4^3
\]

所以贡献

\[
5+3=8.
\]

因此最小次数是

\[
8+12+18+18=56.
\]

答案：

\[
\boxed{\deg(r(z))=56}.
\]

方法总结：

遇到“实系数多项式 + 复根”时，立刻检查共轭根。最小次数不是简单把原来的指数相加，而是把每一对共轭根的重数补到二者中的较大值。

---

### IMG_0847-0849：极坐标曲线的旋转、放大与反射

原图是一条极坐标曲线

\[
r=f(\theta),\qquad 0\leq \theta\leq 2\pi.
\]

从图中可以看出它是四瓣花形，花瓣大致位于

\[
0,\quad \frac{\pi}{2},\quad \pi,\quad \frac{3\pi}{2}
\]

这些方向上。

#### (a) 选择 \(r=2f\left(\theta-\frac{\pi}{3}\right)\) 的图像

这个式子包含两个变换：

1. 外面的 \(2\)：把所有半径扩大为原来的 \(2\) 倍；
2. 里面的 \(\theta-\frac{\pi}{3}\)：把原图整体逆时针旋转 \(\frac{\pi}{3}\)。

为什么是逆时针旋转？

如果原来某个特征出现在角度 \(\alpha\)，即 \(f(\alpha)\) 取到对应形状，那么新图像满足

\[
\theta-\frac{\pi}{3}=\alpha,
\]

所以

\[
\theta=\alpha+\frac{\pi}{3}.
\]

因此角度增加了 \(\frac{\pi}{3}\)，即逆时针转了 \(60^\circ\)。

所以原来的四个花瓣方向

\[
0,\quad \frac{\pi}{2},\quad \pi,\quad \frac{3\pi}{2}
\]

变为

\[
\frac{\pi}{3},\quad \frac{5\pi}{6},\quad \frac{4\pi}{3},\quad \frac{11\pi}{6}.
\]

也就是大约在

\[
60^\circ,\ 150^\circ,\ 240^\circ,\ 330^\circ
\]

方向上，并且半径是原来的两倍。

所以应选择图中花瓣朝向这些方向、外圈刻度约放大到 \(1.5\) 的那一个，也就是截图里被选中的那个图。

知识点：

\[
r=f(\theta-a)
\]

表示把原图逆时针旋转 \(a\)；而

\[
r=f(\theta+a)
\]

表示把原图顺时针旋转 \(a\)。

#### (b) 关于 \(y\)-轴反射

关于 \(y\)-轴反射时，极角 \(\theta\) 会变成

\[
\pi-\theta.
\]

因为一个点

\[
(r,\theta)
\]

的直角坐标是

\[
(r\cos\theta,\ r\sin\theta).
\]

关于 \(y\)-轴反射后，横坐标变号，纵坐标不变：

\[
(-r\cos\theta,\ r\sin\theta).
\]

而

\[
\cos(\pi-\theta)=-\cos\theta,\qquad \sin(\pi-\theta)=\sin\theta,
\]

所以反射后的角度是 \(\pi-\theta\)。

现在原曲线是

\[
g(\theta)=2f\left(\theta-\frac{\pi}{3}\right).
\]

关于 \(y\)-轴反射后的曲线是

\[
r=g(\pi-\theta).
\]

代入 \(g\)：

\[
r=2f\left((\pi-\theta)-\frac{\pi}{3}\right)
=2f\left(\pi-\theta-\frac{\pi}{3}\right).
\]

所以答案是

\[
\boxed{r=2f\left(\pi-\theta-\frac{\pi}{3}\right)}.
\]

方法总结：

极坐标反射要记住三条常用规则：

\[
\text{关于 }x\text{-轴反射：}\quad \theta\mapsto -\theta,
\]

\[
\text{关于 }y\text{-轴反射：}\quad \theta\mapsto \pi-\theta,
\]

\[
\text{关于原点反射：}\quad \theta\mapsto \theta+\pi.
\]

做这类题时，不要只凭图像直觉；先把已有函数记作 \(g(\theta)\)，再做替换最稳。

---

### IMG_0850：只有一个点不同的函数与黎曼积分

题目给出分段函数

\[
g(x)=
\begin{cases}
-2, & x=8,\\
-7, & x\neq 8.
\end{cases}
\]

要在区间 \([7,8]\) 上积分，并考虑分割

\[
P_n=\left\{7+\frac{k}{n}\right\}_{k=0}^{n}
=\left\{7,\ 7+\frac1n,\ 7+\frac2n,\ldots,\ 7+\frac nn\right\}.
\]

这个分割把区间 \([7,8]\) 分成 \(n\) 个小区间，每个小区间的宽度是

\[
\Delta x=\frac{8-7}{n}=\frac1n.
\]

所以答案：

\[
\boxed{n\text{ 个子区间，宽度 }1/n}.
\]

#### 下和

在每个子区间里，函数值几乎处处都是 \(-7\)，只有在 \(x=8\) 这个单点处是 \(-2\)。

因为

\[
-7<-2,
\]

所以每个子区间上的下确界都是 \(-7\)。

因此下黎曼和为

\[
\underline S_{P_n}
=\sum_{k=1}^{n}(-7)\cdot \frac1n
=-7.
\]

答案：

\[
\boxed{\underline S_{P_n}=-7}.
\]

#### 上和

对前 \(n-1\) 个子区间来说，它们不包含点 \(x=8\)，所以函数恒为 \(-7\)。这些区间上的上确界也是 \(-7\)。

最后一个子区间是

\[
\left[8-\frac1n,8\right].
\]

它包含点 \(x=8\)，而

\[
g(8)=-2.
\]

由于 \(-2>-7\)，所以最后一个子区间上的上确界是 \(-2\)。

于是上和为

\[
\overline S_{P_n}
=(n-1)(-7)\frac1n+(-2)\frac1n.
\]

化简：

\[
\overline S_{P_n}
=-\frac{7(n-1)}n-\frac2n
=-7+\frac7n-\frac2n
=-7+\frac5n.
\]

答案：

\[
\boxed{\overline S_{P_n}=-7+\frac5n}.
\]

#### 定积分是多少？

当 \(n\to\infty\) 时，

\[
\underline S_{P_n}=-7,
\]

\[
\overline S_{P_n}=-7+\frac5n\to -7.
\]

上下和趋向同一个数，所以

\[
\int_7^8 g(x)\,dx=-7.
\]

知识点：

只改变有限个点的函数值，不改变黎曼积分。积分看的是“区间上的累计面积”，单个点没有宽度，因此对面积没有贡献。

但是，单个点会影响某些分割下的上和或下和，因为上确界、下确界会看区间内的最大/最小值。这里 \(x=8\) 让最后一个小区间的上确界从 \(-7\) 变成了 \(-2\)，所以出现了额外的 \(\frac5n\)。

---

### IMG_0851：行列式缩放性质

题目给出 \(A\) 是一个 \(n\times n\) 矩阵，并且

\[
\det(A)=7,\qquad \det(7A)=2401.
\]

要求 \(n\)。

核心公式：

\[
\det(cA)=c^n\det(A)
\]

其中 \(A\) 是 \(n\times n\) 矩阵。

这里 \(c=7\)，所以

\[
\det(7A)=7^n\det(A).
\]

代入 \(\det(A)=7\)：

\[
\det(7A)=7^n\cdot 7=7^{n+1}.
\]

又因为

\[
2401=7^4,
\]

所以

\[
7^{n+1}=7^4.
\]

因此

\[
n+1=4,
\]

\[
n=3.
\]

答案：

\[
\boxed{n=3}.
\]

知识点：

很多人会误以为 \(\det(7A)=7\det(A)\)，这是错的。对 \(n\times n\) 矩阵，把整个矩阵乘以 \(7\)，等于每一行都乘以 \(7\)，一共有 \(n\) 行，所以行列式会乘以 \(7^n\)。

---

### IMG_0852：\(\tanh^{-1}\) 与 \(\ln\) 的定义域

题目给出任意函数 \(f:\mathbb R\to\mathbb R\)，定义

\[
g(x)=\tanh^{-1}(\ln(f(x))).
\]

要求 \(g\) 的最大定义域条件。

这题要同时满足两个定义域条件。

#### 第一步：\(\ln(f(x))\) 有定义

自然对数要求输入为正数：

\[
f(x)>0.
\]

#### 第二步：\(\tanh^{-1}(u)\) 有定义

实函数

\[
\tanh^{-1}(u)
\]

的定义域是

\[
-1<u<1.
\]

这里

\[
u=\ln(f(x)).
\]

所以需要

\[
-1<\ln(f(x))<1.
\]

也就是

\[
|\ln(f(x))|<1.
\]

因此题目里的

\[
a=1.
\]

#### 转化成 \(f(x)\) 的范围

由

\[
-1<\ln(f(x))<1
\]

两边取指数函数，因为 \(e^x\) 是严格递增函数，所以不等号方向不变：

\[
e^{-1}<f(x)<e^1.
\]

因此

\[
f(x)\in (e^{-1},e).
\]

按照题目 Maple 格式：

\[
\boxed{b=\exp(-1),\qquad c=\exp(1)}.
\]

最终条件：

\[
\boxed{|\ln(f(x))|<1}
\]

以及

\[
\boxed{f(x)\in(\exp(-1),\exp(1))}.
\]

知识点：

复合函数的定义域要从外到内逐层检查：

1. \(\ln(\cdot)\) 要求输入 \(>0\)；
2. \(\tanh^{-1}(\cdot)\) 要求输入在 \((-1,1)\)；
3. 把外层要求代回内层表达式，再化成对 \(f(x)\) 的要求。

这里 \(-1<\ln(f(x))<1\) 已经自动保证 \(f(x)>0\)，所以最后不需要额外再写 \(f(x)>0\)。

---

### IMG_0853-0854：由 \(r\) 与 \(\theta\) 的图像判断极坐标图像

题目给出的是 \(r\) 随 \(\theta\) 变化的图像，而不是直接给极坐标图像。我们要根据这个 \(r(\theta)\) 的信息选择对应的 polar plot。

从图中读出：

1. \(\theta\) 的主要范围大约在

\[
-\frac{\pi}{2}<\theta<0.
\]

2. 在这个范围内，\(r\) 始终为正，大约在 \(4\) 附近，靠近两端时 \(r\) 迅速变大。

3. \(\theta=-\frac{\pi}{2}\) 对应负 \(y\) 轴方向，\(\theta=0\) 对应正 \(x\) 轴方向。

因此极坐标图像应该位于第四象限附近：从接近负 \(y\) 轴的方向开始，半径很大；中间半径较小；再到接近正 \(x\) 轴方向时半径又变大。

截图中选择的第一个极坐标图正好符合：

- 曲线只出现在第四象限附近；
- 两端向外延伸；
- 中间靠近原点一些；
- 角度范围从 \(-\frac{\pi}{2}\) 到 \(0\)。

所以选择截图中已经选中的第一个图。

知识点：

给 \(r\)-vs-\(\theta\) 图时，不要把它当成普通的 \(xy\) 图。它告诉你的是：

\[
\theta=\text{方向},\qquad r=\text{离原点的距离}.
\]

判断步骤：

1. 先看 \(\theta\) 的范围，确定曲线在哪些方向出现；
2. 再看 \(r\) 的大小，确定每个方向上离原点多远；
3. 如果 \(r>0\)，点就在角度 \(\theta\) 的射线上；
4. 如果 \(r<0\)，点会出现在相反方向，即角度 \(\theta+\pi\) 的射线上。

本题里 \(r\) 都是正的，所以不需要做反向处理。

`IMG_0855` 是这一道极坐标选择题的结尾截图，主要显示两个候选 polar plot 和 “END OF THIS QUESTION”。关键判断已经在 `IMG_0853-0854` 里说明：看 \(\theta\) 的范围和 \(r\) 的正负、大小变化，而不是把 \(r\)-vs-\(\theta\) 图当成普通 \(xy\) 图。

---

### IMG_0856-0858：复数线性方程组，拆成实部和虚部

题目给出复数线性方程组：

\[
\begin{cases}
z_1+\alpha z_2=w_1,\\
z_2+\alpha z_3=w_2,\\
z_3+\alpha z_4=w_3,\\
\alpha z_1+z_4=w_4,
\end{cases}
\]

其中 \(\alpha\in\mathbb R\)，未知量 \(z_1,z_2,z_3,z_4\in\mathbb C\)。

令

\[
z=x+iy,\qquad w=u+iv,
\]

其中

\[
x,y,u,v\in\mathbb R^4.
\]

因为矩阵

\[
A=
\begin{pmatrix}
1&\alpha&0&0\\
0&1&\alpha&0\\
0&0&1&\alpha\\
\alpha&0&0&1
\end{pmatrix}
\]

的所有系数都是实数，所以

\[
A(x+iy)=Ax+iAy.
\]

因此一个复数方程组可以拆成两个实数方程组：

\[
Ax=\operatorname{Re}(w),\qquad Ay=\operatorname{Im}(w).
\]

题目特殊给出

\[
w_k=2e^{i\pi k/2},\qquad k=1,2,3,4.
\]

逐个算：

\[
w_1=2e^{i\pi/2}=2i,
\]

\[
w_2=2e^{i\pi}=-2,
\]

\[
w_3=2e^{3i\pi/2}=-2i,
\]

\[
w_4=2e^{2i\pi}=2.
\]

所以实部向量是

\[
p=\operatorname{Re}(w)=
\begin{pmatrix}
0\\
-2\\
0\\
2
\end{pmatrix},
\]

虚部向量是

\[
q=\operatorname{Im}(w)=
\begin{pmatrix}
2\\
0\\
-2\\
0
\end{pmatrix}.
\]

因此题目中的空应填：

\[
\boxed{q=\langle 2,0,-2,0\rangle}.
\]

知识点：

复数线性方程组如果系数矩阵是实矩阵，就可以把未知量和右端项都拆成实部、虚部。这样一个复数方程组

\[
Az=w
\]

等价于两个实数方程组

\[
Ax=\operatorname{Re}(w),\qquad Ay=\operatorname{Im}(w).
\]

这一步非常重要，因为它把“复数未知量”问题转化成了熟悉的实线性代数问题。

---

### IMG_0857：由增广矩阵判断解的个数

题目说增广矩阵 \((A\mid p)\) 行化简后最后一行为

\[
0\quad 0\quad 0\quad 0\mid *
\]

截图中最后一列也是星号，但从选项和已选答案看，这里的含义是最后一行仍然是相容的零行，也就是没有出现

\[
0=非零数
\]

这种矛盾。

又因为系数矩阵部分最后一行全为 \(0\)，说明 \(A\) 不满秩。未知量有 \(4\) 个，但主元少于 \(4\) 个，所以如果方程组相容，就有自由变量，从而有无穷多解。

因此可判断：

\[
\boxed{Ay=q\text{ 有无穷多解，并且 }Ax=p\text{ 也有无穷多解。}}
\]

对应选项：

\[
\boxed{\text{There are infinitely many solutions to }Ay=q\text{ and therefore infinitely many solutions to }Ax=p.}
\]

知识点：

看增广矩阵时，最重要的是区分两种情况：

1. 如果出现

\[
0\quad 0\quad 0\quad 0\mid c,\qquad c\neq 0,
\]

就表示 \(0=c\)，方程组无解。

2. 如果没有矛盾行，但主元个数少于未知量个数，就有自由变量，因此有无穷多解。

这一题的核心不是具体求解，而是根据行阶梯形矩阵判断：

\[
\text{无矛盾}+ \text{有自由变量}\Longrightarrow \text{无穷多解}.
\]

---

### IMG_0858：证明 \(\alpha>14\) 且 \(w_i\in\mathbb R\) 时，\(z_i\) 必为实数

要证明 \(z_1,z_2,z_3,z_4\) 都是实数，就是要证明它们的虚部都为 \(0\)。

令

\[
z=x+iy,
\]

其中

\[
x,y\in\mathbb R^4.
\]

原方程组可写成

\[
Az=w.
\]

代入 \(z=x+iy\)，得到

\[
A(x+iy)=w.
\]

由于 \(A\) 是实矩阵，

\[
Ax+iAy=w.
\]

现在题目假设

\[
w_1,w_2,w_3,w_4\in\mathbb R,
\]

也就是说 \(w\) 的虚部为 \(0\)。比较虚部可得

\[
Ay=0.
\]

接下来只要证明 \(A\) 可逆，就能推出

\[
y=0.
\]

对这个矩阵，有

\[
\det(A)=1-\alpha^4.
\]

因为 \(\alpha>14\)，所以

\[
\alpha^4\neq 1,
\]

从而

\[
\det(A)\neq 0.
\]

因此 \(A\) 可逆。由

\[
Ay=0
\]

两边左乘 \(A^{-1}\)，得到

\[
y=0.
\]

所以

\[
z=x+iy=x,
\]

即

\[
\boxed{z_1,z_2,z_3,z_4\text{ 都是实数}.}
\]

知识点：

证明复数未知量其实是实数，常用套路是：

1. 写成 \(z=x+iy\)；
2. 把方程拆成实部和虚部；
3. 如果右端是实数，则虚部方程通常是齐次方程；
4. 如果对应矩阵可逆，则齐次方程只有零解；
5. 虚部为零，所以原复数是实数。

这也是线性代数和复数结合题里非常常见的证明结构。

---

### IMG_0859：解含共轭的复数方程 \(5z^2+4\bar z+8=0\)

题目要求找非实数解的共同实部。设

\[
z=x+iy,\qquad x,y\in\mathbb R.
\]

那么

\[
\bar z=x-iy,
\]

且

\[
z^2=(x+iy)^2=x^2-y^2+2ixy.
\]

代入

\[
5z^2+4\bar z+8=0
\]

得

\[
5(x^2-y^2+2ixy)+4(x-iy)+8=0.
\]

把实部和虚部分开：

实部：

\[
5(x^2-y^2)+4x+8=0.
\]

虚部：

\[
10xy-4y=0.
\]

虚部方程可化成

\[
y(10x-4)=0.
\]

题目只考虑非实数解，所以

\[
y\neq 0.
\]

因此必须有

\[
10x-4=0,
\]

即

\[
x=\frac{2}{5}.
\]

所以所有非实数解的共同实部为

\[
\boxed{\frac25}.
\]

如果继续求完整解，把 \(x=\frac25\) 代入实部方程：

\[
5\left(\frac{4}{25}-y^2\right)+\frac85+8=0.
\]

化简：

\[
\frac45-5y^2+\frac85+8=0,
\]

\[
\frac{52}{5}-5y^2=0,
\]

\[
y^2=\frac{52}{25}.
\]

所以

\[
y=\pm \frac{2\sqrt{13}}5.
\]

完整的非实数解是

\[
\boxed{z=\frac25\pm \frac{2\sqrt{13}}5 i}.
\]

知识点：

遇到含 \(\bar z\) 的方程，不要直接套普通一元二次公式，因为 \(\bar z\) 不是 \(z\) 的多项式表达式。标准做法是：

1. 设 \(z=x+iy\)；
2. 写出 \(\bar z=x-iy\)；
3. 展开后比较实部和虚部；
4. 根据“非实数”条件使用 \(y\neq 0\)。

---

### IMG_0859：单位根判断题

题目讨论 \(n\) 次单位根：

\[
w^n=1,\qquad n\ge 2.
\]

单位根的基本事实是：

\[
|w|=1.
\]

因为

\[
|w|^n=|w^n|=1,
\]

所以

\[
|w|=1.
\]

逐项判断：

1. \(w^k=-1\) for some \(k\in\mathbb Z\)

这不是对所有 \(n\) 次单位根都成立。例如 \(w=1\) 本身也是单位根，但

\[
1^k=1
\]

永远不等于 \(-1\)。所以该项为假。

2. \(|w^j-w^k|=|\arg(w^j)-\arg(w^k)|\) for all \(j,k\in\mathbb Z\)

左边是复平面中两个点的弦长，右边是角度差的绝对值。弦长一般不等于角度差。例如单位圆上角度差为 \(\pi\) 的两点距离是 \(2\)，但角度差是 \(\pi\)。所以该项为假。

3. \(w^k\) is also an \(n\)th root of unity for any \(k\in\mathbb Z\)

因为

\[
(w^k)^n=w^{kn}=(w^n)^k=1^k=1.
\]

所以该项为真。

4. If \(w_1,w_2\) are two distinct \(n\)th roots of unity, then \(\left|\frac{w_1}{w_2}\right|<1\)

因为

\[
\left|\frac{w_1}{w_2}\right|=\frac{|w_1|}{|w_2|}=\frac11=1.
\]

不是小于 \(1\)。所以该项为假。

5. \(z=\frac{w^a}{w^b}\), where \(a,b\in\mathbb Z\), is also an \(n\)th root of unity

因为

\[
\frac{w^a}{w^b}=w^{a-b}.
\]

于是

\[
(w^{a-b})^n=(w^n)^{a-b}=1.
\]

所以该项为真。

因此应选：

\[
\boxed{\text{第 3 项和第 5 项。}}
\]

知识点：

单位根题最常用的三件事：

1. \(w^n=1\)；
2. \(|w|=1\)；
3. 幂指数可以模 \(n\) 化简，即 \(w^{m+n}=w^m\)。

---

### IMG_0860-0861：不用显式求根，证明 \(w^{47}+w^{63}\) 是 \(x^2+x-1\) 的根

题目给 \(w\) 是一个 \(5\) 次单位根，且

\[
w\neq 1.
\]

单位根的重要公式：

\[
1+w+w^2+w^3+w^4=0.
\]

因为 \(w^5=1\)，指数可以模 \(5\) 化简：

\[
47\equiv 2\pmod 5,
\]

\[
63\equiv 3\pmod 5.
\]

所以

\[
w^{47}+w^{63}=w^2+w^3.
\]

令

\[
x=w^2+w^3.
\]

我们要证明

\[
x^2+x-1=0.
\]

计算：

\[
x^2+x-1=(w^2+w^3)^2+(w^2+w^3)-1.
\]

展开：

\[
=w^4+2w^5+w^6+w^2+w^3-1.
\]

因为

\[
w^5=1,\qquad w^6=w,
\]

所以

\[
x^2+x-1=w^4+2+w+w^2+w^3-1.
\]

整理：

\[
=1+w+w^2+w^3+w^4.
\]

而 \(w\neq 1\) 且 \(w^5=1\)，所以

\[
1+w+w^2+w^3+w^4=0.
\]

因此

\[
x^2+x-1=0.
\]

也就是说

\[
\boxed{w^{47}+w^{63}\text{ 是 }x^2+x-1\text{ 的根}.}
\]

知识点：

单位根证明题通常不要显式把根写成三角式或根式。更干净的方法是：

1. 用 \(w^n=1\) 把高次幂降次；
2. 如果 \(w\neq 1\)，用

\[
1+w+\cdots+w^{n-1}=0;
\]

3. 把目标表达式化到这些标准形式。

---

### IMG_0862：解释 \(\bar p=-p\) 的代数链条

题目设 \(w,-w,v\) 是三个不同的 \(10\) 次单位根，并定义

\[
p=\frac{v-w}{v+w}.
\]

Michael 写出：

\[
\bar p
=\frac{\bar v-\bar w}{\bar v+\bar w}
=\frac{1/v-1/w}{1/v+1/w}
=\frac{w-v}{w+v}
=-p.
\]

我们逐步解释。

第一步：

\[
\bar p=\overline{\frac{v-w}{v+w}}
=\frac{\bar v-\bar w}{\bar v+\bar w}.
\]

这是因为复共轭满足

\[
\overline{\frac{a}{b}}=\frac{\bar a}{\bar b}\qquad (b\neq 0).
\]

第二步：

因为 \(w\) 和 \(v\) 都是单位根，所以它们在单位圆上：

\[
|w|=|v|=1.
\]

单位圆上的复数满足

\[
\bar w=\frac1w,\qquad \bar v=\frac1v.
\]

因此

\[
\frac{\bar v-\bar w}{\bar v+\bar w}
=\frac{1/v-1/w}{1/v+1/w}.
\]

第三步，把分子分母同乘 \(vw\)：

\[
\frac{1/v-1/w}{1/v+1/w}
=\frac{w-v}{w+v}.
\]

第四步：

\[
\frac{w-v}{w+v}
=-\frac{v-w}{v+w}
=-p.
\]

所以

\[
\boxed{\bar p=-p.}
\]

知识点：

如果一个复数 \(p\) 满足

\[
\bar p=-p,
\]

那么 \(p\) 是纯虚数。因为设 \(p=a+bi\)，则

\[
\bar p=a-bi.
\]

方程 \(\bar p=-p\) 变成

\[
a-bi=-a-bi,
\]

所以

\[
a=0.
\]

因此 \(p\) 的实部为 \(0\)，它在复平面上位于虚轴上。

---

### IMG_0863-0864：用 \(\bar p=-p\) 判断三角形中的角

题目中 \(w,-w,v\) 是三个不同的 \(10\) 次单位根，可以看成单位圆上的三个点：

\[
W=w,\qquad M=-w,\qquad V=v.
\]

从点 \(V\) 出发的两条边对应的复数向量是：

\[
\overrightarrow{VW}=w-v,
\]

\[
\overrightarrow{VM}=-w-v=-(w+v).
\]

两个复数向量的夹角可以通过它们的商来判断。具体地，如果

\[
\frac{\overrightarrow{VW}}{\overrightarrow{VM}}
\]

是纯虚数，那么两条向量相差的辐角是 \(\pm\frac{\pi}{2}\)，也就是两条边垂直。

计算这个商：

\[
\frac{\overrightarrow{VW}}{\overrightarrow{VM}}
=\frac{w-v}{-w-v}
=-\frac{w-v}{w+v}
=\frac{v-w}{v+w}
=p.
\]

上一问已经证明

\[
\bar p=-p.
\]

这说明 \(p\) 是纯虚数。因此

\[
\overrightarrow{VW}
\]

和

\[
\overrightarrow{VM}
\]

垂直。

题目要求

\[
0\le \gamma\le \frac{\pi}{2},
\]

所以角度为

\[
\boxed{\gamma=\frac{\pi}{2}}.
\]

Maple 输入格式：

\[
\boxed{\text{Pi/2}}.
\]

知识点：

复平面里，一个复数商

\[
\frac{z_1}{z_2}
\]

的辐角等于 \(z_1\) 和 \(z_2\) 的辐角差。因此：

1. 如果 \(\frac{z_1}{z_2}\) 是正实数，两向量同向；
2. 如果 \(\frac{z_1}{z_2}\) 是负实数，两向量反向；
3. 如果 \(\frac{z_1}{z_2}\) 是纯虚数，两向量垂直。

本题就是用 \(\bar p=-p\) 说明 \(p\) 是纯虚数，再把它解释成两条边的方向比。

---

### IMG_0865-0866：各种“消去律”的真假判断

题目想比较普通实数乘法中的消去律：

\[
xz=yz,\quad z\neq 0\Longrightarrow x=y
\]

能不能推广到向量、矩阵和叉积。

#### (a) 点积版本是假的

命题：

\[
x\cdot z=y\cdot z\Longrightarrow x=y,
\qquad x,y,z\in\mathbb R^2,\ z\neq 0.
\]

这是假的。反例：

\[
x=(1,0),\qquad y=(-1,0),\qquad z=(0,1).
\]

则

\[
x\cdot z=(1,0)\cdot(0,1)=0,
\]

\[
y\cdot z=(-1,0)\cdot(0,1)=0.
\]

所以

\[
x\cdot z=y\cdot z.
\]

但显然

\[
x\neq y.
\]

知识点：

点积 \(x\cdot z\) 只测量 \(x\) 在 \(z\) 方向上的分量。两个不同向量可以有相同的投影，所以不能从一个点积等式推出两个向量相等。

#### (b) 矩阵乘同一个向量的版本是假的

命题：

\[
Xz=Yz\Longrightarrow X=Y,
\]

其中 \(X,Y\) 是 \(2\times 2\) 矩阵，\(z\neq 0\)。

这也是假的。取

\[
z=
\begin{pmatrix}
1\\
0
\end{pmatrix},
\]

\[
X=
\begin{pmatrix}
1&0\\
0&0
\end{pmatrix},
\qquad
Y=
\begin{pmatrix}
1&2\\
0&3
\end{pmatrix}.
\]

则

\[
Xz=
\begin{pmatrix}
1\\
0
\end{pmatrix},
\qquad
Yz=
\begin{pmatrix}
1\\
0
\end{pmatrix}.
\]

所以

\[
Xz=Yz.
\]

但

\[
X\neq Y.
\]

知识点：

一个矩阵乘以一个固定向量，只告诉你矩阵对这个向量的作用，不能告诉你矩阵对所有向量的作用。两个不同矩阵可以在某一个向量上给出同样结果。

#### (c) 向量加法版本是真的

命题：

\[
x+z=y+z\Longrightarrow x=y.
\]

这是正确的。因为向量加法有加法逆元。两边同时加上 \(-z\)：

\[
x+z-z=y+z-z.
\]

得到

\[
x=y.
\]

所以反例应填：

\[
\boxed{\text{none}}.
\]

知识点：

向量加法和实数加法一样有消去律，因为每个向量 \(z\) 都有加法逆元 \(-z\)。

#### (d) 叉积消去版本是假的

命题：

\[
x\times z=y\times z\Longrightarrow x=y,
\qquad x,y,z\in\mathbb R^3,\ z\neq 0.
\]

这是假的。取

\[
x=(1,0,0),\qquad y=(1,1,0),\qquad z=(0,1,0).
\]

计算：

\[
x\times z=(1,0,0)\times(0,1,0)=(0,0,1).
\]

而

\[
y\times z=(1,1,0)\times(0,1,0)=(0,0,1).
\]

所以

\[
x\times z=y\times z,
\]

但

\[
x\neq y.
\]

知识点：

如果

\[
x\times z=y\times z,
\]

那么

\[
(x-y)\times z=0.
\]

这只说明 \(x-y\) 与 \(z\) 平行，不一定说明 \(x-y=0\)。所以叉积不能像普通实数乘法那样消去。

#### (e) 若 \(x+y+z=0\)，则 \(x\times y=y\times z\) 是真的

题目命题：

\[
x+y+z=0\Longrightarrow x\times y=y\times z.
\]

由

\[
x+y+z=0
\]

可得

\[
z=-x-y.
\]

于是

\[
y\times z=y\times(-x-y).
\]

利用叉积分配律：

\[
y\times(-x-y)=y\times(-x)+y\times(-y).
\]

即

\[
=-y\times x-y\times y.
\]

因为

\[
y\times y=0,
\]

且叉积反交换：

\[
y\times x=-x\times y,
\]

所以

\[
-y\times x=x\times y.
\]

因此

\[
y\times z=x\times y.
\]

也就是

\[
\boxed{x\times y=y\times z}.
\]

所以反例应填：

\[
\boxed{\text{none}}.
\]

知识点：

叉积的两个核心代数性质：

1. 分配律：

\[
a\times(b+c)=a\times b+a\times c;
\]

2. 反交换律：

\[
a\times b=-b\times a.
\]

特别地，

\[
a\times a=0.
\]

这题不是“叉积可以消去”，而是利用 \(x+y+z=0\) 把 \(z\) 换成 \(-x-y\)，再用叉积性质化简。

### IMG_0867-0868：行列式、标量三重积与线性相关

题目设 \(u,v,w\in\mathbb R^3\)，矩阵 \(A\) 的三列分别是

\[
u,\ v,\ w.
\]

所以

\[
\det(A)=\det[u\ v\ w].
\]

核心知识点：

\[
\det[u\ v\ w]=u\cdot(v\times w)=v\cdot(w\times u)=w\cdot(u\times v).
\]

这叫标量三重积。它有两个重要含义：

1. 代数含义：三列向量线性相关时，行列式为 \(0\)；
2. 几何含义：\(|\det(A)|\) 是由 \(u,v,w\) 张成的平行六面体体积。

#### (a) 若 \(v\) 垂直于 \(u\times w\)，则 \(\det(A)=0\)

这是对的。

题目条件是

\[
v\perp u\times w.
\]

所以

\[
v\cdot(u\times w)=0.
\]

而叉积反交换：

\[
w\times u=-(u\times w).
\]

因此

\[
\det(A)=v\cdot(w\times u)
=v\cdot[-(u\times w)]
=-v\cdot(u\times w)
=0.
\]

所以反例填：

\[
\boxed{\text{none}}.
\]

知识点：

看到“某向量垂直于叉积”时，要立刻想到点积为 \(0\)。再把它和三重积公式联系起来。

#### (b) 若 \(\det(A)=\pm1\)，则 \(\{u,v,w\}\) 必为标准正交组

这是错的。

反例可以取：

\[
u=(-2,0,0),\quad v=\left(0,\frac12,0\right),\quad w=(0,0,1).
\]

此时

\[
A=
\begin{pmatrix}
-2&0&0\\
0&1/2&0\\
0&0&1
\end{pmatrix}.
\]

所以

\[
\det(A)=(-2)\cdot\frac12\cdot1=-1.
\]

确实满足

\[
\det(A)=\pm1.
\]

但是

\[
\|u\|=2,\qquad \|v\|=\frac12,
\]

它们不是单位向量，所以 \(\{u,v,w\}\) 不是标准正交组。

知识点：

\(\det(A)=\pm1\) 只说明体积大小为 \(1\)，不说明三条边都长度为 \(1\)，也不说明它们两两垂直。体积可以由“长边”和“短边”互相抵消出来。

#### (c) 若 \(\det(A)=0\)，则存在非零向量同时垂直于 \(u,v,w\)

这是对的。

\[
\det(A)=0
\]

说明矩阵 \(A\) 的三列线性相关，也就是说

\[
\operatorname{span}\{u,v,w\}
\]

不是整个 \(\mathbb R^3\)，它的维数最多为 \(2\)。

在 \(\mathbb R^3\) 中，如果一个子空间维数小于 \(3\)，那么它的正交补一定有非零向量。也就是说，存在

\[
n\ne0
\]

使得

\[
n\cdot u=0,\qquad n\cdot v=0,\qquad n\cdot w=0.
\]

所以反例填：

\[
\boxed{\text{none}}.
\]

知识点：

这一问考的是“行列式为零”等价于列向量线性相关。三维空间中，三个向量如果只张成一条线或一个平面，就一定有一个非零法向量垂直于它们张成的空间。

#### (d) 若 \(w=u-v\)，则 \(\det(A)=0\)

这是对的。

因为

\[
w=u-v.
\]

于是三列向量满足

\[
w-u+v=0.
\]

这说明 \(u,v,w\) 线性相关，所以

\[
\det(A)=0.
\]

反例填：

\[
\boxed{\text{none}}.
\]

知识点：

只要某一列可以由其他列线性组合得到，矩阵列向量就线性相关，行列式必为 \(0\)。

#### (e) 若 \(\det(A)=0\)，则 \(u,v,w\) 中必须有一个是零向量

这是错的。

反例可以取：

\[
u=(1,1,1),\quad v=(2,2,2),\quad w=(3,3,3).
\]

这三个向量都不是零向量，但它们全部在同一条直线上：

\[
v=2u,\qquad w=3u.
\]

所以它们线性相关，行列式为

\[
\det(A)=0.
\]

知识点：

行列式为零只说明列向量线性相关，不说明某一列一定是零向量。非零向量也可以因为互为倍数而线性相关。

### IMG_0869-0872：变上限、变下限积分求导

题目要求计算

\[
\frac{d}{dx}\int_{x^2}^{\ln x}g(t)\,dt,
\]

其中 \(g\) 是连续实值函数。

#### (a) 用辅助函数拆开上下限

设

\[
G(x)=\int_{x^2}^{\ln x}g(t)\,dt,
\]

再设

\[
H(x)=\int_a^x g(t)\,dt.
\]

那么

\[
G(x)=H(\ln x)-H(x^2).
\]

题目写成复合函数：

\[
G(x)=(H\circ f_2)(x)-(H\circ f_1)(x).
\]

所以

\[
\boxed{f_1(x)=x^2},\qquad \boxed{f_2(x)=\ln x}.
\]

知识点：

把

\[
\int_{\text{下限}}^{\text{上限}}g(t)\,dt
\]

拆成

\[
\int_a^{\text{上限}}g(t)\,dt-\int_a^{\text{下限}}g(t)\,dt
\]

是处理变上下限积分最标准的方法。中间点 \(a\) 只是辅助点，最后会抵消，不影响答案。

#### (b) 套用微积分基本定理和链式法则

由微积分基本定理：

\[
H'(x)=g(x).
\]

因为

\[
G(x)=H(\ln x)-H(x^2),
\]

所以用链式法则：

\[
G'(x)=H'(\ln x)\cdot\frac1x-H'(x^2)\cdot2x.
\]

再把 \(H'(u)=g(u)\) 代入：

\[
G'(x)=g(\ln x)\cdot\frac1x-g(x^2)\cdot2x.
\]

因此题目中的

\[
\frac{dG}{dx}(x)=(g\circ A)(x)B(x)-(g\circ C)(x)D(x)
\]

对应：

\[
\boxed{A(x)=\ln x},\qquad
\boxed{B(x)=\frac1x},
\]

\[
\boxed{C(x)=x^2},\qquad
\boxed{D(x)=2x}.
\]

最终答案：

\[
\boxed{\frac{d}{dx}\int_{x^2}^{\ln x}g(t)\,dt
=\frac{g(\ln x)}{x}-2x\,g(x^2)}.
\]

知识点：

上限贡献是正的：

\[
g(\text{上限})\cdot(\text{上限})',
\]

下限贡献是负的：

\[
-g(\text{下限})\cdot(\text{下限})'.
\]

所以公式可以记成：

\[
\frac{d}{dx}\int_{a(x)}^{b(x)}g(t)\,dt
=g(b(x))b'(x)-g(a(x))a'(x).
\]

#### (c) 一般公式的证明和条件

一般地，若

\[
F(x)=\int_{f_1(x)}^{f_2(x)}g(t)\,dt,
\]

取辅助函数

\[
H(u)=\int_a^u g(t)\,dt.
\]

则

\[
F(x)=H(f_2(x))-H(f_1(x)).
\]

如果 \(g\) 连续，则微积分基本定理给出

\[
H'(u)=g(u).
\]

再对 \(F\) 求导：

\[
F'(x)=H'(f_2(x))f_2'(x)-H'(f_1(x))f_1'(x).
\]

代入 \(H'(u)=g(u)\)，得到

\[
\boxed{
F'(x)=g(f_2(x))f_2'(x)-g(f_1(x))f_1'(x)
}.
\]

必要条件：

1. \(g\) 至少要在相关积分区间上连续；
2. \(f_1,f_2\) 要在所讨论的 \(x\) 附近可导；
3. \(f_1(x),f_2(x)\) 的值要落在 \(g\) 的定义域内。

知识点：

这不是一个单独的新公式，而是两个定理叠加：

\[
\text{微积分基本定理}+\text{链式法则}.
\]

以后遇到

\[
\int_{a(x)}^{b(x)}g(t)\,dt
\]

就直接写：

\[
\boxed{g(b(x))b'(x)-g(a(x))a'(x)}.
\]

### IMG_0873：导数界、零点个数与反函数

题目给 \(k:[-6,8]\to\mathbb R\) 连续，在 \((-6,8)\) 可导，并且

\[
2<k'(x)<9.
\]

#### (a) \(k(-6)\) 和 \(k(8)\) 都是二次方程的根

二次方程是

\[
z^2+\mu z-\nu^2=0,\qquad \nu\ne0.
\]

它的两个根乘积为

\[
-\nu^2<0.
\]

所以两个根一正一负。因此 \(k(-6)\) 和 \(k(8)\) 一正一负。

因为 \(k\) 连续，由介值定理，\(k\) 在 \((-6,8)\) 至少有一个零点。

又因为

\[
k'(x)>2>0,
\]

所以 \(k\) 严格递增。严格递增函数最多只能有一个零点。

因此答案是

\[
\boxed{\text{exactly one zero}}.
\]

知识点：

“至少一个零点”常用介值定理；“最多一个零点”常用严格单调。这里 \(k'(x)>0\) 给出严格递增。

#### (b) 已知 \(k(-6)=-22\)，求 \(k(8)\) 的最大下界

由拉格朗日中值定理，存在 \(c\in(-6,8)\)，使得

\[
\frac{k(8)-k(-6)}{8-(-6)}=k'(c).
\]

也就是

\[
\frac{k(8)+22}{14}=k'(c).
\]

因为

\[
2<k'(c)<9,
\]

所以

\[
2<\frac{k(8)+22}{14}<9.
\]

左边给出

\[
k(8)+22>28,
\]

即

\[
\boxed{k(8)>6}.
\]

所以能推出的最大下界是 \(6\)，但注意是严格大于：

\[
\boxed{6}.
\]

知识点：

导数的上下界可以通过中值定理变成函数值差的上下界：

\[
m<k'(x)<M
\Longrightarrow
m(b-a)<k(b)-k(a)<M(b-a).
\]

#### (c) 反函数与反函数图像斜率

Shuai 说：“因为 \(k'(x)\ge0\)，所以 \(k\) 可逆。”这个论证不严谨，所以答案是

\[
\boxed{\text{No}}.
\]

原因是 \(k'(x)\ge0\) 本身不一定推出严格递增，例如常值函数导数为 \(0\)，但不可逆。真正有力的条件是这里的

\[
k'(x)>2>0,
\]

它能推出 \(k\) 严格递增，因此可逆。

反函数图像端点对应：

\[
(-6,k(-6))=(-6,-22),\qquad (8,k(8)).
\]

反函数 \(k^{-1}\) 的图像会经过

\[
(-22,-6),\qquad (k(8),8).
\]

这两点之间割线斜率为

\[
\frac{8-(-6)}{k(8)-(-22)}
=\frac{14}{k(8)+22}.
\]

由中值定理，反函数图像上至少一点的切线斜率等于这个割线斜率。所以

\[
\boxed{\delta=14}.
\]

知识点：

反函数会交换坐标：

\[
(x,k(x))\longleftrightarrow(k(x),x).
\]

所以反函数图像的割线斜率要用交换后的两个点来算。

### IMG_0874：极限等价不代表差趋于零

#### (a) 求比值极限

题目给

\[
\lim_{x\to\infty}
\frac{\sqrt{49x^2+60\ln(\cosh x)+p(x)}}
{\sqrt{x^2+81x^\nu+q(x)}}=L,
\]

其中

\[
p(x)\to0,\qquad q(x)\to0,\qquad \nu<1.
\]

当 \(x\to\infty\) 时，

\[
\ln(\cosh x)\sim x-\ln2,
\]

所以 \(60\ln(\cosh x)\) 是一阶量，而 \(49x^2\) 是二阶量。分子主导项是

\[
\sqrt{49x^2}=7x.
\]

分母中 \(81x^\nu\) 的阶数低于 \(x\)，更低于 \(x^2\)，所以分母主导项是

\[
\sqrt{x^2}=x.
\]

因此

\[
\boxed{L=7}.
\]

知识点：

比值极限只看最高阶主导项。低阶项会影响“差”的极限，但不会影响“比”的极限。

#### (b) 求 \(u(x)-v(x)\) 的极限

令

\[
u(x)=\sqrt{49x^2+60\ln(\cosh x)+p(x)},
\]

\[
v(x)=7\sqrt{x^2+81x^\nu+q(x)}.
\]

虽然

\[
\frac{u(x)}{v(x)}\to1,
\]

但这不代表 \(u(x)-v(x)\to0\)。

先看分子：

\[
u(x)
=7x\sqrt{1+\frac{60\ln(\cosh x)+p(x)}{49x^2}}.
\]

用近似

\[
\sqrt{1+\varepsilon}=1+\frac{\varepsilon}{2}+o(\varepsilon),
\]

得

\[
u(x)=7x+\frac{60\ln(\cosh x)}{14x}+o(1).
\]

因为

\[
\frac{\ln(\cosh x)}x\to1,
\]

所以

\[
u(x)=7x+\frac{30}{7}+o(1).
\]

而

\[
v(x)=7x+o(1),
\]

因为 \(\nu<1\) 时 \(x^\nu/x\to0\)，对应修正项趋于 \(0\)。

所以

\[
\boxed{\lim_{x\to\infty}[u(x)-v(x)]=\frac{30}{7}}.
\]

知识点：

\[
\frac{u}{v}\to1
\]

只说明相对误差趋于 \(0\)，不说明绝对误差趋于 \(0\)。

#### (c) 构造 \(\frac{f}{g}\to1\)，但 \(f-g\to\infty\)

取

\[
\boxed{f(x)=x+\ln x,\qquad g(x)=x}.
\]

则

\[
\frac{f(x)}{g(x)}
=\frac{x+\ln x}{x}
=1+\frac{\ln x}{x}\to1.
\]

但

\[
f(x)-g(x)=\ln x\to\infty.
\]

Maple 形式：

\[
\boxed{x+\ln(x),\ x}.
\]

知识点：

“相对误差小”不等于“绝对误差小”。当基准量 \(g(x)\to\infty\) 时，即使相对误差趋于 \(0\)，差值仍可能趋于无穷。

### IMG_0875-0878：洛必达法则的反向推断与连续性

#### (a) 导数比极限不存在，能否推出原函数比极限不存在？

Julie 的想法是：

如果

\[
\lim_{x\to a}\frac{g'(x)}{k'(x)}
\]

不存在，那么

\[
\lim_{x\to a}\frac{g(x)}{k(x)}
\]

也一定不存在。

这个想法是错的，所以答案是

\[
\boxed{\text{incorrect}}.
\]

反例可以取

\[
g(x)=x^2,\qquad k(x)=x^2(2+\sin(1/x))
\]

在 \(x\to0\) 附近讨论，并适当定义 \(g(0)=k(0)=0\)。不过这个反例的导数比检查较麻烦。

更简单的思路是：洛必达法则只说“若导数比极限存在，在满足条件时可推出函数比极限”；它不说“导数比极限不存在时，函数比极限一定不存在”。这叫不能随便取逆否命题。

知识点：

如果定理是

\[
P\Rightarrow Q,
\]

那么不能推出

\[
\neg P\Rightarrow \neg Q.
\]

这在逻辑上叫“否定前件谬误”。

#### (b) Julie 的 conjecture

命题是：

若

\[
\lim_{x\to a}\frac{g'(x)}{f'(x)}
\]

存在且不等于 \(0\)，并且

\[
\lim_{x\to a}\frac{f(x)}{k(x)}
\]

不存在，则

\[
\lim_{x\to a}\frac{g(x)}{k(x)}
\]

也不存在。

这个命题是对的。

证明：

因为

\[
\lim_{x\to a}g(x)=\lim_{x\to a}f(x)=0,
\]

并且

\[
\lim_{x\to a}\frac{g'(x)}{f'(x)}
\]

存在且为非零数，设为 \(c\ne0\)。由洛必达法则，

\[
\lim_{x\to a}\frac{g(x)}{f(x)}=c\ne0.
\]

又有恒等变形：

\[
\frac{g(x)}{k(x)}
=\frac{g(x)}{f(x)}\cdot\frac{f(x)}{k(x)}.
\]

假设反过来

\[
\lim_{x\to a}\frac{g(x)}{k(x)}
\]

存在。由于

\[
\frac{g(x)}{f(x)}\to c\ne0,
\]

所以

\[
\frac{f(x)}{g(x)}\to\frac1c.
\]

于是

\[
\frac{f(x)}{k(x)}
=\frac{f(x)}{g(x)}\cdot\frac{g(x)}{k(x)}
\]

也应该存在极限。这和题设

\[
\lim_{x\to a}\frac{f(x)}{k(x)}
\]

不存在矛盾。

因此

\[
\boxed{\lim_{x\to a}\frac{g(x)}{k(x)}\text{ 不存在}}.
\]

知识点：

乘上一个趋于非零常数的因子，不会把“不存在极限”的振荡或发散问题消掉。证明时最好用反证法。

#### (c) 分段函数在 \(0\) 处连续

函数为

\[
F(x)=
\begin{cases}
\dfrac{e^{x^p}-1}{x^n},&x\ne0,\\
b,&x=0,
\end{cases}
\]

其中 \(p\) 是正整数，\(n\in\mathbb Z\)。

当 \(x\to0\) 时，

\[
e^{x^p}-1\sim x^p.
\]

所以

\[
\frac{e^{x^p}-1}{x^n}\sim x^{p-n}.
\]

分三种情况：

1. 若 \(n>p\)，则 \(p-n<0\)，极限发散，不可能靠选 \(b\) 变连续；
2. 若 \(n=p\)，极限为 \(1\)，选 \(b=1\) 可连续；
3. 若 \(n<p\)，极限为 \(0\)，选 \(b=0\) 可连续。

所以临界整数是

\[
\boxed{N=p}.
\]

当 \(p=11\) 时，可以取两组不同的 \((n,b)\)：

\[
n_1=10,\quad b_1=0;
\]

\[
n_2=11,\quad b_2=1.
\]

答案可写为

\[
\boxed{[10,0],[11,1]}.
\]

知识点：

指数函数在 \(0\) 附近的基本等价是

\[
e^y-1\sim y.
\]

这里令 \(y=x^p\)，得到

\[
e^{x^p}-1\sim x^p.
\]

### IMG_0879-0880：黎曼和、单调性与凸性

题目设

\[
K=\int_2^5 g(x)\,dx.
\]

区间长度是 \(3\)，常见比较对象是宽度为 \(1\) 的三个矩形面积。

#### (a) 若 \(g'(x)>0\)，比较 \(K\) 与 \(D=g(3)+g(4)+g(5)\)

因为 \(g'(x)>0\)，所以 \(g\) 严格递增。

在每个区间

\[
[2,3],\ [3,4],\ [4,5]
\]

上，右端点函数值分别是最大值：

\[
g(3),\quad g(4),\quad g(5).
\]

因此右端点黎曼和高估积分：

\[
\int_2^5 g(x)\,dx<g(3)+g(4)+g(5).
\]

所以

\[
\boxed{K<D}.
\]

知识点：

递增函数：左端点和低估，右端点和高估。

#### (b) 若 \(g'(x)>0\)，比较 \(K\) 与 \(E=g(2.5)+g(3.5)+g(4.5)\)

\(E\) 是中点和。仅知道函数递增，并不能判断中点和一定高估还是低估。

例如线性函数时中点和等于积分；某些弯曲方式下可能高估或低估。

所以答案是：

\[
\boxed{\text{信息不足}}.
\]

知识点：

单调性只能控制左/右端点和，不能单独决定中点和误差方向。

#### (c) 若 \(g''(x)=0\)，比较 \(K\) 与 \(E\)

\[
g''(x)=0
\]

说明 \(g\) 是一次函数：

\[
g(x)=ax+b.
\]

中点法则对一次函数是精确的。每个长度为 \(1\) 的小区间上，矩形高取中点值，面积刚好等于该段积分。

所以

\[
\boxed{K=E}.
\]

知识点：

中点法对线性函数精确，因为线性函数在区间上的平均值等于中点值。

#### (d) 若 \(g''(x)>0\)，比较 \(K\) 与 \(E\)

\[
g''(x)>0
\]

说明 \(g\) 是凸函数。

对凸函数，中点值低于区间平均值。也就是说在每个小区间上：

\[
g(\text{中点})<\text{该区间上的平均函数值}.
\]

所以中点和低估积分：

\[
E<K.
\]

因此

\[
\boxed{K>E}.
\]

知识点：

凸函数图像在弦的下方，但区间平均值会大于中点值；所以中点法对凸函数低估积分。

### IMG_0881-0882：由范数放大推出 \(I+A\) 可逆

题目设 \(A\) 是 \(m\times m\) 矩阵，并且对每个非零向量 \(x\in\mathbb R^m\)，都有

\[
|Ax|>|x|.
\]

#### (a) 证明 \(|(I+A)x|\ne0\)

用反证法。

假设存在非零向量 \(x\)，使得

\[
|(I+A)x|=0.
\]

向量范数为 \(0\) 等价于向量本身为 \(0\)，所以

\[
(I+A)x=0.
\]

即

\[
x+Ax=0.
\]

因此

\[
Ax=-x.
\]

两边取范数：

\[
|Ax|=|-x|=|x|.
\]

这与题目条件

\[
|Ax|>|x|
\]

矛盾。

所以对所有非零 \(x\)，

\[
\boxed{|(I+A)x|\ne0}.
\]

知识点：

范数的两个基本事实：

\[
|y|=0\Longleftrightarrow y=0,
\]

\[
|-x|=|x|.
\]

#### (b) 证明 \(I+A\) 可逆

令

\[
B=I+A.
\]

一个方阵可逆，当且仅当它的零空间只有零向量，也就是

\[
Bx=0\Longrightarrow x=0.
\]

由 (a) 已知：只要 \(x\ne0\)，就有

\[
(I+A)x\ne0.
\]

因此如果

\[
(I+A)x=0,
\]

那么不可能 \(x\ne0\)，只能是

\[
x=0.
\]

所以齐次方程只有零解，故

\[
\boxed{I+A\text{ 可逆}}.
\]

知识点：

证明方阵可逆的常见方法：

1. 证明行列式不为 \(0\)；
2. 证明列向量线性无关；
3. 证明齐次方程 \(Bx=0\) 只有零解。

这一题最自然用第 3 种。

### IMG_0883-0885：分块矩阵与可逆性

题目给 \(2\times2\) 可逆矩阵 \(A\)，并且

\[
A\binom{-2}{6}=\binom72.
\]

#### (a) 解 \(Bx=(7,2,-7,-2)^T\)

矩阵 \(B\) 相当于两个 \(A\) 放在对角线上：

\[
B\binom{r}{s}
=
\binom{Ar}{As},
\]

其中 \(r,s\in\mathbb R^2\)。

要让

\[
Ar=\binom72,
\]

可取

\[
r=\binom{-2}{6}.
\]

要让

\[
As=\binom{-7}{-2}=-\binom72,
\]

可取

\[
s=-\binom{-2}{6}=\binom2{-6}.
\]

所以

\[
\boxed{x=(-2,6,2,-6)^T}.
\]

Maple 形式：

\[
\boxed{<-2,6,2,-6>}.
\]

知识点：

分块对角矩阵会把未知向量拆成两块分别作用。看到 \(B\) 的结构，应把 \(x\) 写成 \((r,s)\)。

#### (b) 当 \(p=0\)，解 \(C_0x=(0,0,7,2)^T\)

把 \(x\) 拆成

\[
x=\binom{r}{s},\qquad r,s\in\mathbb R^2.
\]

由 \(C_p\) 的结构：

\[
C_p\binom{r}{s}
=
\binom{A(r+8s)}{A(pr+s)}.
\]

当 \(p=0\) 时：

\[
C_0\binom{r}{s}
=
\binom{A(r+8s)}{As}.
\]

题目要

\[
A(r+8s)=0,\qquad As=\binom72.
\]

因为 \(A\) 可逆，

\[
r+8s=0,\qquad s=\binom{-2}{6}.
\]

于是

\[
r=-8s=-8\binom{-2}{6}=\binom{16}{-48}.
\]

所以

\[
\boxed{x=(16,-48,-2,6)^T}.
\]

Maple 形式：

\[
\boxed{<16,-48,-2,6>}.
\]

知识点：

可逆矩阵 \(A\) 满足：

\[
Ay=0\Longrightarrow y=0.
\]

所以能把 \(A(r+8s)=0\) 直接化成 \(r+8s=0\)。

#### (c)(d) 判断 \(C_p\) 何时可逆

继续写

\[
x=\binom{r}{s}.
\]

则

\[
C_p\binom{r}{s}
=
\binom{A(r+8s)}{A(pr+s)}.
\]

因为 \(A\) 可逆，\(C_p\) 是否可逆只取决于从 \((r,s)\) 到

\[
(r+8s,\ pr+s)
\]

这个变换是否可逆。

对应的 \(2\times2\) 标量矩阵是

\[
\begin{pmatrix}
1&8\\
p&1
\end{pmatrix}.
\]

它的行列式是

\[
1-8p.
\]

所以 \(C_p\) 可逆当且仅当

\[
1-8p\ne0.
\]

即

\[
\boxed{p\ne\frac18}.
\]

知识点：

大分块矩阵有时可以降维看。这里 \(A\) 只是对每一块都做同一个可逆变换，真正决定可逆性的是块之间的线性组合系数。

#### (e)(f) 当 \(C_p\) 不可逆时，求 \(C_px=0\) 的通解

不可逆发生在

\[
p=\frac18.
\]

齐次方程为

\[
C_p\binom{r}{s}=0.
\]

等价于

\[
A(r+8s)=0,\qquad A\left(\frac18r+s\right)=0.
\]

因为 \(A\) 可逆，所以

\[
r+8s=0,\qquad \frac18r+s=0.
\]

这两个方程其实是同一个条件：

\[
r=-8s.
\]

令

\[
s=\binom{t_1}{t_2}.
\]

则

\[
r=\binom{-8t_1}{-8t_2}.
\]

所以通解是

\[
\boxed{x=(-8t_1,-8t_2,t_1,t_2)^T}.
\]

Maple 形式：

\[
\boxed{<-8*t1,-8*t2,t1,t2>}.
\]

知识点：

不可逆时齐次方程有非零解。这里自由变量来自 \(s\)，而 \(r\) 被条件 \(r=-8s\) 决定。

### IMG_0886：积分函数的导数与被积函数连续性

题目定义

\[
I(x)=\int_{-2}^{x}f(t)\,dt.
\]

核心知识点是微积分基本定理：

如果 \(f\) 在 \(x\) 处连续，那么

\[
I'(x)=f(x).
\]

如果 \(f\) 在 \(x\) 处跳跃不连续，\(I'(x)\) 可能不存在。

#### (a) \(f(x)=|x+1|\)

虽然 \(f\) 在 \(x=-1\) 有尖点，但 \(f\) 本身连续，所以 \(I\) 在该点可导，且导数等于 \(f(-1)=0\)。

分段写：

\[
|x+1|=
\begin{cases}
-x-1,&x<-1,\\
0,&x=-1,\\
x+1,&x>-1.
\end{cases}
\]

因此

\[
\boxed{I'(x)=-x-1\quad(x<-1)},
\]

\[
\boxed{I'(-1)=0},
\]

\[
\boxed{I'(x)=x+1\quad(x>-1)}.
\]

知识点：

要看 \(I\) 是否可导，看的是被积函数 \(f\) 是否连续，不是看 \(f\) 自己是否可导。

#### (b) \(f(x)=x\) 当 \(x\ne-1\)，但 \(f(-1)=4\)

这里 \(f\) 在 \(x=-1\) 不连续，因为

\[
\lim_{x\to-1}f(x)=-1,
\]

但

\[
f(-1)=4.
\]

积分对单点函数值不敏感，所以 \(I(x)\) 实际上和积分 \(t\) 的效果一样。导数在 \(-1\) 处等于连续延拓后的值 \(-1\)，不是 \(f(-1)=4\)。

所以

\[
\boxed{I'(x)=x\quad(x<-1)},
\]

\[
\boxed{I'(-1)=-1},
\]

\[
\boxed{I'(x)=x\quad(x>-1)}.
\]

知识点：

改变被积函数在单个点的值，不改变定积分。因此 \(I\) 的导数可能等于“去掉单点异常后的连续值”。

#### (c) 跳跃函数

\[
f(x)=
\begin{cases}
-2,&x<-1,\\
7,&x\ge -1.
\end{cases}
\]

当 \(x<-1\) 时，\(f\) 连续且等于 \(-2\)，所以

\[
\boxed{I'(x)=-2}.
\]

当 \(x>-1\) 时，\(f\) 连续且等于 \(7\)，所以

\[
\boxed{I'(x)=7}.
\]

但在 \(x=-1\) 处，左侧函数值趋势是 \(-2\)，右侧函数值趋势是 \(7\)。因此 \(I\) 在该点左导数和右导数不同：

\[
I'_-(-1)=-2,\qquad I'_+(-1)=7.
\]

所以导数不存在：

\[
\boxed{I'(-1)=\text{not defined}}.
\]

知识点：

被积函数有跳跃不连续时，积分函数通常会出现“折角”：连续但不可导。左导数等于左侧被积函数值，右导数等于右侧被积函数值。
